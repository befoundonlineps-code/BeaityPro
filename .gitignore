# dependencies
/node_modules

# next.js
/.next/
/out/
# the separate build dir NEXT_BUILD_DIR points at, so a verification build
# cannot overwrite the .next a running dev server is serving from
/.next-check/

# production
/build

# env files
.env*.local

# misc
.DS_Store
*.pem
npm-debug.log*
