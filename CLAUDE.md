# BeaityPro

> # 🔴 بوّابةُ أمانٍ سارية — تُقرأ قبل أيّ شغلٍ على الجرد
>
> **ترحيلُ الجرد الحقيقيّ — بأيّ شاشة، القديمةِ أو الجديدة — ممنوعٌ أن يُستعمل
> على عمليّاتٍ حقيقيّةٍ فيها موظّفون، حتى يُبنى تتبّعُ البيع والاستهلاك في
> الخدمة.** أوّلُ جردٍ حقيقيٍّ سيُظهر **عجزًا وهميًّا** ويكتب على المسؤول عن
> المستودع **سجلَّ نقصٍ باسمه لا يُمحى** (ADR-051).
>
> **وثلاثةُ أسبابٍ مستقلّة، كلٌّ منها وحدَه كافٍ:**
>
> **① ميزةٌ لم تُبنَ:** لا مُنشِئَ في الشيفرة كلِّها لمستندِ `sale` ولا
> `service_consumption` — فالرصيدُ «النظريُّ» لا يعرف شيئًا عمّا استُهلك في
> خدمةٍ أو بيع لزبونة. **وليس تأخيرًا في التسجيل، بل غيابًا كاملًا.**
>
> **🔴 ②و③ عطلان في كودٍ يعمل اليوم — لا نقصُ ميزة:**
> **٣.١٣ب** إعادةُ الجرد **تضاعف** الخطأ (`+1800` ثمّ `+26700` بفارق ٣٩ ثانية،
> ونمطٌ في أربعة منتجات) — **فالتحقّقُ نفسُه هو ما يُفسد.**
> **٣.١٣د** جردٌ مُغرِّمٌ يُعكَس **تبقى غرامتُه قائمةً** معلَّقةً على مستندٍ
> أُلغي — **فـ«نصلحه بالعكس» ليس مخرجًا.**
>
> **والعدُّ نفسُه آمن** — الممنوعُ هو `post_stocktake_session` وحدَها.
>
> **⚠️ ولا تُقتبس هذه البوّابةُ بصيغة «خصمٌ من الراتب»** — المبلغُ لا يُحسَب،
> ولا قارئَ لـ`stock_fines`، ولا موديولَ رواتب. **السلسلةُ تنتهي عند صفٍّ في
> القاعدة يسمّي إنسانًا** — والخطرُ مؤجَّلُ الظهور لا منفيّ.
>
> **🔴 وقاعدةٌ تشغيليّةٌ احتياطيّة:** **من احتاج يومًا أن يعكس مستندَ جردٍ عليه
> غرامة: يتوقّف، ولا يستعمل مسارَ العكس العادي، ويُصعِّد للتصميم** — والعكسُ
> موصولٌ بالواجهة، فلا شيءَ يمنع الضغطة غيرُ هذه القاعدة.
>
> ⚠️ **و`stock_fines` صفرٌ اليوم — مؤكَّدًا حيًّا (٢٣ آب) بصلاحياتٍ تستبعد RLS
> سببًا** (`rls_forced = false` والمالكُ معفًى). **وكان مكتوبًا هنا «غرامتان
> قائمتان»، والسببُ تصفيرُ ٠٨٩.** ⇒ **والعيبُ باقٍ لأنه من قراءة الشيفرة لا من
> عدّ الصفوف** — فأوّلُ غرامةٍ تُكتب تقع تحته.
>
> **الشروطُ الأربعةُ لرفعها، وقرارُ المالك، والقياسات كلُّها:**
> [docs/SAFETY-GATE-stocktake.md](docs/SAFETY-GATE-stocktake.md) — **يُقرأ كاملًا
> قبل أيّ لمسٍ لزرّ «حفظ الجرد».**

## ✅ محرّكُ تكلفة الدفعة أُطلق ومُثبَت — والتعليقُ رُفع (١٦ آب ٢٠٢٦)

٠٩٤ · ٠٩٤ج · ٠٩٥ · ٠٩٥ب/أ–د **نُفِّذت كلُّها وأكّدها المالك.** والدوالُّ الأربعُ
**مقيسةٌ بالتشغيل الفعليّ لا بقراءة النصّ**: الانقسامُ إلى حركةٍ لكلّ دفعة ·
FIFO بثمن كلّ دفعةٍ على حدة · المتبقّي مشتقًّا · الدفعةُ المقدَّرةُ على الدرجتين
٢ و٤ · النقلُ بدفعةٍ جديدةٍ في الوجهة · **والغرامةُ سطرًا واحدًا للمنتج
بمتوسّطٍ مرجَّحٍ من الدفعات المسحوبة فعلًا** (٧٤÷١٣ = ٥٫٦٩٢٣) · والعكسُ يعيد كلَّ
شريحةٍ إلى دفعتها هي.

⚠️ **وهذا يثبت الحسابَ والتخطيط، لا العزل** — المحرّرُ يتجاوز RLS و`auth.uid()`
فارغةٌ فيه. **والعزلُ مُثبَتٌ سلفًا بـ٠٩٤ب.**

| الشاشة | حالتُها |
|---|---|
| **شطبُ بضاعة** | ✅ **مُغلَقةٌ** — ٠٩٩ب ٧/٧، حقلُ الرقم مجرَّبٌ حيًّا، وتحقّقٌ مستقلٌّ على معاملاتٍ حقيقيّةٍ عند المالك طابق `unit_cost` بالحركة مع `unit_cost` الدفعة على مستندَين فعليَّين (تقريرُ المراجعة الخارجيّة) |
| **إرجاعٌ إلى مورّد** | ✅ **تعمل ومتحقَّقٌ منها بمعاملتين حقيقيّتين** — قُورنت القاعدةُ بالشاشة: `unit_cost` محميّةٌ حتى مع سعرٍ عُدِّل يدويًّا، و`entered_unit_price` مسجَّلٌ منفصلًا، والمبلغُ المستلَم وطريقةُ الدفع كما أُدخلا · **وسلوكُ حارسَي ١٠١ مقيسٌ بـ١٠١ج** · ⚠️ **ويبقى غيرَ مقيس: أربعُ رسائلَ من خمسٍ في «إدخال من فاتورة»** (`allEmpty` وحدَها مقيسة) |
| **نقلٌ بين مستودعات** | ⏸ **تعمل على الشاشة المشتركة · ولا مواصفةَ إعادةِ تصميمٍ بعد** |

**✅ والشغلُ كلُّه مدموجٌ في `main`** — [PR #5](https://github.com/befoundonlineps-code/BeaityPro/pull/5)
(المحرّك والشاشتان والأداة) و[PR #6](https://github.com/befoundonlineps-code/BeaityPro/pull/6)
(حزمُ ملفّات الترجمة). **ومنشورٌ ومتحقَّقٌ منه على الإنتاج.**

**✅ ومقيسٌ لا مقروءٌ من تعليق: `stock_doc_type` تسعُ قيم** (٠٩٦ب) —
`opening · supply · write_off · return_to_supplier · stocktake · transfer ·
service_consumption · sale · reversal`.

**🔴 والسطرُ أعلاه («نقلٌ بين مستودعات ⏸») كان يقول «ولا مواصفاتٍ بعد» فقُرئ
نفيًا لوجود العمليّة — وهي عاملةٌ اليوم.** `transfer` في الـenum، و
`transfer_stock` منشورةٌ بخمسة حرّاس، **ولها زرٌّ في شريط العمليّات**
(`ProductsSecondaryBar`) ونداءٌ في `stockIO.js` ومدخلٌ في `DOCUMENT_VIEWS`.
**والناقصُ أنها ليست في `REFERENCE_FORM_VIEWS`** فتُرسم على الشاشة المشتركة.

⚠️ **والفرقُ بين «العمليّة» و«الشاشة» هو ما ضاع** — وبُنيت على النفي المفترَض
فرضيّةٌ كاملة: «القاعدةُ تجهيزٌ للمستقبل». **عينُ الصنف المحذَّر منه في
`سير العمل`: ترويسةٌ صحّت يومَ كُتبت وصارت تُقرأ ادّعاءً أوسعَ منها.**

**والمُخرِجاتُ عبر `post_stock_document` أربع:** `write_off` **صريحةٌ مُلزَمة**
بعد ٠٩٦، و`return_to_supplier` و`service_consumption` و`sale` **تبقى FIFO
ضمنيًّا.** ⚠️ **فالمسارُ الضمنيُّ ليس إرثًا ميّتًا — له مُنادون دائمون** (البيعُ
والاستهلاك)، وأيُّ تشديدٍ عليه يمسّ كلَّ صرفةِ خدمةٍ وكلَّ بيعة.

⚠️ **و٠٤٤ كان يسمّيها بتعليقٍ عربيّ («البيع والصرف»)** — وتسميةُ قيمةِ enum من
نثرٍ هي الصنفُ الذي كلّف المشروعَ أربعَ مرّات. **فالقائمةُ أعلاه من `pg_enum`.**

**🔴 والسؤالُ المؤجَّلُ عمدًا — أُجيب: نعم، قد تكون الدفعةُ التالفةُ أحدثَ من
الأقدم.** والنصُّ الأصليُّ للسؤال، محفوظًا لأن قرارَه بُني عليه:
هل يُحتمل عمليًّا أن تكون الدفعةُ التالفةُ **ليست الأقدم**؟ (توريدٌ أحدثُ تلف
وأقدمُ منه سليم.) **لو نعم، فالشطبُ اليدويُّ يحتاج اختيارَ دفعةٍ صريحًا لا FIFO
مطلقًا** — و`draw_stock_from_lots` تفرض الأقدمَ أوّلًا بلا مَخرج. **يُطرح على
المالك قبل بناء الشاشة لا بعده.**

**🔴 وهذا السطرُ كان يقول «المواصفاتُ جاهزةٌ كاملة» — وكان صحيحًا عن المحادثة
وخاطئًا عن المستودع، وهو ما يجعله أسوأ من الصمت.**

**✅ ومُستدرَك الآن: `design/write-off-products-spec.md` مودَعٌ (١٩٢ سطرًا،
١٦ آب ٢٠٢٦)** — بالأقسام الثلاثة (نصُّ المراجع · قراءةُ اللقطات موسومةً · ما
لا يُعرف موسومًا سؤالًا)، وترويستُه تُقرّ بنفسها أن الصورَ الخمسَ (لا الستَّ)
غيرُ موجودةٍ في المستودع وأن الوثيقةَ هي ما بقي منها.

🔴 **وهذا السطرُ نفسُه كان لا يزال يقول «لا صورةَ ولا وثيقة» بعد إيداع الملفّ
بأيّامٍ** — ترويسةٌ صحّت يومَ كُتبت وصارت كذبًا بصمت، **عينُ الصنف المحذَّر
منه في `سير العمل`.** فُحص الملفُّ فعليًّا (`wc -l` + تاريخُ التعديل) لا
افتُرض وجودُه من هذا السطر.

> **والقاعدةُ نفسُها باقية: مواصفةُ شاشةٍ تُودَع ملفًّا في `design/` قبل أوّل
> سطرِ كود**، بنصّها وبما استُنتج من لقطاتها موسومًا استنتاجًا. **ولا يُكتب في
> هذا الملفّ «جاهزة» عن شيءٍ لا يشير إلى مسارٍ في المستودع — ولا «مفقودة» عن
> شيءٍ صار له مسار.**

## 🔴 بنودٌ مؤجَّلة بقرار المالك — **لكلٍّ شرطُ تنفيذه، لا شرطٌ واحدٌ للجميع**

⚠️ **وكان العنوانُ «بندان… يُنفَّذان بعد إغلاق شاشة الشطب» فصارت ثمانيةً، ولكلِّ
واحدٍ شرطُه** — **و«أ» و«ب» تبقيان على شرطهما الأصليّ: بعد إغلاق شاشة الشطب لا
قبله.** و«ج» و«د» و«هـ» و«و» و«ز» و«ح» شروطُها مكتوبةٌ معها.

> **وعنوانٌ يعِد بشرطٍ واحدٍ لبنودٍ شروطُها شتّى هو نفسُ صنف «ترويسةٍ صحّت يومَ
> كُتبت»** — ولهذا يُعدَّل العنوانُ كلَّما دخل بندٌ بشرطٍ جديد.

**وأ وب** كُشفا بمسحِ `Number(` الذي طلبه المراجع بعد وقوع الصنف أربعَ مرّات.
**ومسجَّلان هنا بمواقعهما المقيسة لا بذكرٍ عابر، لأن ذكرًا عابرًا لا ينجو من
ضغط السياق** — وهي العلّةُ التي محت مواصفةَ الشطب ولقطاتِها الستّ.

**البند أ (الأولويّة) — `Number(null).toLocaleString('ar')` تُرجع `"0"`.**

فتكلفةٌ **غيرُ معروفةٍ** تُرسم `٠٫٠٠ ₪` على شاشةٍ حيّة. ⚠️ **وقاعدتُنا صريحةٌ
ضدّه:** «الشاشة تقول *١٢ قطعة · التكلفة ٠ ₪ (غير معروفة)* بدل `0 ₪` صامتة» —
وهو **وجهُ العرض لعلّة التسميم نفسِها**، لا مسألةَ تنسيق.

```
components/StockDocumentScreen.js:156   const cash = (value) => Number(value).toLocaleString(…)
components/StocktakeScreen.js:141-142   quantity / money
components/StorageBalances.js:45-46     money / quantity
components/ProductsBrowser.js:480, 789  priceShort · balances
```

**البند ب — قاعدةُ «الفراغُ عدمٌ لا صفر» مكتوبةٌ **أربعَ** نسخٍ حرّةٍ في أن تتباعد:**

```
lib/productForm.js:27        ┐
lib/serviceForm.js:23        ├─ text === '' ? null : Number(text)
lib/storageForm.js:15        ┘
lib/stockDocumentForm.js:334  ← الرابعة، انكشفت بقياس سُلَّم رصيد المورّد
```

⚠️ **والرابعةُ ليست نسخةً حرفيّة، وهذا أسوأ:** `numberOrNull` محلّيّةٌ تمرّ
بـ`String(value ?? '')` أوّلًا، **فتفترق عن المشتركة على غير النصوص** —
`false` تُقرأ `null` عندها و`0` عند المشتركة، وكذلك `[]`. **ولا أثرَ حيًّا
اليوم** (قيمُ النماذج نصوصٌ دائمًا)، **لكنّ نسختين تحملان الاسمَ نفسَه وتختلفان
في السلوك هي بالضبط ما يجعل النقلَ من ملفٍّ إلى ملفٍّ يغيّر النتيجةَ بصمت.**

✅ **والموضعُ الواحدُ موجودٌ الآن:** `numberOrNull` في
[lib/decimalPlaces.js](lib/decimalPlaces.js)، **وحارسٌ يفشل مغلقًا** على أيّ
`Number(` عاريةٍ في مكتبتَي الشطب (`lib/writeOffGrid.test.js`). **فالبندان
توسيعُ نطاقِ حارسٍ قائمٍ لا بناءٌ جديد.**

⚠️ **و«أ» عطلٌ ماليٌّ يراه إنسانٌ اليوم، و«ب» نظافةٌ تتبعه** — والترتيبُ قرارُ
المالك لا ذوق.

**البند ج — عميلُ Supabase يُبنى وقتَ الاستيراد، فاختباراتُ وحدةٍ لا علاقةَ لها
بالقاعدة تموت بتغيّرِ مكتبةٍ لا تستعملها.**
⏸ **شرطُه: بعد دمج `feat/document-view-screens` — لا قبله، بقرار المراجع.**

```
lib/supabaseClient.js:6   export const supabase = createClient(url, key)
node_modules/@supabase/realtime-js/dist/main/RealtimeClient.js:630
                          result.transport = … WebSocketFactory.getWebSocketConstructor()
```

**والنداءُ في المُنشئ لا عند `.channel()`** — مقيسًا بحذف `globalThis.WebSocket`
على v24 فارتمى نصُّ الخطأ حرفيًّا. ⇒ **فالملفُّ يرمي وقتَ التحميل، و٦٣ ملفًّا
يستوردونه سقطوا قبل أن يُنفَّذ فيهم فحصٌ واحد** تحت Node 20.

⚠️ **والعلاجُ الذي نُفِّذ (رفعُ Node إلى ٢٢) أغلق الحادثةَ لا الصنف:** أيُّ
تغيُّرٍ قادمٍ في متطلّبات تلك المكتبة — WebSocket اليوم، غيرُه غدًا — **يُسقط
اختباراتٍ لا تمسّ القاعدة إطلاقًا** (تحليلُ صفحات · رسمُ شاشات).

**والعلاجُ الصنفيّ: العميلُ كسولًا (دالّةُ إنشاءٍ تُنادى عند الاستعمال) أو
مُستبدَلًا بنسخةٍ وهميّةٍ في كلّ اختبارٍ ليس اختبارَ تكامل.**

⚠️ **ولا يُبدأ فيه قبل شرطه** — سُجِّل هنا لأن **ذكرًا عابرًا لا ينجو من ضغط
السياق**، وهي نفسُ علّة «أ» و«ب» أعلاه.

**البند د — `counter-evidence.mjs` يرجع `✗` ويخرج بنجاح، فيُبنى حقنٌ فوق
معلَّق.**
⏸ **شرطُه: لا شرطَ زمنيّ — أوّلُ جولةٍ تلمس `scripts/`.**

**وقع فعلًا:** استُرجع مرّتين على نسخةٍ واحدة، **فالثانيةُ طبعت
`✗ ما في نسخةٌ معلَّقة — ما في شي يُرجَّع` وخرجت بنجاح**، وحُقن الثالثُ فوق
حقنٍ لم يُرجَع ⇒ **فحصٌ سقط بلا سببٍ في الحقن الجاري.**

⚠️ **والأداةُ لم تكذب — هي صرخت، والقارئُ مرّ فوقها.** ولهذا العلاجُ إعدادٌ لا
انتباهة، وهي قاعدةُ هذا الملفّ نفسِها: «أفضلُ مكانٍ للقاعدة ليس نصًّا يُتذكَّر».

```
snapshot  ⟵ يرفض إن كانت هناك نسخةٌ معلَّقة، ويسمّي ملفّاتِها
          ⟵ والتجاوزُ صريحٌ: `discard` أوّلًا
```

**البند هـ — `engines.node: "22.x"` تسمح بـ22.0، والأرضيّةُ المقيسة `22.13.0`.**
⏸ **شرطُه: قياسُ قبولِ Vercel لصيغة المدى — قبل أيّ تعديل، لا بعده.**

```
eslint-visitor-keys@5.0.1     ^20.19.0 || ^22.13.0 || >=24
validate-npm-package-name@7   ^20.17.0 || >=22.9.0
```

⚠️ **والحارسُ الديناميكيُّ لا يسدُّها:** `lib/nodeVersionPin.test.js` يقيس
**النسخةَ التي تشغّل الفحوص**، **وVercel لا يشغّل الفحوصَ إطلاقًا** — يقرأ
`engines.node` ويبني. ⇒ **بقعةٌ عمياءُ هناك وحدَها.**

✅ **ولا خطرَ اليوم، مقيسًا بالواقع:** Preview يعمل صحيحًا الآن، **فنسخةُ Vercel
الفعليّةُ كافية.**

🔴 **ولا يُضيَّق المدى بالتخمين:** لم يُقَس هل يقبل Vercel صيغةَ مدًى
(`">=22.13.0 <23"`) أم الصيغةَ الرئيسة (`22.x`) وحدَها — **وصيغةٌ يرفضها تُسقط
بناءَ الإنتاج.** فالقياسُ أوّلًا على Preview، ثمّ التعديل.

**البند و — سقفُ المشية في `lib/nodeVersionPin.test.js` يبتلع ولا يشتكي.**
⏸ **شرطُه: متابعةٌ صغيرةٌ بلا استعجال، بعد دمج الحارس — وقد دُمج.**

```js
const walk = (dir, depth) => { if (depth > 4) return ; … }
```

**والرقمُ ٤ كُتب تخمينًا لا قياسًا، وقاله المراجعُ سؤالًا.** ⚠️ **والقياسُ
بعده — بمشيةٍ بلا سقفٍ إطلاقًا:**

```
أقصى عمقٍ فعليّ    ١      (node_modules/@dotenvx/dotenvx/node_modules/*)
حزمٌ بلا سقف      ٨١٠     ⟵ ونفسُها بالضبط مع السقف ٤
```

⇒ **فلا أثرَ له اليوم.** **لكنّه سقفٌ صامت:** لو عشّشت npm أعمقَ منه يومًا
(نزاعُ نسخٍ متعدٍّ) **تخطّى الحزمَ بلا كلمة** — **وهو يفشل مفتوحًا، عينُ ما
بُني الحارسُ ليمنعه.**

✅ **والعلاجُ لا يرفع السقفَ بل يجعله يشتكي:** يسقط الفحصُ **إن بُلغ السقفُ
أصلًا**، فيصير حدًّا يُعلن عن بلوغه بدل حدٍّ يبتلع.

**البند ز — `base_unit` و`entry_uom` مجالان مختلفان، و`'pcs'` صالحةٌ في الأوّل
وغلطٌ في الثاني — ولا شيءَ يقول ذلك.**
⏸ **شرطُه: أن يُبنى مع أوّل شاشةٍ تلمس الإطارَ من جديد، لا كتنظيفٍ منفصل.**

```
base_unit   ⟵ pcs · ml · g …        ⟵ ما يُقاس به المنتج
entry_uom   ⟵ unit · package        ⟵ الإطارُ الذي كُتب فيه الرقم
```

🔴 **ووقع فعلًا:** تجهيزةُ اختبارٍ كتبت `uoms: { p1: 'pcs' }`، فأرجعت
`baseUnitsFor` عدمًا، **فصارت `factBase` عدمًا، فصمت حارسان معًا** — ولم يُمسَك
إلّا لأن صفًّا مقابلًا **يتوقّع رسمًا**؛ **والصفُّ المتوقِّعُ صمتًا كان سيمرّ
خضراءَ بالخطأ.**

⚠️ **وهذه خامسُ مرّةٍ لصنف «اسمٍ يبدو مرادفًا»، والعلاجُ المعتادُ لا يمسكها:**
**قائمةٌ واحدةٌ من القيم الصالحة تقبل `'pcs'`** — لأنّها صالحةٌ فعلًا، في الحقل
الآخر. ⇒ **فالمطلوبُ نوعان مسمَّيان للحقلين، لا قائمةٌ مشتركةٌ بينهما**
(و`public.entry_uom` نوعٌ في القاعدة أصلًا — **والجانبُ الذي بلا نوعٍ هو
JavaScript**).

⚠️ **والتجهيزاتُ تستوردُه كما يستوردُه الكود** — فبند (ب) أعلاه يقول إن النسخةَ
الحرّةَ تتباعد، **وهذا يقول إن التجهيزةَ الحرّةَ تكذب بصمت**، وهو الأخطر: **لا
يتباعد شيءٌ، بل يصمت الحارسُ ويُقرأ صمتُه نجاحًا.**

**البند ح — أداةُ القياس تدخل السطحَ الذي يُقاس، فتُحرّف العدَّ المقتبَس.**
⏸ **شرطُه: أن يُتّفق أوّلًا على علامةٍ للمؤقّت** (بادئةُ اسمٍ أو مجلّد)، **ثمّ
يُبنى حارسٌ يسقط عليها كـ`counterEvidencePending`** — ولا يُبنى حارسٌ بلا علامة.

**والصنفُ عامٌّ: كلُّ رقمٍ مصدرُه اكتشافٌ تلقائيٌّ فوق سطح** — مشيةُ نظام ملفّات،
أو نصُّ صفحةٍ مخدومة — **يتحرّك حين تحطّ أداةُ القياس نفسُها على ذلك السطح.**

```
مسبارُ اليوم        صفحةٌ مؤقّتةٌ في pages/ ⟵ pagesParse يولّد اختبارَين لكلّ صفحة
                    ⟶ ٢٥٧٥ بدل ٢٥٧٣، والرقمُ مُقتبَسٌ في رسالة الإيداع
                    ⟶ مقيسٌ بالتكرار: أُنشئت صفحةٌ فارغةٌ ثمّ حُذفت، فتحرّك ٢
أخوه (الحمولة)      البحثُ عن نصٍّ مترجَمٍ في HTML الخام ⟵ `serverSideTranslations`
                    تُسلسل **كلَّ** الـnamespace داخل `__NEXT_DATA__`
                    ⟶ `includes()` يجد القاموسَ لا الرسم، **فقال ✓ أربعَ مرّاتٍ
                      كذبًا** عن نصٍّ لم يُرسم قطّ
```

⚠️ **والاثنان ليسا «فحصًا أعمى» بل فحصًا صحيحًا على سطحٍ تلوّث:** `pagesParse`
يعمل كما صُمّم، و`includes` تجد ما بحثت عنه فعلًا. **والمتحرّكُ هو ما يقف عليه
السؤال، لا منطقُ السؤال.** ⇒ **ولهذا لا يمسكه تدقيقُ الفحص نفسِه.**

**وقاعدةُ الاحتراز:** أيُّ عددٍ إجماليٍّ مصدرُه اكتشافٌ تلقائيٌّ من نظام الملفّات
**يُتحقَّق من خلوّ الشجرة من أثرٍ مؤقّتٍ قبل الوثوق به** — و`git status` قبل
الاقتباس أرخصُ من رقمٍ خاطئٍ في رسالة إيداع.

✅ **وقُبض عليه قبل الدفع، وصُحّح بتعديل رسالة الإيداع لا بحذف الأثر** —
والسببُ باقٍ مكتوبًا فيها، **فالرقمُ الخاطئُ لم يخرج إلى المستودع.**

---

⚠️ **و`docs/db-functions.sql` صار أقدمَ من الواقع بعد ٠٩٥** (أربعٌ أُعيد بناؤها
واثنتان أُضيفتا). **يُعاد توليدُه بتشغيل ٠٩٥أ٢ من جديد ولصقِ مخرَجه** — والملفُّ
قائمٌ وقابلٌ للتكرار.

⚠️ **والأربعُ دوالٍّ تتحوّل وتُطلق معًا بدفعةٍ واحدةٍ منسّقة، لا تدريجيًّا** —
لأن `ADR-051` يقول إن الرقمَ المختوم لا يُصحَّح إلّا بعكس المستند، **فإطلاقٌ
نصفيٌّ يختم أرقامًا غلطًا بلا استرداد.**

⚠️ **وصارت ستّةَ كائناتٍ لا أربعة عند الكتابة** (٠٩٥): `open_estimated_lot` و
`draw_stock_from_lots` مساعدتان، **لأن سلّمَ التقدير كان مكرَّرًا شبهَ حرفيٍّ في
ثلاث دوالّ** — ونسخُه مرّةً رابعةً بجوار منطق FIFO يضاعف الازدواج بدل أن ينهيه.
**والستّةُ تُطلق معًا، والقاعدةُ أعلاه لا تتغيّر بتغيّر العدد.**

🔴 **وقفلُ الدفعة الذي قُرِّر غيرُ ممكن، والقائمُ يكفي:** `SELECT … FOR UPDATE`
**يطبّق `USING` الخاصّةَ بـ`UPDATE`** تحت RLS (`PROJECT_HANDOFF:563`)، و٠٩٤ج سحب
منحةَ `UPDATE` عن `stock_lots` ولا سياسةَ لها. **و`perform 1 from products …
for update` القائمُ في الدوالّ الثلاث يسبق كلَّ قراءةٍ للمتبقّي**، فعمليّتان
على نفس المنتج تتسلسلان — وهو عينُ المطلوب. **يُقرأ هذا قبل أيّ محاولةٍ لإضافة
قفلٍ «ناقص».**

✅ **ولا ترحيلَ بيانات:** ٠٩٣ قِيس وأكّد أن `stock_movements` و`stock_documents`
**فارغتان تمامًا بكلّ النظام** — فكلُّ حركةٍ من اليوم تصير أوّلَ دفعةٍ لها مباشرةً.

**⚠️ وسؤالٌ مؤجَّلٌ عمدًا، يُطرح على المالك عند بناء التوزيع التلقائيّ ولا يُنسى:**
هل يُحتمل عمليًّا أن تكون الدفعةُ التالفةُ **ليست الأقدم**؟ (توريدٌ أحدثُ تلف
وأقدمُ منه سليم.) لو نعم، **الإدخالُ اليدويُّ يحتاج اختيارَ دفعةٍ صريحًا لا FIFO
مطلقًا.**

## الوثائق — وين تدوّر

- شكل جدول أو عمود؟ [docs/DATABASE_DIAGRAM.md](docs/DATABASE_DIAGRAM.md)
- توقيع دالة، رمز رفض، `SECURITY DEFINER`، `search_path`؟
  [docs/db-functions.sql](docs/db-functions.sql)
- "ليش انعملت هيك"؟ [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md)
- سياق المشروع وحالته؟ [docs/PROJECT_HANDOFF.md](docs/PROJECT_HANDOFF.md)
- خلفية عن المجال؟ [docs/BEAUTY_PRO_REFERENCE.md](docs/BEAUTY_PRO_REFERENCE.md)

**الوثائق بتوجّهك، والقاعدة بتقرّر.** هاي الملفات صورة عن حالة، مش الحالة
نفسها. شغّل الاستعلام الرخيص وتأكّد قبل أي SQL جديد حتى لو الوثيقة واضحة —
وثيقة واثقة وغلط أسوأ من لا وثيقة، لأنها بتخليك تتخطّى التحقّق بثقة. هذا
صار فعلًا: نسخة قديمة من المخطط قادت لاستنتاج غلط عن حالة القاعدة، وللقول
إن التوثيق متأخر وهو مش متأخر.

أول أربعة بتتحدّث **بنفس الـcommit** تبع التغيير اللي غيّرهن. الـSQL مش
مُدار بالإصدارات هون، فالـcommit هو السجل الوحيد ولازم يحمل الاثنين مع بعض.
`BEAUTY_PRO_REFERENCE.md` مستثنى: بيوصف المجال لا النظام، فما بيتقادم لما
يتغيّر الكود.

`db-functions.sql` ما بينكتب بالإيد — بينولّد، والاستعلام برأس الملف.

⚠️ **وعرضُ السطر عرفٌ لكلّ ملفٍّ لا للمستودع، ومقيسًا بالحروف لا بالبايتات**
(٢٤ آب ٢٠٢٦): `CLAUDE.md` **١٨ سطرًا فوق المئة من ٢٣٣٣** · `SAFETY-GATE`
**٢ من ٦٩٤** ⇒ **الاثنان يلفّان عند ~٨٠**؛ و`PROJECT_HANDOFF` **٦٧٠ من ١٧٩٥
وأطولُها ٣٣٢١** ⇒ **لا يلفّ إطلاقًا.** 🔴 **ولا يُوحَّد:** «تنظيفُه» يُنتج فرقًا
بمئات الأسطر بلا معنًى **يدفن أيَّ تغييرٍ حقيقيٍّ داخله.**
⚠️ **و`awk 'length'` يعدّ بايتات والحرفُ العربيُّ بايتان** — فأعطى «١١٠٩ فوق
المئة» في `CLAUDE.md` والحقيقةُ ١٨. **يُعدُّ بالحروف.**

🔴 **ورقمان لغرضَين ولا يُخلطان: العرفُ ~٨٠، والمئةُ عتبةُ صيدِ الشاذّ لا
عرف.** ⚠️ **وقد خُلطا في السطر أعلاه نفسِه: قِيس «فوق المئة» فقُرئ عرفًا،
والمقيسُ كان الشاذَّ لا الحدَّ.**

**ومقيسًا بالمئين ٩٠ لعرض السطر (٢٥ آب ٢٠٢٦) — والشاهدان المستقلّان ملفّان
سابقان:**

```
CLAUDE.md 79   ·   SAFETY-GATE 79        ⟵ **سابقان: شاهدان مستقلّان**
balances-and-coverage 79 · stock-transfer 79
      ⟵ كُتبتا اليومَ على هذا العرف، **فهما أثرُ الالتزام لا شاهدٌ عليه**
```

⚠️ **والتمييزُ يُكتب لأنّ أربعةَ أرقامٍ متطابقةٍ تبدو حجّةً أقوى مرّتين ممّا
هي.** ✅ **والسابقان يكفيان.** ⚠️ **وما فوق المئة جداولُ واقتباساتٌ وكتلُ كودٍ
لا نثر** ⇒ **فالنثرُ يُلفّ عند ٨٠، والجدولُ لا يُلفّ.** **وتحريرٌ جديدٌ يُقاس
بالثمانين، ومراجعةُ ملفٍّ قديمٍ تُصطاد بالمئة.**

## سكربتات قاعدة البيانات — قوانين ثابتة

- **⚠️ حالة تشغيل السكربتات تُكتب بـ[docs/sql/README.md](docs/sql/README.md)
  لحظة تأكيد المالك — ولا تُستنتج من المحادثة.**

  انزلق التتبّع فعلًا: قلت للمالك إن سكربتين معلّقين وكان قد شغّلهما، **وسؤاله
  هو اللي كشف الفارق لا فحص عندنا**. والسبب بنيوي: **حالة التشغيل عايشة عند
  المالك وحده والمستودع ما بيشوفها**، فأي ذاكرة عنها بتتقادم بصمت.

  **🔴 وصارت مرّة تانية، والمراجع هو اللي عدّها:** انقال «٠٨٩ ما انشغّل»
  و«٠٨٨ بعد ٠٨٩» — **والسلسلة كلها (٠٨٨ ⟵ ٠٨٨ب ⟵ ٠٨٧_٢ ⟵ ٠٨٩) كانت خلصت
  ونجحت.** والسجلّ كان ⏸ لأن المالك شغّلها وأكّدها لغير هالمحادثة.

  ⚠️ **والعطل مش تقادُم السجلّ — هاد بنيويّ ومعروف. العطل إن السجلّ المتقادم
  انقال جملةً عن الواقع.** «٠٨٩ ما انشغّل» ادّعاء عن القاعدة، و«سجلّي بيقول
  معلّق» ادّعاء عن الملفّ — **والتاني وحده صادق دايمًا.**

- **🔴 محرّرُ Supabase بيعرض مجموعةَ النتائج الأخيرة وحدَها — فملفُّ التحقّق
  جملةُ `select` واحدة.**

  وقعت مرّتين: ٠٩٣ بأربع جملٍ وصل منه واحد، و٠٩٤ب بخمسة أقسامٍ **وصل القسمُ
  الأخير وحدَه**. ⚠️ **والابتلاعُ صامت** — ما في سطرٌ بيقول «في أربعُ نتائجَ ما
  انعرضت»، فبيبان الملفُّ كأنه ما فيه إلا قسمٌ واحد.

  ⚠️ **والثانيةُ ما كانت لتُكتشف لولا أن المالك عدّ الأقسامَ الغائبة** — أي
  بالصدفة لا بفحصٍ عندنا.

  > **فالتحقّقُ يُقسَّم ملفًّا لكلّ سؤال** (٠٩٤ب/أ … ٠٩٤ب/هـ)، لا ملفًّا بأقسام.
  > **والكتلةُ `do` بتنتهي بـ`select current_setting(...)`** — وهي الجملةُ الأخيرة
  > فبتُعرض.

  ⚠️ **وهو أخو «`raise notice` ما بتنعرض»:** الاثنان **حارسٌ اشتغل وما حدا شافه**،
  والفرقُ إنك بتظنّ إنك قِسْت.

  > **فحالة التشغيل ما بتنقال خبرًا أبدًا، بتنقال سؤالًا:** «سجلّي بيقول ٠٨٩
  > معلّق — أكّدلي؟» بدل «٠٨٩ ما انشغّل». الكلفة كلمتان، **والبديل إن حدا يعيد
  > تشغيل محوٍ لا يُستدرَك لأنه صدّق سجلًّا داخليًّا.**

  **🔴 وثالثةٌ، وصنفُها جديدٌ ولهيك مكتوبة: ما انقال ادّعاءٌ عن التشغيل — انكتب
  عنوانٌ بينقرأ ادّعاءً.**

  انبعتت السكربتاتُ التلاتة للمراجعة، وانفتح التقريرُ بعدها بـ**«✅ الثلاثةُ
  نُفِّذت»** — والمقصودُ **القراراتُ** التلاتة اللي حسمها المالك. **بس الفقرةَ
  اللي قبلها كانت عن السكربتات التلاتة**، فرجع السؤال: «أكّدولي، هل ٠٩٠/٠٩٠ب/٠٩١
  فعلًا اتنفّذوا؟ وابعتولي مخرَج `raise notice` الخام بالأرقام».

  ⚠️ **ولا سطرَ غلط بالتقرير.** «نُفِّذت» صحيحةٌ عن القرارات، **والمرجعُ وحدَه
  غامض** — والقارئُ بيحلّه بأقرب شي ذكرتَه، لا بأقرب شي قصدتَه.

  **⚠️ وخطرُ هالصنف أعلى من الصنفين قبله**، لأن السابقين ادّعاءان قابلان
  للتكذيب، **وهاد بيدفع لطلب مخرَجٍ خامٍّ غيرِ موجود** — والاستجابةُ الغلط لها
  اسمٌ واحد: اختلاقُ أرقام.

  > **فكلمةُ التنفيذ («نُفِّذ · انشغّل · تمّ») ما بتنكتب إلا وفاعلُها ملاصقٌ
  > إلها:** «نُفِّذت **القراراتُ** التلاتة» أو «**السكربتاتُ** لسا معلّقة». والأصلبُ
  > منها: **ما تُستعمل الكلمةُ إطلاقًا عن شي بيشغّله المالكُ بإيده** — «القراراتُ
  > التلاتة انبنَت» ما فيها لبس.

  ✅ **والجوابُ الصحيح لمّا ينطلب مخرَجٌ ما بيملكه أحد: «ما بقدر — وما رح
  أخترع».** لا وصولَ عندي إلا `SUPABASE_KEY` العامّ، **فالمخرَجُ الخامُّ يملكه
  المالكُ وحدَه** بعد ما يلصق السكربت.

- **⚠️ النصّ العربي اللي بينشحن للقاعدة بينقرأ راجعًا بنفس السكربت.**

  مش كل العربي بالسكربت شرحًا: `comment on column` و`using hint = '…'`
  **بينزلوا القاعدة وبيوصلوا شاشة المستخدم** — والـ`hint` هو الدرجة التانية
  بسلّم رسائل الخطأ عندنا.

  والمالك شغّل سكربت ٤٧ **بتعليقات إنجليزية** تفاديًا لمشكلة تشفير صارت مع
  ٤٣ — **فالسؤال «هل نجا العربي جوّا `$function$`؟» صار مفتوحًا ومقيسًا
  بسكربت ٤٨**. ⚠️ **وما في اختبار عندنا بيمسك هاد الصنف**، لأن اختباراتنا
  بتقرأ المستودع لا القاعدة.

  **فالقاعدة: أي سكربت بيودع نصًّا عربيًّا بيحمل `select` بيقرأه راجعًا.**

  ✅ **وانقاس (سكربت ٤٨): مش تلفًا — ترجمة.** القاعدة `UTF8`، **ودالّتان
  تسبقان المشروع كله** (`freeze_consignment_after_use` ·
  `refuse_archiving_stocked_storage`) **عربيّهما سليم جوّا جسميهما**. فالعربي
  بينجو جوّا `$function$` بلا مشكلة، والتمانتعشر رسالة **انترجموا** لمّا
  انعالجت مشكلة تشفير مسّت التعليقات — **فامتدّ العلاج لما مش تعليقًا.**

  **⚠️ والتمييز اللي بينحفظ:**

  | | بيخرج من القاعدة؟ | بينترجم بلا ثمن؟ |
  |---|---|---|
  | `--` جوّا الدالّة | لأ، شرح للي بيقرأ `pg_get_functiondef` | ✅ |
  | `using hint = '…'` | **آه — بيوصل شاشة المستخدم حرفيًّا** | ❌ |
  | `comment on column` | **آه — بينقرأ بأدوات القاعدة** | ❌ |

  **فجملة إنجليزية بمنتج عربيّ هي جملة غلط**، ولا اختبار عندنا بيمسكها.

المالك **ما عنده صلاحيات إدارية** على قاعدة البيانات (publishable key فقط).
كل تعديل على القاعدة بينفّذه هو يدويًا بمحرّر SQL. يعني: جهّز السكربت
واعرضه، ولا تفترض إنك تقدر تشغّله بنفسك.

### 1. لا تجمع DDL دائم مع **أي جملة تقدر تفشل** بنفس السكربت

⚠️ **العنوان كان «مع `RAISE EXCEPTION`» وهو تضييق للصنف، كلّف جولة.**

الشرط مش «ما في `RAISE`» — الشرط **«ما في جملة بتفشل»**، و`RAISE` مجرد أشهر
أسبابها. وجملة **التحقّق** جملة.

صار بسكربت ٠٥١ج: جملة الفحص بآخر الملف جمعت `count(*)` مع أعمدة مبنية على
`p.prosrc` بلا `group by`، فرفضها بوستجرس — **والرفض رجّع `CREATE OR REPLACE`
اللي فوقه**، فما انحفظت الدالّة. وترويسة الملف كانت بتقول «لا `RAISE` هنا»
**كأن هاد هو شرط الأمان**، فسمّت الثابتة الغلط.

⚠️ **وما في شي بالسكربت المتغيّر بيقدر يمسكها:** جملة بتفشل بتبلّغ عن فشلها
هي، مش عن التراجع اللي سبّبته. **اللي مسكها ملف منفصل** (٠٥١د) قال
`rev_copies_bonus_expect_1 = 0`.

**فالقاعدة البنيوية: التحقّق اللي بيقدر يُسقط التغيير ما بيصلح يعيش بمعاملته.**
فحصٌ بملفّه ما بيقدر يتراجع عن شي. والاستثناء الوحيد هو القراءة الراجعة للنصّ
العربي — لازم تكون بنفس السكربت — **وهاي بالضبط اللي لازم تكون بسيطة لدرجة
ما تقدر تفشل.**

**وحارسان بـ[lib/sqlVerificationShape.test.js](lib/sqlVerificationShape.test.js)،
والتاني هو اللي بيسكّر الصنف:**

**١. الشكل** — تجميع بمستوى أعلى، بلا `group by`، مع عمود مؤهَّل برّا التجميع.
**ضيّق بإعلانه، مش مدقّق SQL**، وفيه الجملة المكسورة نفسها كبيّنة مضادّة.

**٢. البنية** — **ما في ملف بيجمع DDL مع استعلام تحقّق.** وهاد اللي بيقفل
الثغرة فعلًا، لأن «هل هالجملة رح تفشل؟» **مش قابل للحسم بقراءة النص** (اسم عمود
غلط · نوع ما بينحوّل · مرجع غامض · قسمة على صفر — كلهن بيسقّطوا الدالّة بنفس
الطريقة)، بينما **«هل فشلُها بيقدر يمحي تغييرًا؟» قابل للحسم.** وفحصٌ ما بيقدر
يمحي إشي آمِن مهما كان غلطًا.

⚠️ **ومش «جملة تنفيذية وحدة بالملف»** — هاد بيرفض ٠٥١أ، وستّ جمله (العمود،
القيدان، التعليق) **لازم تكون معاملة وحدة**: لو رفض القيدُ صفوفًا قائمة، لازم
العمود يروح معه. **الذرّية بالـDDL ميزة لا خطر؛ الخطر استعلامُ تقريرٍ بمعاملة
التغيير اللي بيقرّر عنه.**

**والقائمة المستثناة أسماءٌ صريحة (١٧ ملفًا شُغّلوا قبل القاعدة) — بتفشل مغلقة:**
ملف جديد بيجمع بيسقط، وحدا بيبصّ عليه مرّة. **وما بينُقسَموا بأثر رجعي**، لأن
تقسيمهم بيعيد كتابة سجلّ ما نُفِّذ فعلًا، وهو السجلّ الوحيد الموجود.

### 1ب. الصياغة الأصلية للبند ١ (`RAISE EXCEPTION`) — لسّه صحيحة، بس مش الحدّ

الـDDL ببوستجرس **معاملة زي أي شي تاني**، ومحرّر SQL بينفّذ النص المُرسَل
كله بمعاملة ضمنية واحدة. فأي `RAISE EXCEPTION` — حتى لو مقصود عشان يرجّع
بيانات اختبار — بيُسقط المعاملة كاملة، **وبضمنها `CREATE OR REPLACE`
اللي فوقه**.

هذا صار فعلاً: سكربت أصلح دالة ثم فحصها بنجاح ثم محا إصلاحه، فبقيت
النسخة المكسورة حيّة والفحص بيقول "نجح". كلّف دورة تشخيص كاملة، وبدا
شكله زي تأخر كاش أو مشكلة بيانات — وما كان لا هذا ولا هذاك.

**الصح:** رجّع بيانات الاختبار عبر كتلة `begin … exception when others
then … end` داخلية. هاي معاملة فرعية، فالاستثناء جوّاها بيرجّع شغلها هي
بس والمعاملة الكاملة بتكمل:

```sql
begin
  perform my_function(...);
  select status::text into v_status from ... ;
  raise exception 'ROLLBACK_MARKER';
exception when others then
  if sqlerrm <> 'ROLLBACK_MARKER' then v_code := sqlstate; v_msg := sqlerrm; end if;
end;
```

**والنقطة اللي بتنفلت:** «الإرجاع بكتلة داخلية» يعني الإرجاع **عبر ما ينجو
منها** — لا الكتابة جوّاها. الكتلة معاملة فرعية، فعند التقاط الاستثناء
**بتُلغى كل تغييرات القاعدة اللي صارت جوّاها**، وبضمنها الإدراج بجدول
مؤقّت. جدول مؤقّت لسّه «حالة قاعدة». فلو راكمت التقرير جوّا الكتلة، بيرجع
`select` الأخير **فاضيًا** وإنت مفكّر إن الفحص ما اشتغل.

**اللي بينجو هو متغيّرات PL/pgSQL وحدها.** راكم بمصفوفة نصّية، وأدرجها
**بعد** الكتلة:

```sql
declare v_log text[] := '{}';
begin
  begin
    …
    v_log := v_log || format('① … cost=%s', v_cost);
    raise exception 'ROLLBACK_MARKER';
  exception when others then
    if sqlerrm <> 'ROLLBACK_MARKER' then
      v_log := v_log || ('💥 ' || sqlstate || ' — ' || sqlerrm);
    end if;
  end;
  insert into probe select unnest(v_log);   -- خارج الملغاة
end $$;
```

الكتلة الخارجية بلا `EXCEPTION` فمش معاملة فرعية، فاللي بعدها بيبقى.
و`RAISE NOTICE` ما بينعرض بمحرّر Supabase، وهذا بالضبط اللي بيغري
باستخدام الاستثناء من الأساس.

**🔴 و`RAISE NOTICE` ما بينعرضلوش أصلًا — مقيسًا عند المالك، لا مستنتَجًا.**

٠٩٠ب (قسم C) و٠٩١ الاتنان بلّغا نتيجتَهما بـ`raise notice`، **وما وصلت
بشّار ولا وحدة** — «آخر نتيجة جدول ظهرتله» وبس. **يعني أرقامُ الحارس
راحت، والسكربتُ اشتغل.** وهاد أسوأ من مخرَجٍ غلط: **حارسٌ اشتغل وما حدا
شافه بيساوي حارسًا ما اشتغل**، والفرقُ إنك بتظنّ إنك قِسْت.

> **فالقاعدة: كلُّ رقمٍ لازم يوصل إنسانًا بينزل بـ`select`، لا بـ`raise
> notice`.** والـ`notice` بيبقى للتشخيص العابر وحدَه.

**والحالة الصعبة هي اللي بتلزمها كتلة** (تجربةُ قيدٍ بإدراجٍ لازمه يفشل).
والحلّ **بلا جدولٍ مؤقّت**، لأن `create temp table` بتُصنَّف `ddl` فبتقع
تحت البند ١، بينما `do $$…$$` بتُصنَّف `other` فبتمرق مع `select`:

```sql
do $$
declare v_log text := '';
begin
  begin
    insert into t(c) values ('');      -- لازم يفشل
    v_log := v_log || 'blank_refused=NO ';
  exception when check_violation then
    v_log := v_log || 'blank_refused=yes ';
  end;
  -- ⚠️ بعد الكتلة لا جوّاها: تراجعُ المعاملة الفرعية بيمحي
  -- `set_config` متل ما بيمحي الإدراج بجدولٍ مؤقّت.
  perform set_config('probe.result', v_log, false);
end $$;

select current_setting('probe.result') as result;   ← بينعرض تلقائيًا
```

**واللي بينجو من الكتلة هو متغيّرُ PL/pgSQL وحدَه** — والـ`set_config`
بينحطّ **بعده**، فبيوصل الـ`select` الأخير.

⚠️ **والسكربتُ اللي اشتغل ما بينتصلّح بمكانه.** ٠٩٠ب و٠٩١ اتنفّذوا،
**والملفُّ هو السجلُّ الوحيد لما انلصق فعلًا** — فالتصحيح بينزل بسكربتٍ
جديد يقرأ الحالة، لا بإعادة كتابة ملفٍّ منفَّذ. نفس قاعدة «ما بينُقسَموا
بأثر رجعي».

**وقانون مرافق: `auth.uid()` فاضية بمحرّر SQL.** المحرّر ما بيحمل JWT
وبيشتغل بدور المالك، فأي `where id = auth.uid()` بيرجع صفر صفوف وأول
`insert` بيسقط بـ`23502`. خُد الصالون بـ`select id from salons limit 1`.
**والأثر التاني أهم: RLS متجاوَزة بالكامل هناك** — فسكربت بينجح بالمحرّر
**ما بيثبت إشي عن العزل**، بيثبت الحساب فقط. صرّح فيها برأس السكربت،
لأن اللي بيقرأ نتيجته لاحقًا رح يظنّها أثبتت الاثنين.

### 1ج. استعلامُ الغياب بيحمل شاهدَ صدقٍ جوّاه — وإلا «ما في» و«ما سألت» بيتطابقوا

**لمّا يكون السؤال «هل في شي هون؟»، النتيجة الفارغة إلها معنيان ما بينفرقوا
بالمخرج:** لا يوجد شي فعلًا، **أو** الاستعلام ما وصل الموضع أصلًا.

**صار فعلًا بـ٠٧١:** انسأل عن مشغّلاتٍ تمسّ الغرامة بأربعة جداول، **و`stock_fine_lines`
مش منهن** — والمخرج ما فيه ولا صفّ عنه. **و«ما إله مشغّلات» و«ما انسأل عنه» بدوا
متطابقين تمامًا**، وما انميّزوا إلا لأن **المفتاح الأجنبيّ بيفرض** وجود مشغّلات
`c_` على الجدول الحامل له — أي **بالصدفة، من خارج تصميم السؤال.**

**فالقاعدة: يُدخَل بالنطاق شيءٌ نعرف أنه موجودٌ ولازم يظهر.** ٠٧٤_١ بيسأل عن
مشغّلات `storages` **وبيضمّ `storage_categories` معها** — وعليه مُشغّل ٠٦٨أ اللي
كتبناه بإيدنا. فلو ظهر `refuse_unlinking_stocked_folder` بـ`is_user_trigger = true`،
**بيكون الاستعلام أثبت إنه بيشوف المشغّلات المكتوبة بإيد** — **وعندها صفرُ مشغّلاتٍ
على `storages` خبرٌ لا صمت.**

> **والفرق إن التمييز مبنيٌّ بالسؤال بدل ما يُستنقَذ من الصدفة.**

⚠️ **وقائمةُ الجداول بحدّ ذاتها توقّعٌ مكتوبٌ بيد.** ٠٧١ سأل عن خمسة **وفاته
`stock_fine_lines`**، و٠٧٤_١ سأل عن تلاتة **فلقي حارسًا حيًّا غير موثَّق على
`storages` — لأن الشكّ وقع هناك تلك المرّة وحدها.** فاللي ما بنعرفه انلقى
**بالحظّ لا بالمنهج**، ولو وقع الشكّ بمكان تانٍ لضلّ مجهولًا.

> **فالسؤال اللي ما بيفوته شي بيشيل القائمة كلها:** `where not t.tgisinternal
> and n.nspname = 'public'` — بلا `relname` وبلا شكّ. **وشاهد الصدق بيصير
> «لازم يرجّع الاتنين اللي بنعرفهن»، وعندها الجواب السلبيّ بينقال لأول مرّة:
> «ما في غيرهن» بدل «ما لقينا بما سألنا عنه».**

⚠️ **وهو أخو «فحص ما بيقدر يفشل مش فحصًا»:** هناك الفحص بينجح دايمًا، وهون
**بيصمت بنفس الطريقة سواء وُجدت الحالة أو ما انسأل عنها** — والقارئ ما عنده ما
يفرّق.

### 2. افحص محتوى الدالة، لا وجودها

`select count(*) from pg_proc where proname = …` **عاجز بنيويًا** عن كشف
إصلاح انرجع — النسخة القديمة إلها نفس الاسم ونفس التوقيع بالضبط.

افحص `prosrc` نفسه:

```sql
select (length(prosrc) - length(replace(prosrc, ')::appointment_status', '')))
       / length(')::appointment_status')
from pg_proc where proname = '…';
```

وانتبه: أي عدّاد نصّي بيلقط كمان **التعليقات** جوّا الجسم. لو التعليق
بيذكر التحويل باسمه، العدّ بيزيد واحد. استخدم جزءًا ما بيظهر إلا
بالكود الفعلي (مثلاً القوس بـ`)::type`).

### 2ب. نثرُ الدالّة ما بيسمّي المعرِّف اللي بيمنعه فحصُها

`prosrc` بيحمل **الجسم بتعليقاته**، فأي عدّاد نصّي بيشوف الشرح والكود سوا.
**فلمّا يكون الفحص «هالجدول ما بينقرأ هون»، وجسمُ الدالّة يسمّيه بتعليقٍ
يشرح ليش ما بينقرأ — بيصير الفحص لازم يختار بين التقاط الشرح والتقاط
المخالفة، وما بيقدر الاتنين.**

**صار فعلًا، والإبرة تأرجحت مرّتين قبل ما ينشاف السبب:**

```
'stock_movements'              ⇒ التقطت التعليق، فأبلغت فشلًا عن دالّةٍ سليمة
'from public.stock_movements'  ⇒ فوّتت `from stock_movements` بلا مؤهِّل
                                 (وsearch_path مثبَّت، فالصيغة تشتغل تمامًا)
```

⚠️ **والتصليح اللي بيقفز للذهن بالحالة الأولى أخطر من العطل: حذفُ التعليق**
— أي إزالةُ الجملة اللي بتشرح القاعدة، **إرضاءً لعدّادٍ بيدوّر على كلمة لا
على استعمال.**

> **فالحلّ إن التعليق يتغيّر لا الإبرة:** «لا مجموعٌ ثانٍ على جدول الحركات
> الخام» بدل تسميته. **وعندها الإبرة بترجع للاسم المجرَّد وبتلتقط كل صور
> الاستعمال، مؤهَّلةً كانت أو لا.**

⚠️ **والتأرجح نفسه هو الدليل:** إبرةٌ بتضيق وبتتوسّع بجولاتٍ متتالية **معناها
إن المشكلة مش فيها.**

### 3. الفحص لازم ينفّذ الدالة فعلاً

PL/pgSQL بيخزّن الجسم **نصًا** وبيخطّط كل جملة أول مرة **تُنفَّذ**. فدالة
ممكن تُنشأ بنجاح وتفشل أول استدعاء. و"نجحت مرة" ما بتعني كل فروعها
سليمة — فرع `CASE` ما مرّ عليه التنفيذ لسّه ما انخطّط.

اختبر كل فرع بيكتب لعمود: الأساسي والمشروط وحلقات الإدراج.

### 4. الفحص الحاسم ما بيجوز يعتمد على مطابقة أسماء

`where name = 'قص شعر للأطفال'` بيفشل بصمت لأن الاسم الحقيقي
`'قص شعر للأطفال (حتى 6 سنوات)'`، فبينسكت الفحص كله. خُد صفًا حقيقيًا
وخُد خدمته وموظفه **من نفس الصف**، وخلّي البحث بالاسم للتشخيص فقط
(بـ`like`، وبيعرض الموجود فعلاً).

### 4ب. كل «عدّ» أو «هل اكتمل؟» يُشتقّ من النظام، لا من قائمةٍ كُتبت بيد

**البند ٤ يقول لا تطابق بالاسم. وهذا يقول لا تعدّ من الذاكرة** — وهما وجهان
لعطلٍ واحد وقع **أربع مرّات بجولةٍ واحدة**، والمراجع هو من عدّها:

| ما سُئل | بأيّ مرشَّحات | والحقيقة |
|---|---|---|
| اسم المورّد | `name` · `full_name` · `contact_name` | `first_name` / `last_name` |
| عمود العكس | `reversed_document_id` | `reverses_document_id` |
| حارس `entered_uom` | `pg_constraint` بـ`contype='c'` | **نوعٌ** `entry_uom` لا قيد |
| قيود جدولٍ جديد | القيود التي **سمّيتُها** بالسكربت | خمسة — المضمَّن `references` سمّاه بوستجرس |
| **وجهةُ `stocktake_sessions.started_by`** | **الاسمُ**، وسردُ الأعمدة في `stocktakeSessionIO.js` | **`profiles` لا `employees`**، و`nullable` |

**⚠️ والخامسةُ تضيف شيئًا جديدًا على الأربع: الخطرُ سُمّي بالاسم قبل وقوعه ووقع.**

كُتب بالرسالة المرافقة لـ٠٩٥ب/ج، قبل تشغيله بدقائق: «قائمةُ أعمدة الجدولين
مأخوذةٌ من `stocktakeSessionIO.js` لا من الكتالوج، **وهذا هو الصنفُ الذي كلّف
هذا المشروعَ أربعَ مرّات**» — ثمّ وقع، بـ`23503` على `started_by_fkey`.

> **فالإعلانُ عن مخاطرةٍ ليس تخفيفًا لها.** «أعرف أن هذا قد يكون خطأً» و«قرأتُ
> `pg_constraint`» **يبدوان متشابهين في تقرير، وأحدُهما وحدَه فعلٌ.**

⚠️ **وسردُ الأعمدة في ملفّ IO أغرى أكثرَ من الذاكرة**، لأنه **مصدرٌ حقيقيٌّ عن
سؤالٍ آخر**: يقول أيَّ أعمدةٍ تُقرأ، **ولا يقول إلى أين يشير أيٌّ منها** — أختُ
«`information_schema.columns` صحيحةٌ ولا تعرف القيود».

> **القاعدة: اسأل الكتالوج، لا الذاكرة.** `pg_constraint` · `pg_type` ·
> `information_schema.columns` · `pg_policies` — **لأنها تعرف ما لم يُسمَّ.**

**⚠️ والقاسم المشترك ليس الإهمال، وهو ما يجعل «انتبه أكثر» جوابًا فاشلًا:**
**الأداة كانت سليمةً بالمرّات الأربع.** ما فشل هو **مصدر المرشَّحات** — والاسم
التلقائيّ، والنوع بدل القيد، وكلّ مصدرٍ ضمنيٍّ آخر **موجودٌ بالتعريف خارج أيّ
قائمةٍ يكتبها إنسان.** فقائمةٌ مكتوبةٌ بيد **تفشل مفتوحةً دائمًا**: تجد ما وضعتَه
فيها، وتسكت عن كلّ ما عداه بنفس نبرة النجاح.

**⚠️ والحدّ يُقال أيضًا:** استعلامٌ على الكتالوج **بسؤالٍ ضيّق** يفشل بنفس
الطريقة — `contype = 'c'` سؤالٌ للكتالوج ولم يرَ النوع. **فالقاعدة ليست «استعلِم»
بل «استعلِم عن الفئة كلّها ثم صفِّ بعينك»:** اقرأ **كلّ** قيود الجدول، **كلّ**
أعمدته، ثمّ انظر — لأن التصفية بالسؤال تُخفي ما لم تتوقّعه، والتصفية بالعين لا.

**⚠️ وأخطرُ صورةٍ من هذا: كتالوجٌ صحيحٌ عن سؤالٍ آخر.**

`information_schema.columns` يُرجع الاسم والنوع وقابليّة العدم والافتراضيّ.
**والقيد ليس في أيٍّ منها.** فقراءةُ `text · YES` واستنتاجُ «مجالٌ مفتوح»
**ليست بيّنةً ضعيفة — هي لا بيّنة**: المخرج نفسه بالحرف سواءٌ كان على العمود
`CHECK` أو لم يكن.

**وصار:** بُني ملفُّ مسحٍ كامل على «خمسة أعمدةٍ بمجالٍ مفتوح» مقروءةً من سرد
الأعمدة، **وأوّلها `products.accounting_direction` مقيَّدٌ بـ`CHECK` بثماني قيم**
— موثَّقًا بمكانين ومُتحقَّقًا منه على القاعدة الحيّة.

> **فالسؤال «هل مجالُه مفتوح؟» جوابُه في `pg_constraint` وحدَه**، و«ما لقيت قيدًا
> بسرد الأعمدة» جملةٌ عن مكان البحث لا عن العمود. **وهي أخت «لم يُقَس ≠ غير
> موجود» حين تكون الأداةُ سليمةً والسؤالُ في الجدول الخطأ.**

**⚠️ والإسقاطُ الضيّق يُخفي كما يُخفي المرشِّح الضيّق — بل أكثر.**

`information_schema.columns` **فيها `domain_name`، وهي معبَّأة.** و٠٧٩ب_١ اختار
الاسم والنوع والعدميّة والافتراضيّ **ولم يخترها**. فالعمودُ الذي كان يحسم
السؤال **كان داخل مجموعة النتائج التي بين أيدينا**، وقُرئ المخرج كأنه كلّ ما في
الصفّ.

> **فليست «الجواب كان بالمستودع» — الجواب كان على الشاشة، بعمودٍ لم يُسأل عنه.**

⚠️ **والمرشِّحُ على الأقلّ يُعلن عن نفسه في `WHERE`؛ الإسقاطُ لا يترك أثرًا
إطلاقًا** — لا شيء في المخرج يقول إن هناك عمودًا سادسًا لم يُطلَب.

**⚠️ وأخبثُ صورةٍ للتضييق: أن ينتقل من `WHERE` إلى `JOIN`.**

كُتب بترويسة ٠٨٠_١ أن `contype='c'` سؤالٌ ضيّق **ثم كُتب تحتها**
`join pg_class c on c.oid = con.conrelid` — **وقيدُ الـdomain عنده `conrelid = 0`
فيُرمى قبل أن تُقرأ `contype` أصلًا.** التضييقُ نفسُه، **بموضعٍ لا يبدو تضييقًا**،
تحت ترويسةٍ تحذّر منه.

**ومقيسٌ لا مُحتجٌّ به:** عمودٌ نوعُه `domain` بقيد `CHECK` **يظهر
`text · YES` بسرد الأعمدة، وصفرَ صفوفٍ باستعلام القيود، ويرفض القيمة عند
الإدراج.** **مغلقٌ تمامًا، ويبدو مفتوحًا على مصراعيه بالاستعلامَين معًا.**

### 5. هوية الدالة = عدد المعاملات وأنواعها

إضافة معامل جديد — حتى لو إله `default` — بتعمل **overload** مش استبدال.
بتصير نسختان، والاستدعاء بيصير غامضًا. احذف القديمة صراحة:

```sql
drop function if exists my_function(uuid, timestamptz, uuid);
```

وتأكد إن `count(*) = 1` بعدها.

### 6. استبدال view: أعِد كتابة خياراته صراحةً، ولا تسأل إن كانت بتنحفظ

`create or replace view` بلا `WITH` — **وما بينفع تبني على معرفتك إن الخيارات
بتنحفظ أو لأ.** لأن الكلفتين غير متكافئتين إطلاقًا:

| | |
|---|---|
| صريح وهي محفوظة أصلًا | سطرٌ زائد، بلا أثر |
| محذوف وهي بترجع للافتراضي | **`security_invoker` بتضيع ⇒ الـview بيشتغل بصلاحية مالكه ⇒ كل صالون بيشوف بيانات كل صالون** |

⚠️ **والفشل صامت تمامًا عند صالون واحد بالقاعدة** — ما بيبان مهما جرّبت. بيبان
يوم يوجد صالون تانٍ، **وما في شي حينها بيشير لهذا السطر.**

```sql
create or replace view public.product_balances
  with (security_invoker = true)
as select …
```

**والتحقّق بيقرأ الصلاحية لا بيفترضها** — سرد الأعمدة بيقول «الشكل سليم» عن
view ممكن يكون فتح كل شي:

```sql
select relname, reloptions from pg_class
where relnamespace = 'public'::regnamespace and relname = '…';
-- المتوقَّع: {security_invoker=true}
```

**والقاعدة أعمّ من الـviews:** أي `CREATE OR REPLACE` بيعيد كتابة الكائن كاملًا،
**وكل خاصيّة ما بتنذكر معرَّضة**. نفس عائلة `SECURITY DEFINER` و`search_path`
بالدوال (البند ٥ فوق).

### 7. إعادة تسمية عمود بتكسر أجسام الدوال بصمت

الأجسام مخزّنة نصًا وبتنحلّ وقت التنفيذ. أي `rename column` بيلزمه إعادة
إنشاء كل دالة بتذكر العمود، **بنفس السكربت**.

- **⚠️ قيدٌ على المدى ما بيحرس من قيمةٍ مشروعةٍ بالمعنى الخاطئ.**

  `post_stock_document` **فيها `unit_cost_required` فعلًا** — القاعدة بتفرض
  سعرًا بالتوريد وبترفض السالب. **وما منعت علّة التسميم**، لأن الخانة الفارغة
  وصلت `0` و`0` بتجتاز `v_cost >= 0` باقتدار.

  **فحارس القاعدة كان قائمًا والعلّة مرقت من فوقه، وما أوقفها إلا حارس الشاشة**
  (رفض الفراغ **قبل** ما يشوفه `Number()`).

  **والدرس مش «الشاشة أهمّ»** — الدرس إن القيد بيحرس **المدى** والشاشة بتحرس
  **المعنى**، وصفرٌ مقصود (عيّنة مجانية) وصفرٌ من خانة فاضية **نفس الرقم
  بالضبط**. ما بيفرّق بينهما إلا اللي بيشوف الخانة.

### 8. `default` على عمود جديد بيكتب ادّعاءً عن التاريخ — وفحصُه ما بيقدر يفشل

`add column cost_is_estimated boolean not null default false` **ما بيملّي
فراغًا، بيكتب جملة** على كل صف قائم: «هذا السعر أملاه إنسان أو طلع من متوسّط
حقيقي». **والقاعدة ما بتعرف هذا** — العمود اللي كان بيعرف هو نفسه اللي عم
ينضاف.

⚠️ **والفحص الطبيعي عاجز بنيويًا:**

```sql
select count(*) from stock_movements where cost_is_estimated;   -- المتوقَّع صفر
```

الجواب صفر **بحكم `default` نفسه لا بحكم الحقيقة** — يعني بيفحص إن بوستجرس
نفّذ `default`، وهاد مضمون. **وفحص ما بيقدر يفشل مش فحصًا.**

**والصح: سرد يُقرأ بالعين، لا عدّاد يُقارَن.** الصواب هون حكم إنسان («من وين
إجا سعر هالصف؟») لا رقم متوقَّع. ولمّا تكون الصفوف قليلة (تمانية، بيانات
تجربة) الكلفة صفر اليوم وغير قابلة للاستدراك بعد شهر.

**وهو «لم يُقَس ≠ غير موجود» بالاتجاه المعاكس: `false` مكتوبة افتراضًا مش
`false` مقيسة.**

### 9. القيمة بتنسند جوّا الفرع اللي بيعرفها، لا بعد ما يسكّروا كلهن

متغيّر بينكتب بفرع واحد وبينقرأ **بعد** الفرعين هو خطر، والتصفير الاحتياطي
بأول الحلقة **حارس** إله — والأفضل منه بنية ما فيها شي يُحرَس:

```sql
if p_doc_type in ('supply','opening') then
  …
  v_estimated := false;                          ← الفرع بيعرف جوابه
else
  select sum(quantity_base) into v_sum_qty …;
  v_estimated := (coalesce(v_sum_qty, 0) <= 0);  ← وهاد كمان
  …
end if;
```

بدل `v_estimated := (p_doc_type not in (…)) and coalesce(v_sum_qty,0) <= 0;`
بعد الفرعين — تعبير بيقرأ متغيّرًا **ما انكتب بهالتكرارة أصلًا** لما المستند
توريد، وبينجو بالمصادفة لأن الشقّ الأول كاذب فبتقصر الدارة. **أي إعادة ترتيب
بتكسره بصمت.**

**والفرق مش أناقة:** التصفير سطر بتحذفه إعادةُ تنظيم صحيحة وما بيشتكي شي —
نفس مصير التعليق اللي انحذف مع توحيد `hasKnownValue`. والإسناد جوّا الفرع ما
فيه شي يُحذف. **الحارس كود، والكود المكتوب عن علّة معرَّض إلها.**

**وشرط واحد يُقرأ مرّتين، لا شرطان متقابلان:**

```sql
✗  v_estimated := (v_balance <= 0);   …   if v_balance > 0 then
✓  v_estimated := (v_balance <= 0);   …   if not v_estimated then
```

نقيضان لازم يبقوا نقيضين للأبد، وواحد بينعدّل لحاله يومًا فبيصمت الخلاف —
**نفس علّة `hasKnownValue` بالضبط** (خليّة ومجموع بشرطين تباعدوا).

⚠️ **وسؤال مرافق ما بينفترض: `select … into` عند صفر صفوف بيسنّد العدم.**
عليه مبنية كل سلسلة التكلفة (`if v_cost is null` هو اللي بينقّلها لدرجتها
الجاية). لو كان بيبقي القيمة القديمة، خيبةُ البحث بتترك **تكلفة المنتج
السابق من الحلقة** مختومة — رقم غلط، مقنع، دائم، وبلا سطر يشتكي. فيه فحص
جاهز بآخر [docs/sql/043-cost-estimated.sql](docs/sql/043-cost-estimated.sql).

### 10. السكربتُ يُودَع قبل أن يُشغَّل

لا يُشغَّل على قاعدة البيانات نصٌّ ليس مُودَعًا في `docs/sql/`.
والترتيبُ لا يُقلَب: يُكتب الملفّ · يُودَع · يُشغَّل · تُسجَّل حالتُه.

🔴 وسكربتٌ عاش في `/tmp` هو سكربتٌ لم يوجد.
   مقيسًا (٢٦ آب ٢٠٢٦): السكربتات ٠٠١–٠٤١ لم تُحذف — لم تُودَع قطّ، ولا تُستردّ.
   والسكربت ١١٠ عاش في `/tmp` هذا الأسبوع، فالعلّةُ تعمل الآن لا في الماضي.

ومخرَجُ التشغيل يُنقل بنصّه إلى سطر الحالة في `docs/sql/README.md`، لا يُلخَّص.
ورقمُ السكربت عرفٌ بشريٌّ لا أداة — ٠٤٣ أُودع قبل ٠٤٢ بيوم — فالترتيبُ
لا يُقرأ من الاسم، ويُقرأ من سطر الحالة وحدَه.

**و«أُودع» تُعرَّف، فالبندُ يستعملها ثلاثَ مرّاتٍ ولم يعرّفها (٢٧ آب ٢٠٢٦):**

> **مُودَعٌ = ملتزَمٌ ومدفوعٌ إلى الخادم. لا مدموجًا.**

⚠️ **والحدُّ مشتقٌّ من الضرر المقيس لا من اللفظ:** ٠٠١–٠٤١ · ١١٠ · `check5` ·
«٠٨٠ الضيّقة» — **أربعتُها لم تدخل `git` قطّ، ولا واحدةٌ منها «كانت على فرعٍ
فحُذف».** ⇒ **فالدفعُ يسدّ الضررَ المقيس، والدمجُ يضيف الاكتشافَ لا البقاء.**

**وثغرةُ الدفع وحدَه تُسدّ بشرطين، لا بتشديدٍ في التعريف:**

```
① سطرُ الحالة يحمل رقمَ الإيداع الذي جرت منه النسخة
   ⟹ فإن حُذف الفرعُ لاحقًا، بقي في main رقمٌ يدلّ على نصٍّ مُستردّ
② وكلُّ صفٍّ في الجدول يسمّي ملفًّا موجودًا في `main`
   ⟹ فسكربتٌ جرى من فرعٍ ثمّ هُجر يصير صفُّه إحالةً معلَّقةً تُقاس بأمر
```

**والاتّجاهُ الآخر محروسٌ آليًّا** —
[lib/sqlStatusRows.test.js](lib/sqlStatusRows.test.js): **ملفٌّ في `docs/sql`
بلا سطرِ حالةٍ يُسقط الحزمة، ويُسمّى في الرسالة.**

🔴 **وحدودُه مكتوبةٌ في رأسه هو ولا تُنسخ هنا** — **فنسختان تتباعدان، وقائمةُ
حدودٍ ناقصةٌ أخطرُ من غيابها لأنّها توهم التمام.** ⇒ **من يبني على الحارس
يفتح ملفَّه ويقرأ الحدودَ حيث تُصان مع الشيفرة التي تصفها.**

🔴 **وكان مكتوبًا هنا «وحدودُه خمسةٌ»: فامتُنع عن نسخ القائمة ونُسخ عددُها —
فتباعد في أوّلِ تغييرٍ بعد التصميم، بفارق ساعات.** ⚠️ **وكان العددُ مُلتبسًا
يومَ كُتب لا يومَ تغيّر: عدَّ نقاطَ القائمة لا حدودَها، وأُولاها `✅ يحرس` وهي
ما يحرسه لا حدٌّ عليه.** ⇒ **والعددان — النقاطُ والحدودُ الحيّة — يُقرآن من
رأس الحارس ولا يُنقلان هنا.**

📌 **والقاعدةُ أعمُّ من الواقعة: الامتناعُ عن النسخ لا يكفي.** ما نُقل — ولو
كلمةً أو عددًا — **يُسمَّى موضعُه الثاني في رأس الأوّل**، فيُفتح ويُفحص. ⚙️
**وموضعُ استدعائها: قبل إيداعِ تغييرٍ يمسّ شيئًا موصوفًا في موضعٍ آخر — تُفتح
المواضعُ المسمّاةُ وتُقرأ.** ⇒ **وكلُّ عددٍ يُكتب مع ما يعدّه: «خمسُ نقاطٍ»
ليست «خمسةَ حدود».**

⚠️ **وأثقلُها يُقال هنا لأنّه يمسّ هذا البندَ لا الحارس:** الحارسُ **يفرض
عرفًا غيرَ مكتوب** — أنّ كلَّ سكربتٍ مُودَعٍ مرقَّم. ⇒ **فمن يودع
`docs/sql/helpers.sql` تسقط عليه الحزمةُ ولا يجد القاعدةَ التي خالفها.**

⚠️ **وأُجّل هذا الحارسُ يومَ شُحن البند، عمدًا:** كان ٥٥ ملفًّا بلا سطر، فكان
سيُولد أحمرَ ومعه قائمةُ استثناءٍ بأسمائها. **وحارسٌ يُولد أحمرَ يُطفأ في أوّل
يومٍ مزدحم.** ⇒ **فمُلئ السجلُّ أوّلًا (١٦٨ من ١٦٨)، ثمّ وُلد أخضرَ
بلا استثناءٍ واحد.**
✅ **وكان اثنان منها تغطيتُهما من مدًى يُعلن الغياب (`٠٧٦` · `٠٧٨`) — فكُتب
لهما صفّان وأُغلق التوسيعُ في اليوم نفسِه (٢٧ آب ٢٠٢٦).** 🔴 **وأثرُ الثغرة
لم يكن على الحراسة وحدَها: الملفّان سقطا من دفعة الإضافة لأنّ المُنتقيَ
عدَّهما مغطَّيَين — فكانت خمسةً وخمسين وكان صوابُها سبعًا وخمسين.** ⇒ **فأداةُ
القياس حدّدت نطاقَ العمل فأنقصته بعطبها، ولا يُكشف ذلك من داخل الدفعة:
قائمةُ «ما لا صفَّ له» مخرَجُ الأداة المعطوبة نفسِها.**

## الكتابات — وين تنحطّ الضمانة

كل كتابة جديدة بتقع بوحدة من ثلاث، والفرق بينهن **مين بيتضرّر لو الكتابة
وقفت بالنص**:

1. **نداء عادي من العميل** لما تكون الكتابة صفًّا واحدًا. الصف بيكتب كله أو
   ما بيكتب — بوستجرس بيضمنها مجانًا، ودالة بتلفّها بتكرّر ضمانًا موجودًا.
2. **RPC** لما تكون صفوفًا متعددة **ويُشتقّ منها شي تاني**، أو لما تقرأ ثم
   تكتب بناءً على اللي قريته (`post_stock_document` عم بتعمل الاثنين).
3. **قيد أو مُشغّل بالقاعدة** لما تكون القاعدة عن **صحّة صف واحد** لا عن
   علاقة بين كتابات — لأن حارس بالواجهة بينحلّ بأول نداء من مكان تاني.

**والحد اللي بينفلت هو بين ١ و٢، وما بينحسم بعدد الصفوف.** جدولان الاثنان
بيتكتبوا بعدة نداءات، وواحد لزمه RPC والتاني لأ:

- `product_set_components` **بيان تعريفي**: وصف كتبه إنسان. لو انحذف السطر
  ونجح، وفشل الإدراج، بيرجع الطقم ناقص مكوّنات، **وما حدا اشتقّ إشي من الحالة
  الناقصة**، **وإعادة الحفظ بتصلحها لأن الفرق بينحسب من قراءة جديدة للجدول**
  لا من صورة عن اللي كان.

  ⚠️ **ومش لأنها «مرئية».** النافذة بتعرض اللي كتبه الشخص لا اللي بالجدول،
  فبعد فشل جزئي بيشوف قائمته كاملة وفوقها شريط خطأ، وما بيعرف أي صف نجا إلا
  لما يسكّر ويفتح. الرؤية ما كانت يومًا جزءًا من البرهان.
- `stock_movements` **سجلّ**: ما حدا بيقرأ الحركات، بيقرأوا **رصيدًا محسوبًا
  منها**. فنصف مستند بيخلّي رقمًا غلطًا، **مقنعًا**، ودائمًا — وما في شاشة
  بتقدر تعرضه كنقص لأنه بيبان رصيدًا سليمًا.

فالسؤال مش "كم صفًا؟" بل: **الحالة الناقصة مرئية وقابلة للتصحيح، ولا صامتة
ودائمة؟** ولما تختار ١ لكتابة متعددة، اكتب السبب فوق الدالة — بلا سطرين
بيلاقي اللي بعدك سابقتين متناقضتين وما فيه شي يفصل بينهن.

- **🔴 وكتابةٌ بتمسّ جدولين يملكهما خطّافان لازمها تحدّث الاتنين — والنسيان
  بينقرأ بيانةً غلط لا خطأً.**

  صار ووصل شاشة المالك: إنشاءُ مجلّدٍ بيكتب `product_categories` (بـ
  `useProductCatalog`) **ورابطَه** بـ`storage_categories` (بـ
  `useInventoryDirectories`)، و`onSaved` كان `catalogue.reload` وحدَها.
  فظهر المجلّدُ ورابطُه لأ، **وقرأت الشاشةُ غيابَه عن نسختها القديمة «ما في
  رابط»** ⇒ شارةُ «بلا مستودع» عن رابطٍ مكتوبٍ بالقاعدة، واختفاءٌ من شجرة
  مستودعه.

  ⚠️ **وهاد أخبثُ من عطل كتابة:** الكتابةُ سليمة والقاعدة سليمة، **والشاشةُ
  بتعرض حالةً قديمةً كأنها الحقيقة** — فالتشخيصُ بيروح للكتابة وهي آخرُ مكان
  فيه العطل.

  ⚠️ **وولا اختبارٍ عندنا كان يقدر يمسكه**: اختباراتُ الشاشة بتمرّر المصفوفة
  **خاصّيّةً جاهزة**، فما في واحدٌ منها بيمرّ بـ«شو بيصير **بعد** الكتابة».
  اختبارُ الدالّة النقيّة أعمى عن التحديث **بنيويًّا**.

  **والعلاجُ بنيويٌّ لا قاعدةٌ تُتذكَّر: المصفوفةُ ودالّةُ تحديثها بتوصلوا
  كائنًا واحدًا** (`directories={directories}` لا `storageCategories={…}`) —
  فما بيقدر منادٍ يعطي وحدةً وينسى التانية، لأنّهما شيءٌ واحد. **ومُحدِّثٌ
  واحدٌ بكلّ موضع حتى حيث يكفي أحدُهما**، لأن «متى يكفي الواحد؟» سؤالٌ بينطرح
  بكلّ إضافةٍ جايّة **وإجابتُه الخاطئة صامتة**.

  والحارس بـ[lib/storageFolders.test.js](lib/storageFolders.test.js): كلُّ
  `onSaved` نفسُ الاسم، وتعريفُ ذاك الاسم بينادي الاتنين.

## المخزون — ثلاث قواعد لا تُخرَق

- **ما في رقم بلا وحدته قدّام إنسان — والوحدة قبل الرقم لا بعده.**

  **⚠️ «رقم» لا «كمّية».** كُتبت القاعدة بكلمة «كمّية» فانطبّقت على نصف السطر:
  الكمّية تقول `بالعبوة: 5 · بالقطعة: 5`، والسعر جنبها يقول **«تكلفة الوحدة»
  وما بيقول أيّ وحدة** — و`unit_cost` مخزَّن **للوحدة الأساسية**، فمنتج بعبوة
  من ١٥ بيعرض `6.6667` واللي كتب `100` ما بيتعرّف عليه. صار السعر يسمّي وحدته
  (`تكلفة القطعة`) مشتقّةً من `base_unit` بنفس آلية `بالقطعة`.

  **وحدّ القاعدة تعلّمناه من السعر: إطاران فقط لما يكون الاثنان مخزَّنَين.**
  الكمّية بتعرض إطارين لأن `entered_quantity` و`quantity_base` **عمودان
  محفوظان**، فالعرض بيقول حقيقتين. والسعر إله إطار واحد محفوظ، والتاني لازم
  يُشتقّ — **وأول رسم حقيقي للمشتقّ طبع `100.0005 ₪` لسعر كُتب `100`**، لأن
  العمود بيحفظ أربع منازل فـ`100/15` بترجع `6.6667` والضرب ما بيقفل. **ورقم
  مشتقّ جنب رقم محفوظ، على سطر مال، بموديول كل تاريخه `unit_cost` غلط — هو
  عين ما بيرفضه ADR-051.** فالوحدة تُسمّى، ولا شي يُخترع.

  المالك قرأ حركته ما تعرّف عليها: أدخل **٥ عبوات** والصفّ بيقول **٧٥**، فقال
  «ما بتذكر إني عملت تحويل 75». الاثنان صح — `entered_quantity = 5` و
  `entered_uom = package` و`quantity_base = 75`. **والمشكلة إن صاحب النظام
  نفسه ما تعرّف على حركته**، فموظّفة الاستقبال بعد ستة شهور أولى.

  **فكل كمّية بتنعرض بتحمل الإطار اللي انكتبت فيه:** `بالعبوة: 5 · بالقطعة: 75`
  — لا `75` وحدها (اللي أدخلها ما بيعرفها) ولا `5` وحدها (ما بتنجمع مع سطر
  انكتب بالقطعة). **الاثنان معًا وحدهما بيصدقوا مع القارئ ومع الحساب.**

  **والوحدة قبل الرقم** لأن `5 عبوة` غلط و`5 عبوات` صح، وما منريد فرعًا نحويًا
  — نفس قاعدة العدد فوق. `بالعبوة: 5` بتصحّ عند ١ و٢ و٥ و١٥.

  وبينسحب على الرصيد وشاشة الجرد وأي تقرير جاي. وهو أخو البند ٣١
  (`nominal_purchase_price` رقم بلا وحدة بيُقرأ بوجهين) بلبوس الكمّية بدل السعر.

- **حركة الجرد بلا إطار مُدخَل: `entered_quantity = null` و`entered_uom = null`.**

  بكل نوع مستند تاني العمودان وجهان لرقم واحد (`entered × factor = base`).
  **وبالجرد لأ:** `post_stocktake` بتكتب `v_diff := v_counted - v_balance`
  بـ`quantity_base` وبتحطّ **اللي مرّره النادي** بـ`entered_quantity`،
  **وبتقبل أي جواب بصمت**. والخيارات تلاتة:

  ```
  العدد  ⇐ بيرسم «بالعبوة: 10 · بالقطعة: -5» بكل سطر جرد
  الفرق  ⇐ الثابتة بتصمد، والعدد المعدود ما بينحفظ بأي مكان
  ولا شي ⇐ ما في ادّعاء غير صحيح                              ← هاد
  ```

  **مش تسوية بين اثنين** — هو اللي بتفترضه `movementFrames` أصلًا، ومثبَّت
  باختبار بـ[lib/stockDocumentList.test.js](lib/stockDocumentList.test.js)،
  ومبنيّ تنفيذًا بـ`stocktakeAdjustment` بـ
  [test-utils/stockFixtures.js](test-utils/stockFixtures.js).

  ⚠️ **وهاد السطر موجود هون لأنه كان بتعليق اختبار وحده — والتعليق بيثبّت ولا
  بيُعلن.** الجواب كان مكتوبًا عندنا **قبل جولات** ومرّ سؤالًا مفتوحًا لأن
  اللي بيكتب نادِيَ `post_stocktake` ما بيقرأ اختبار قائمة المستندات.
  **التثبيت والإعلان وظيفتان مختلفتان: الاختبار للأولى، وهذا المكان للتانية.**

  ✅ **ومقيس بالمخطّط: `entered_quantity` و`entered_uom` الاثنان `nullable`** —
  فالقرار **نادٍ لا مخطّط**، ومطبَّق فعلًا، ولا شي بيعترضه.

- **`unit_cost` بـ`stock_movements` هو `NOT NULL` — وهاد قيد صحيح يجب أن يبقى.**

  فـ`coalesce(v_cost, 0)` بآخر سلسلة الاحتياط **ما كان كسلًا ولا اختصارًا، كان
  إلزامًا**: العمود بيرفض العدم، فالسلسلة مضطرّة تنتهي برقم، **والرقم الوحيد
  المتاح للجهل هو الصفر**.

  ⚠️ **والعلاج الظاهر — تخليته `nullable` — أسوأ من العلّة**، لأن
  `sum(quantity_base * unit_cost) / sum(quantity_base)` بيتخطّى العدم ولا
  بيبطل فيه: **حركة بتكلفة `NULL` بتطلع من البسط وبتبقى بالمقام**، فالمتوسّط
  بينخفض بصمت بمقدار حصّتها. **وهي علّة التسميم بأنظف صورها — رقم أصغر من
  الحقيقة، متّسق مع نفسه، بلا خطأ ولا سطر — وأخفى من الصفر، لأن الصفر بينشاف
  بالسطر والانخفاض ما بينشاف إطلاقًا.**

  **ومقيس عندنا لا مستنتَجًا:** اختبار «only SOME movements were priced»
  بـ[test-utils/stockFixtures.test.js](test-utils/stockFixtures.test.js)
  بيوري `10 × 100` مع `10` بلا تكلفة = متوسّط **50** لا 100.

  **فالجهل حقيقة عن الرقم لا قيمة له، ومكانه عمود بجانبه** — `cost_is_estimated`،
  منفَّذ. **فبتقدر الشاشة تقول «١٢ قطعة · التكلفة ٠ ₪ (غير معروفة)»** بدل
  `0 ₪` صامتة.

- **⚠️ ومعنى `cost_is_estimated` بالضبط: نفيُ الدرجة ١ — لا «مُشتقّة مقابل
  مُملاة»، ولا «لمّا تنزل السلسلة درجتها الأخيرة».**

  الاتنين كتبتهن، والاتنين غلط، وانكشفوا بأول جرد حقيقي — **سطرين بنفس مستند
  الجرد**، فما في قاعدة مصوغة بنوع المستند بتفسّرهن:

  ```
  شامبو (نقص)   -2 @ 50   cost_is_estimated = false   ← إله رصيد وتاريخ هون
  باكيج (فائض)  +5 @  0   cost_is_estimated = true    ← ما تحرّك قطّ
  ```

  **والإسناد بالدوال التلاتة هو حرفيًا نفي الدرجة ١:**

  ```sql
  v_estimated := (v_balance <= 0);
  if not v_estimated then   -- الدرجة ١: متوسّط مرجّح من حركات هذا المستودع
  ```

  **فتلات طرق لـ`false` لا وحدة:** سعر أملاه إنسان (توريد/افتتاح — **ولا
  سلسلة بتنستدعى أصلًا**) · الدرجة ١ نفسها (**وهي مشتقّة، وهون بالضبط بتنكسر
  صياغة «مشتقّة مقابل مُملاة»**) · ونسخًا لمّا يعكس مستندٌ حركةً.
  **و`true` = الدرجات ٢–٥، من أوّلها لا من آخرها.**

  **والجملة الجامعة اللي بتنحفظ:** الرقم **إمّا ثمن دُفع فعلًا، أو متوسّط
  أثمانٍ دُفعت فعلًا لبضاعة موجودة هون**. وما عداه بديلٌ عن ثمنٍ ما قدر هذا
  المستودع يقدّمه — **وهو وحده اللي بينوسم**.

  ⚠️ **والوصف الغلط كان مُودعًا بالقاعدة نفسها** (`comment on column` بسكربت
  ٤٣)، فتصحيحه بالملفات وحدها بيخلّي القاعدة تقول شي والمستودع يقول غيره —
  انصلح بسكربت مستقلّ (٠٤٦). **والدرس: وصفٌ بيوصف سلوكًا بيصير جزءًا منه لمّا
  ينشحن.**

- **⚠️ خاصّيّةٌ معروفةٌ ومقبولة، لا بندَ تنفيذ: `entered_unit_price` لا يقول من
  أين جاء.**

  خانةُ سعر التوريد **ترتدُّ إلى سعر الكتالوج حين تُترك فارغة**
  (`orderGrid.js:117 priceBoxValue`)، **والمُرحَّلُ هو ما ظهر في الخانة**
  (`SupplyProductsScreen.js:135`). ⇒ **فالعمودُ يحمل حالتين في نفس النوع:**

  ```
  سعرٌ كتبه إنسانٌ بيده           ⟶  numeric(14,4)
  سعرُ الكتالوج ارتدّت إليه الخانة  ⟶  numeric(14,4)   ← ولا عمودَ يفرّق
  ```

  ✅ **وهذا مقبولٌ بحكم الاتّساق:** ما يُعرض لاحقًا هو **ما رآه المرحِّلُ وأقرّه
  بالترحيل**، فالشاشةُ صادقةٌ عمّا حدث. **والضائعُ أصلُ الرقم لا قيمتُه.**

  🔴 **ويُكتب هنا لسببٍ واحد: كي لا يُبنى عليه تمييزٌ غيرُ موجود.** تقريرُ
  «الأسعارُ المعدَّلةُ يدويًّا» **غيرُ قابلٍ للبناء من البيانات القائمة** —
  ويلزمه عمودٌ جديدٌ يُكتب وقتَ الترحيل، لا استعلامٌ ذكيّ.

  ⚠️ **والإرجاعُ لا يرتدُّ إطلاقًا** (`returnGrid.js:235`)، **والفراغُ الحقيقيُّ
  يُرفض عند الترحيل** على كلّ مستندٍ `money: true`
  (`stockDocumentForm.js:237` ⟵ `unitCostRequired`).

  ⚠️ **ومكانُها هنا لا في `design/document-view-screens-spec.md`:** هي خاصّيّةُ
  **شاشة إدخال التوريد**، أقدمُ من شاشات العرض، **ومَن يبني تقريرًا عنها لن
  يفتح مواصفةَ شاشاتِ عرضٍ لا علاقةَ لها بمشروعه.** (كشفها المراجعُ بسؤالٍ عن
  مسار الحفظ.)

## المخزون — القاعدتان الأصليتان

- **حركة مُسجَّلة خطأً تُعالَج بعكس مستندها ثم إعادة التسجيل. ما في تعديل
  لصفّ بـ`stock_movements` ولا لـ`unit_cost` عليه.** (ADR-051)

  السبب هو المبدأ الحاكم للموديول: **الرصيد مجموع حركات، لا عمودًا يُصحَّح.**
  والتعديل المباشر بيخالفه بتلات طرق: بيمحو اللي صار (الصرفيات مختومة برقم ما
  عاد إله مصدر) · بيكسر التتبّع (العكس بيسيب مستندين بيقولوا شو صار وليش) ·
  وبيفتح بابًا ما بينسكّر (ما دام مقبولًا مرّة، بيصير مقبولًا كل ما بدا الرصيد
  «غلط»). **وبقاء مستندين بدل واحد ميزة لا عيب** — التاريخ بيقول إن غلطًا صار
  وانصحّح، لا إنه ما صار.

- **المُشغّل اللي بيرفع رفضًا بيخصّ إنسانًا بيكتب `using hint = '…'` بلغة
  الإنسان.** الحقل بيوصل الواجهة، والشاشة بتعرضه لما ما يكون للرمز مفتاح
  مسمّى بعد. يعني **من يكتب مُشغّلًا اليوم بيحصل على رسالة مفهومة من لحظة
  كتابته**، بلا مفتاح ترجمة وبلا انتظار جولة مراجعة.

  والترتيب: **مفتاح مسمّى ← `hint` ← الجملة العامّة.** المفتاح أولًا لأنه
  قابل للترجمة و`hint` عربي ساكن بالقاعدة. والشرط **وجود `hint`** لا وجود
  رسالة — استثناء داخلي برسالة تقنية بلا `hint` ما بينعرض كأنه مكتوب لحدا.

  **وكل الشاشات بتستعمل `dbErrorSentence(error, t, context, overrides)`** من
  [lib/dbErrors.js](lib/dbErrors.js) — **طريق واحد، مش تلاتة**. إرجاع مفتاح ما
  بيقدر إلا يرمي الـ`hint`، فلازم المُساعِد ياخد `t` ويرجّع جملة.

  والحارس بـ[lib/oneErrorEntryPoint.test.js](lib/oneErrorEntryPoint.test.js)
  بيمنع رجوع الشوكة: أي شاشة بتلفّ مفتاح خطأ بـ`t()` بإيدها بتسقّطه.

## البيّنة المضادّة — ترتيبها إجراء لا انتباهة

اختبار الحارس بكسره صار عادةً كل جولة، وهو الفحص الصحيح: حارسٌ ما انشاف
بيعضّ مش حارسًا. **بس ترتيبه بيقرّر شو بيضيع.**

⚠️ **`git checkout -- <file>` بيرجّع الملف لآخر نسخة مُودَعة، فبيمحو كل ما لم
يُودَع فيه** — لا الكسر المتعمَّد وحده.

**🔴 وصار مرّتين، والتانية بعد ما انكتبت القاعدة هون:**

```
الأولى   جُرّبت استعارة لون بمحاكاة، ورجّعها checkout ومعها إصلاحٌ
         للمخالفة نفسها اللي الاختبار بيفحصها
التانية  حُقن حارسُ الدورة بـfolderStorageScope، ورجّعه checkout ومعه
         تصديرُ الدالّة والحدُّ — كُتبا قبل الحقن بدقائق
```

**⇒ فالقاعدة «أودِع أوّلًا ← اكسر ← استرجع» قاعدةٌ تُتذكَّر، وفشلت مرّتين.**
وهاد الملفّ نفسُه بيقول شو بينعمل عندها: «لو كانت لنفسك، فأفضل مكان إلها مش
نصًّا يُتذكَّر بل إعدادًا أو حارسًا بيمنع الغلط بلا ما حدا يتذكّر شي».

**فانشال `git` من طريق الاسترجاع أصلًا** —
[scripts/counter-evidence.mjs](scripts/counter-evidence.mjs):

```
node scripts/counter-evidence.mjs snapshot lib/a.js lib/b.js
…  احقن، وشغّل  …
node scripts/counter-evidence.mjs restore     ← من نسخته هو، بايتًا ببايت
```

الاسترجاعُ من نسخةٍ أخذتها الأداةُ بنفسها، **فما في شي غير المحقون بيقدر
ينمسّ — مُودَعًا كان أو لأ.** وبيتحقّق من تطابق البايتات بعد النسخ، **لأن
استرجاعًا بيفشل بصمتٍ بيترك الحقنَ مكانه والتقريرُ بيقول «انرجّع».**

⚠️ **وحقنٌ منسيٌّ بمكانه ما بيقدر ينُودَع:**
[lib/counterEvidencePending.test.js](lib/counterEvidencePending.test.js)
بيسقّط الحزمة على أي نسخةٍ معلَّقة، وبيسمّي الملفّات بالرسالة.
**و[.githooks/pre-commit](.githooks/pre-commit) بيرفض الإيداع نفسه** — بيفحص
وجودَ ملفٍّ واحد، مش الحزمة، لأن خطّافًا بعشرين ثانية بيعلّم صاحبَه يكتب
`--no-verify`.

⚠️ **و`core.hooksPath` إعدادٌ محلّيٌّ ما بينودَع:** فكلُّ بيئةِ عملٍ جديدة —
نسخةٌ جديدة، جهازٌ تانٍ، مجلّدُ عملٍ موازٍ — لازم تشغّل **`npm run setup-hooks`
مرّةً وحدةً بنفسها**، وإلا الخطّافُ موجودٌ وغيرُ موصول. **و`snapshot` بيقرأ
الإعداد وبيصرخ لمّا ما يكون موصولًا** — عند اللحظة اللي بتهمّ بالضبط. و
`git commit --no-verify` بيتخطّى كلَّ خطّافٍ ولا شي بيمنعه، وقولُه أصدق من
الصمت عنه.

**🔴 ورابعةٌ من نفس العائلة، وقعت بعد بناء الأداة — والأداةُ كانت جاهزةً وما
انستعملت:**

```
انعمل إيداعُ تجربةٍ لقياس الخطّاف، وانتراجَع عنه بأمرٍ بيرجّع الشجرة
للإيداع السابق ⇒ محا تلات ملفّاتٍ معدَّلةٍ وغير مودَعة
```

**والسبب إن الأداة كانت بتنقرأ «أداةَ حقن» لا «أداةَ حفظٍ قبل الخطر».**
⇒ **`snapshot` قبل أيّ أمرٍ بيقدر يرجّع الشجرة، لا قبل الحقن وحده.** الكلفةُ
ثانية، والبديلُ شغلُ ساعةٍ ما إله استرداد.

⚠️ **والدرس التاني من نفس الحادثة: مسارُ السماح بحارسٍ ما بينقاس بإنشاء حالةٍ
لازم تتمحى بعدها.** الخطّافُ طرفاه `exit 1` و`exit 0`، **فقياسُ الرفض بيقيس
الاتنين** — والسماحُ غيابُ الرفض لا حالةٌ تانية. وإيداعُ التجربة أنشأ شيئًا
لازمه تراجُع، **والتراجعُ هو اللي محا.**

🔴 **وخامسةٌ (٢٦ آب ٢٠٢٦)، وهي أوّلُ واحدةٍ بلا حقنٍ إطلاقًا — وتوسّع الشرطَ
بدل أن تكرّره: `snapshot` يشمل أمرَ القياس.**

```
$ git checkout -q origin/main -- .      ⟵ داخلَ أمرٍ مركَّبٍ لعدّ اختباراتِ `main`
⇒ أعاد ثمانيةَ ملفّاتٍ إلى حالة `main` — **شجرةً وفهرسًا**
```

⚠️ **وأمرُ التعديل يستدعي `snapshot` في ذهن كاتبه؛ وأمرُ القياس لا يستدعيه**
— **لأنّ القياسَ يُقرأ على أنّه لا يمسّ شيئًا.** ⇒ **فالشرطُ ليس «انتبه للأوامر
المركَّبة» — ذاك مكتوبٌ فوق ولم يمنع.** **الشرطُ: كلُّ أمرٍ يقدر يُرجع الشجرة،
وأمرُ القياس منها.**

✅ **والذي أنقذ: الإيداعُ والدفعُ كانا قد تمّا لغرضٍ آخر** — **ثالثةُ حالةٍ في
هذا الملفّ يأتي فيها الخلاصُ من فعلٍ سابقٍ لغرضٍ آخر لا من حارس.**

🔴 **ويُقال ما نجا بدقّةٍ ولا يُوسَّع:** **«لم يضع شيءٌ ممّا هو تحت المراجعة»**
— **وما عداه غيرُ قابلٍ للقياس بأثرٍ رجعيّ**، فتعديلٌ غيرُ مُودَعٍ لحظةَ الأمر
كان `checkout` سيمحوه بلا أثرٍ يُستدلّ به بعده. ⚠️ **والفرقُ بين الصياغتين هو
الفرقُ بين حادثةٍ مغلقةٍ وحادثةٍ نحسبها مغلقة** — **والأداةُ التي يُقاس بها هي
التي محت المقيس.**

**والبديل الأنظف لمّا يكون ممكنًا: اكسر نسخةً لا الأصل** (كتلة نصّيّة داخل
الاختبار، زي ما بيعمل `sqlVerificationShape` ببيّنته المضادّة) — **ما بيلمس
شجرة العمل إطلاقًا.**

**والحزمة كاملة بعد الاسترجاع مش تزيّدًا:** هي اللي كشفت الضياع تلك المرّة،
و`Tests: 1 failed` وحدها كانت رح تمرّ.

**⚠️ وثالثةٌ من نفس العائلة، مقيسة: الطفرة اللي ما انطبّقت.** حقنٌ بـ`replace`
على نصٍّ ما طابق (فراغٌ أو نهاياتُ سطور) **بيطبع «تمّ» وبيرجّع الحزمةَ خضراء
— فتُقرأ نجاحًا للحارس وهي «ما صار إشي».** فالحقنُ بيطبع **هل انطبّق فعلًا**،
أو بينعمل بتعديل سطرٍ مفهرَسٍ مع فحصِ محتواه قبل الكتابة.

## البناء والسيرفر

- **`next build` بيكتب فوق نفس `.next` اللي سيرفر التطوير عم يخدم منه**، فأي
  بناء والسيرفر شغّال بيخلّي كل المسارات `500`
  (`Cannot find module './chunks/vendor-chunks/next.js'`). صار.

  **الحل مش قاعدة تتذكّرها — مجلّد منفصل:**

  ```
  NEXT_BUILD_DIR=.next-check npx next build
  ```

  `next.config.js` بيقرأ المتغيّر، وبلاه بيبقى `.next` العادي فنشر الإنتاج ما
  بيتغيّر. **القاعدة بتنُنسى، والمسار المنفصل ما بينُنسى.**

- **~~`next build` ما بيمسك اللي بيبان إنه بيمسكه~~ — انسدّت الفجوة، والبناء
  صار هو البوّابة.** (بند ٣٢، منفَّذ)

  كانت: حُقن خطاف مشروط (`if (…) useState()`) و**نجح البناء**، لأن ما في
  ESLint مثبَّت ولا TypeScript — فخطوة «Linting and checking validity of
  types» **كانت فاضية عمليًا**.

  انثبّت `eslint@8` + `eslint-config-next@14` و`.eslintrc.json`، **والقياس
  بنفس الحقنة بالضبط:**

  ```
  خطاف مشروط محقون  →  eslint: rules-of-hooks error  ·  next build: Failed to compile
  بعد الإرجاع        →  eslint: 0 errors               ·  next build: ✓ Compiled successfully
  ```

  **والنقطة اللي بتنفلت: ما لزمه ربط بـ`jest`.** وجود الإعدادات وحده بيشغّل
  خطوة اللينت **جوّا `next build`**، فالبوّابة موجودة أصلًا وبتشتغل لحالها —
  **وهذا نفس درس `NEXT_BUILD_DIR` و`reloadOnPrerender`: الأداة بتفرض نفسها
  بمكان قائم، لا بقاعدة تتذكّرها.**

  **والأخطاء بتوقف البناء والتحذيرات لأ** — مقيس: البناء النظيف نجح ومعه ١٤
  تحذير `exhaustive-deps`. و`npm run lint` بيعرضهن كلهن.

  ⚠️ **وصنف بند ٣٢ فاضي اليوم: صفر مخالفة `rules-of-hooks` بالمستودع كله**
  (٢٤٤ ملفًا). الحارس بيمسك الجاية، **بس الادّعاء إن في صنفًا صامتًا موجودًا
  حاليًا ما تأكّد** — والفرق بين «حارس لحالة قادمة» و«حارس كشف علّة» يستاهل
  يُقال.

- **الحارس اللي ما بيعضّ بيصير خلفيّة بصريّة — وسياسة `exhaustive-deps`.**

  ستّة حرّاس بهالمشروع **بيسقّطوا** (`pagesParse` · `translationKeys` ·
  `arabicCounts` · `oneErrorEntryPoint` · `i18nReload` · حارس كلمات
  البدائيّات)، وواحد **بيطبع سطرًا**: `react-hooks/exhaustive-deps`. وهو
  **الوحيد اللي بيحرس الصنف الصامت** — إغلاق قديم بيقرأ قيمة ماتت فبيطلع جواب
  معقول غلط بلا ما يوقع شي.

  **و١٤ اليوم ٤٠ بعد ستة شهور، وبعدها ما حدا بيقرأهن.** رقم بيكبر بلا ما يمنع
  شي بيصير خلفيّة — **نفس علّة الترويسة اللي «كانت صحيحة يوم انكتبت»، بلبوس
  مخرَجات بدل تعليق.**

  **فالسياسة مش إصلاحهن اليوم، بل تثبيت عددهن:**

  > كل تحذير جديد **إمّا يُصلَح وإمّا يُعطَّل بسطر بيقول ليش**
  > (`// eslint-disable-next-line react-hooks/exhaustive-deps — القيمة مقصودة ثابتة عند التركيب`).
  > **والتعطيل المعلَّل مش تهرّبًا** — بيحوّل «ما حدا بيعرف أيّهن متعمَّد» لـ«كلهن
  > متعمَّدون ومكتوب سببهم»، وهو نفس اللي انعمل مع `react/jsx-key`.

  ⚠️ **ولمّا يوصل صفرًا، تُرفَع القاعدة لـ`error` بـ`.eslintrc.json` — ولا قبل**،
  وإلا بيتعطّل البناء على شي ما انقرّر بعد.

  والحارس بـ[lib/hookDepsRatchet.test.js](lib/hookDepsRatchet.test.js)،
  **وخطّه الأساسي خريطة لا مجموعًا** — إصلاح واحد مع ظهور تاني بمكان تاني
  بينلغوا بعض بالمجموع وبيمرقوا. **ومقيس بالاتجاهين: زيادة بتسقّط، ونقصان
  بيسقّط كمان** (فيُخفَّض الرقم بنفس الـcommit).

- **🔴 الدمجُ في `main` نشرٌ فوريٌّ على التطبيق الذي يعمل عليه موظّفون — ولا
  خطوةَ نشرٍ منفصلة.**

  `main` هو Production Branch على Vercel، **والدمجُ يطلق النشرَ تلقائيًّا.**
  مؤكَّدٌ من المالك، **ومقيسٌ عمليًّا مرّتين في يومٍ واحد**: PR #5 ثمّ PR #6،
  وكلاهما ظهر أثرُه على الرابط الحيّ بلا أن يفعل أحدٌ شيئًا بعد الدمج.

  ⇒ **فتوقيتُ الدمج قرارُ تشغيلٍ لا قرارُ مستودع.** «مدموجٌ» و«منشورٌ على
  الموظّفين» جملةٌ واحدةٌ هنا، **و«نراجع الآن وننشر لاحقًا» غيرُ متاحٍ بلا
  إجراءٍ إضافيّ.**

- **🔴 الأخضرُ محلّيًّا لا يقول شيئًا عن Vercel — والفرقُ ليس في الكود بل في
  ما يُحزَم معه.**

  التطبيقُ الحيُّ عرض **مفاتيحَ الترجمة الخامّة على كلّ صفحة منذ أوّل نشرة**،
  **والفحوصُ ٢٣٤٢ خضراءُ طوال الوقت.** ولم يُرَ لأن أحدًا لم يفتح الرابطَ
  الحيَّ قبل ذلك اليوم.

  ```
  next dev / next start   يقرآن من الشجرة نفسِها      ⟵ يعمل
  دالّةُ Vercel            تُحزَم وحدَها بتتبّعٍ ساكن   ⟵ لا يعمل
  ```

  **و`public/` تُرفَع أصولًا ساكنةً على الـCDN ولا تدخل حزمةَ الدالّة.** فكلُّ
  ما يُقرأ بـ`fs` **وقتَ الطلب** يجب أن يُذكَر صراحةً في
  `experimental.outputFileTracingIncludes` — **وما لا يُذكَر لا يُحزَم، ولا
  شيءَ يشتكي: لا خطأٌ في البناء ولا سطرٌ في السجلّ.**

  ⚠️ **والصفحاتُ السبعُ كلُّها `getServerSideProps` ولا `getStaticProps` فيها
  واحدة** — فلا شيءَ يُخبَز وقتَ البناء، والقراءةُ كلُّها وقتَ الطلب.

  **⇒ والقاعدةُ الأعمّ: أيُّ قراءةِ ملفٍّ بمسارٍ يُبنى وقتَ التشغيل غيرُ
  مرئيّةٍ للتتبّع الساكن.** فكلُّ `readFile`/`readdir` على مسارٍ محسوبٍ يستحقّ
  سؤالَ «هل هذا الملفُّ داخل الحزمة؟» **قبل النشر لا بعده.**

- **⏸ مؤجَّلٌ بقرار المالك، ومسجَّلٌ كي لا يضيع: فحصُ Preview قبل الدمج.**

  هذه الحادثةُ تقول إن الحزمةَ الخضراءَ **لا تجيب «هل يعمل على Vercel؟»**
  إطلاقًا. والاقتراحُ الأوّل كان «افتح رابط الـPreview وانظر» — **وهو يمسك
  الصارخَ وحدَه، ويعمى عن الصامت** (بيانات ناقصة، رسالةٌ لا تظهر).

  ⇒ **والأدقُّ فحصٌ مقروءٌ لا منظور:** طلبٌ واحدٌ إلى `‎/products` على رابط
  الـPreview، وقراءةُ `initialI18nStore` من `__NEXT_DATA__`، **والسقوطُ إن كان
  أيُّ نطاقٍ صفرَ مفاتيح** — وهو عينُ القياس الذي كشف العطلَ فعلًا.

  ```
  curl -s <preview-url>/products | grep -o '"common":{[^}]\{0,40\}'
  ```

  **فارغٌ ⟵ معطوب · فيه مفاتيح ⟵ سليم.** ويصلح سطرًا في أيّ فحصٍ قبل الدمج.

## الواجهة

- كل نص جديد بالواجهة بيجي من ملفات i18n من أول سطر كود — صفر عربي مكتوب
  بالكود.
- **~~أعد تشغيل السيرفر بعد كل مفتاح~~ — القاعدة انحذفت، ما انكتبت أحسن.**

  كانت: `next-i18next` بيقرأ `public/locales/` **مرة واحدة عند الإقلاع**،
  فمفتاح تضيفه والسيرفر شغّال بيوصل المتصفح **باسمه خامًا** رغم إنه بالملف
  وبالـcommit — وبيبان تمامًا زي مفتاح ناقص، فبتدوّر بالمكان الغلط.

  **صارت ستّ مرات عبر تلات صياغات** (قاعدة · فعل مقرون بفعل · سطر بهذا
  الملف)، والسادسة وصلت شاشة المالك لا شاشتي. **واللي بيرجع رغم تلات صياغات
  مش مسألة تذكُّر.** فانحلّت زي ما انحلّ `NEXT_BUILD_DIR` — بإلغاء الحاجة:

  ```js
  // next-i18next.config.js
  reloadOnPrerender: process.env.NODE_ENV === 'development',
  ```

  **والسلوك مقيس على هذا السيرفر لا مقروء من التوثيق** — الدمج
  `{ ...قديم, ...جديد }` سطحي (`i18next.js:1517` بيمرّر `deep = undefined`):

  | التغيير على ملف اللغة والسيرفر شغّال | بلا إقلاع |
  |---|---|
  | إضافة مفتاح (جذري أو متداخل) | ✓ |
  | تغيير قيمة (جذرية أو متداخلة) | ✓ |
  | حذف مفتاح **متداخل** (الأب بينستبدل كاملًا) | ✓ |
  | حذف مفتاح **ورقة بالجذر** | ✗ بيضلّ لحدّ الإقلاع |

  ⚠️ **والسطر الأخير ثغرة باقية، وكنت كتبت عكسها قبل ما أقيسها.** لكنها ضيّقة
  وبتفشل بشكل **أضعف**: عرضها **نصّ قديم**، ما هو مفتاح خام. وهاي هي الفايدة —
  **مفتاح خام عالشاشة صار إله سبب واحد: ناقص من الملف**، و
  [lib/translationKeys.test.js](lib/translationKeys.test.js) بيسقط عليه بالضبط.
  والحارس على سطر الإعدادات نفسه بـ[lib/i18nReload.test.js](lib/i18nReload.test.js).

  **🔴 و«سبب واحد» ناقصة — في تانٍ، وهو اللي وقع فعلًا ووصل شاشة المالك.**

  ظهر `supplyRef.unitCostColumn` خامًّا بلقطة المالك، **والمفتاح موجودٌ بالملفّ
  وبالسيرفر معًا** — مقيسان: `products.json` بيحمل القيمة، و`initialI18nStore`
  بحمولة الصفحة بيحملها كمان (٢١ مفتاحًا بالمجموعة). **والحزمة خضراء
  و`translationKeys` ناجح، وكلاهما كان محقًّا.**

  ⚠️ **والسبب: لوحةُ متصفّحٍ مفتوحةٌ من قبل الإضافة.** القاموس بيوصل **مع تحميل
  الصفحة**، والنافذةُ بتنرسم بالعميل بعد التشبيك — فتبويبٌ محمَّلٌ قبل ما
  يُضاف المفتاحُ بيضلّ ماسكًا الحمولةَ القديمة مهما أُعيد فتحُ النافذة.

  > **و`reloadOnPrerender` بتصلّح كاش السيرفر، ما بتصلّح حمولةً انبعتت.**

  **فالأسباب اتنان لا واحد، والفرق بينهما مقيسٌ بسؤالين:**

  | | ناقصٌ من الملفّ | تبويبٌ قديم |
  |---|---|---|
  | `translationKeys` | ✗ بيسقط | ✓ بيمرّ |
  | القاموس بـ`initialI18nStore` | ما فيه المفتاح | **فيه المفتاح** |
  | العلاج | يُضاف للملفّ | **إعادةُ تحميل التبويب** |

  ⚠️ **ولا تُقال «أعِد التحميل» قبل قياس الاثنين** — هي جوابٌ صحيحٌ لسببٍ
  واحدٍ من اثنين، **وقولُها عن مفتاحٍ ناقصٍ فعلًا بيرمي العطلَ على المستخدم.**

- **القاعدة الموجَّهة لقارئ ما بيمرّ فيها ما بتشتغل — ولو كانت صحيحة.**
  الاثنتان صارتا بجولتين: «لا تشغّل `next build` والسيرفر شغّال» كُتبت للمالك
  وهو ما بيشغّل `next build` أبدًا، و«أعد تشغيل السيرفر» انقالت للمالك وأنا
  اللي بشغّله وبوقّفه (`preview_start`/`preview_stop`). **الفاعل هو أنا
  بالاثنتين.** فقبل ما تكتب قاعدة، اسأل: **مين بينفّذ الفعل؟** ولو كانت
  لنفسك، فأفضل مكان إلها مش نصًّا يُتذكَّر بل إعدادًا أو حارسًا بيمنع الغلط بلا
  ما حدا يتذكّر شي.

  🔴 **ولمّا يكون الفاعلُ المالك: كلُّ تسميةٍ في الوصفة تُنقل من ملفّ الترجمة
  أو من الـJSX — لا من الذاكرة.**

  **وقعت (٢٦ آب ٢٠٢٦):** كُتب في وصفةِ زيارةٍ زرٌّ اسمُه «رمي الجلسة»
  **وهو غيرُ موجود**؛ الزرّان `products:stocktakePeriod.cancel` («إلغاء») و
  `products:stocktake.discardButton` («ألغِ الجرد وابدأ من جديد»).
  ⇒ **فوقف المالكُ عند خطوةٍ لا يجد لها زرًّا، وانتهت الزيارةُ ناقصةً بلا سببٍ
  حقيقيّ.**

  ⚠️ **والاسمُ الصحيحُ كان معروضًا على شاشته**، في تلميح زرّ «إلغاء» الذي
  كتبناه نحن: «مسح العدّ كلّه مش من هون — إله زرّه لحاله جنبه: **«ألغِ الجرد
  وابدأ من جديد»**». ⇒ **فالمصدرُ لم يكن بعيدًا، كان في الملفّ ولم يُقرأ.**

  🔴 **وكلفتُها أعلى من كلفة اسمٍ خاطئٍ في تقرير:** التقريرُ يُصحَّح في الجولة
  التالية، **والوصفةُ تُنفَّذ مرّةً واحدةً عند إنسانٍ لا يملك أن يخمّن ما
  قصدناه.**

  **وبعد أي تغيير لغة، افحص العقد المرسومة لا حمولة `__NEXT_DATA__`.** كل
  الـnamespace بينتسلسل جوّا الحمولة، فـ`includes()` على الصفحة الخام بتلاقي
  **القاموس** وبتقول ✓ عن نص ما انرسم أبدًا — صار، وقال ✓ أربع مرات كذبًا.
  جرّد `<script>` أولًا. **وفحص كاذب أسوأ من غياب فحص**، زي تعليق صحيح صار
  خاطئًا بالضبط: الغياب بيدفعك تقرأ، والكذب بيعفيك.

- **العدد بالعربية ما بينحطّ بمحلّ بيحكمه إعراب.** المعدود بيوافق عدده بخمس
  صور (`مسؤول واحد` · `مسؤولان` · `خمسة مسؤولين` · `١٥ مسؤولًا` ·
  `٢٥٠ مسؤولٍ`)، ونصّ واحد ما بيرضيهن كلهن. `"{{n}} مسؤولًا"` صحيحة من ١١
  لـ٩٩ وغلط بكل ما عداها — وبضمنها ١ و٢، وهنّ أشيع الأعداد بجملة تأكيد.
  ووصلت الشاشة فعلًا.

  **والحل لا صيغ جمع ولا `count` بـi18next** — ست صور جمع عربية بمفتاح ناقص
  بترجعك لنفس المكان. الحل: اكتب اللي ما بيتغيّر بتغيّر العدد، وحطّ الرقم بعد
  نقطتين أو جوّا قوسين:

  ```
  ✗  لهذا المستودع {{n}} مسؤولًا ماليًا
  ✓  لهذا المستودع مسؤولون ماليًا مسجّلون (العدد: {{n}})
  ```

  **رمز الوحدة استثناء** (`د`, `مل`, `كغ`) — اختصار بلا جمع ولا إعراب، فـ`3 د`
  و`45 د` صحيحتان. والوحدة المكتوبة كاملة **ليست** استثناءً: `3 دقيقة` غلط.

  والحارس بـ[lib/arabicCounts.test.js](lib/arabicCounts.test.js) بيمسح كل
  ملفات اللغة، لأن سطرًا هون بينقرأ مرة والجملة الجاية فيها رقم بينكتبها حدا
  بعد شهر ما شافه.

- **التطويل (`ـ` — U+0640) قبل متغيّر: صحيح قبل اسم أو رقم أو مقتبس، غلط قبل
  كلمة عربية.** كان بيتشحن `"بالـ{{unit}}"` فبينرسم **بالـعبوة** والصح
  **بالعبوة** — على كل سطر كمّية بالموديول، **ونجا من مراجعة لقطات** لأن حرفًا
  ممطوطًا بينقرأ كخاصيّة خط لا كعطل. انكشف بلقطة من محرّك حقيقي.

  **والسبب عادة بملف المصدر بتسرّب للمنتج:** لما تكتب `بال{{unit}}` بمحرّر
  JSON، الـ`ل` بتبان منفصلة عن `{{` اللاتينية، فالتطويل بيخلّي **المصدر** يبان
  صح و**الرسم** يطلع غلط.

  **والتمييز ما بينقرأ من النصّ** لأنه بيعتمد على اللي رح يحطّه المتغيّر —
  `لـ"طقم"` و`لـ11:29` صحيحتان، و`بالـعبوة` لأ. فـ
  [lib/arabicTatweel.test.js](lib/arabicTatweel.test.js) بيعدّ **المسموح**
  بأسبابه لا الممنوع: أي تطويل جديد بيسقط، وحدا بيبصّ عليه مرّة ويضيفه أو
  يشيله. **قائمة المستثنى بتفشل مغلقة.**

- **⚠️ كل مفهوم بيخترعه النظام وما طلبه المستخدم — بينشرح حيث بيظهر، بمثال.**
  (قاعدة المالك، بلفظه: «لازم نشرحها للمستخدم، نضيف شرح عنها مهما كان،
  ونعطيه مثال — مشان تصير التجربة أحسن.»)

  **ما حدا بصالون بيعرف شو «التكلفة المقدَّرة»، ولا حدا طلبها.** بتظهر فجأةً
  جنب رقم، **وشارةٌ بلا شرح بتنقرأ عطلًا أو بتنتجاهَل — والاثنان بيفقدوا
  المعلومة اللي انضافت لأجلها.**

- **⚠️ ونصّ المساعدة بيقول شو بيعمل الحقل وشو بيترتّب عليه — لا ليش اخترنا
  هالتصميم.** وسبب التصميم بيروح للتعليق والوثيقة.

  صارت فعلًا: نصٌّ تحت «صاحبة المستودع» كان يقول «اخترنا الموظفة بدل الدور
  عمدًا: رصيد لكل موظفة ما بيتحقّق ببركة مشتركة…». **بيشرح مفاضلةً بين
  بديلين، والمستخدم ما بيشوف بديلين** — قدّامه قائمة أسماء وما في خيار
  «دور» بأي مكان. **فبيجاوب سؤالًا ما انطرح وبيخلق واحدًا: «وين خيار
  الدور؟»**.

  **والمقارنة جوّا نفس الشاشة بتحسمها:** نصّ نوع المستودع بيقول شو بيعني كل
  خيار، ونصّ الغرامة بيقول شو بيترتّب على الفراغ مقابل الصفر — **الاتنان
  بيخاطبوا اللي بيقرّر**، والتالت وحده بيبرّر قرارًا انتخذ بمكان تاني.

  ⚠️ **والعلّة عادةٌ جيّدةٌ بغير موضعها:** ترويسات SQL عنّا بتسمّي البديل
  المرفوض وسببه، **وهاد اللي بيخلّيها تُقرأ بعد سنة** — وهي نفسها الغريزة
  اللي كتبت هالجملة. **بس قارئ الترويسة بدّه «ليش ما انختارت الطريق
  التانية»، وقارئ الشاشة بدّه «شو رح يصير إذا اخترت هالاسم».**

  ⚠️ **والترجمة الحرفيّة بتوقف القارئ:** «بركة» لـ`pool` مش عربيّةً طبيعيّةً
  بهالمعنى، **وبتنقرأ بلا تشكيل «بِركة» (حوض ماء) أو «بَركة» (خير)** —
  ومصطلحٌ بيوقف القارئ لحظة **بجملةٍ انوُضعت لتشرح بيعمل عكس شغله.**

- **⚠️ ورسالة الرفض بتسمّي الحالة اللي وقعت، لا الصنف اللي بتنتمي إله.**

  صارت ووصلت شاشة المالك: خانتا الغرامة «كلتاهما أو لا واحدة»، ورسالةٌ
  واحدةٌ بتشرح **«نسبة بلا أساس»** — **والمالك عمل العكس: أساسٌ بلا نسبة.**
  الرفض صحيح، **والسبب المعروض مش سببه.**

  والشاشة **بتعرف أي خانة فاضية**، فبتقدر تسمّيها. **فرسالتان لا وحدة**،
  وبهيك بيصير الغلط **غير ممكن** بدل ما يكون **منتبَهًا له**.

  **والشرح بيحمل تلاتة، والتالت هو اللي بيخلّيه نافعًا:**

  1. **شو معناها** — بلغة اللي بيقرأها لا بلغة الجدول.
  2. **مثال** بأسماء وأرقام من عالمه.
  3. **كيف تختفي** — فعلٌ يقدر يعمله.

  ⚠️ **والتالتة مش تزيينًا:** شرحٌ بيوصف بلا ما يعطي فعلًا بيترك القارئ
  عاجزًا، **وهاد أسوأ من إنه ما ينشرح** — لأنه بيعلمه بمشكلة وما بيعطيه بابًا.

  **والنموذج موجود بالمشروع:** «المستندان بيضلّوا ظاهرين — التاريخ بيقول إنه
  صار غلط وانصحّح، لا إنه ما صار». بيشرح، وبيزيل قلقًا رح يقع، وبلغة اللي
  بيقرأه.

  **ومكانه: على الشارة نفسها، فتحٌ عند الطلب** — لا نصًّا دائمًا بالشاشة.
  **الشرح بينطلب، والشارة بتنشاف.**

- **صيغة المخاطبة: النصّ الجديد ما بيخاطب أحدًا بجنس.** الحالة مقيسة —
  **١١٠ نصًّا بيخاطبوا المستخدم، كلهم بالمذكّر، وصفر بالمؤنّث وصفر مختلط**
  عبر تمان namespaces. يعني ما في ارتجال، في **قرار انتُخذ بالتقصير** ولا حدا
  اتخذه: أغلب صاحبات الصالونات نساء.

  **للنصّ الجديد:** استعمل المصدر لا الأمر — «إضافة مستودع» لا «ضيف مستودع»،
  و«يمكن البدء بمستودع عام واحد» لا «ابدأ». هيك ما بتخاطب أحدًا فما بتخطئ
  فيه — **نفس حيلة قاعدة العدد فوق: أخرج الكلمة من موضع الإلزام.**

  **⚠️ وتحويل الـ١١٠ القائمة تغيير صوت للمنتج كله، لا لموديول.** تحويل
  namespace واحد بيصنع بالضبط الارتجال اللي عم نتجنّبه: شاشة محايدة وجارتها
  بالمذكّر. فهو بند مستقل يمشي على الملفات التمانية دفعة وحدة، وقرار المالك
  لأن الصيغة المحايدة أطول بكل تلميح.
- **أي مدى ساعات بيمرّ على [components/TimeRange.js](components/TimeRange.js)
  — ولا تكتب واحدًا بإيدك.** الأرقام اللاتينية أوروبية (EN) والفاصل بينها
  محايد، فخوارزمية bidi بتعطي المحايد اتجاه الفقرة وبتبدّل النصفين: المخزَّن
  `10:44 – 11:29` والمرسوم `11:29 – 10:44`. النص والـDOM سليمان، والمقلوب هو
  الرسم — فما بينكشف بقراءة أي قيمة، وهيك ستّ نسخ محلية غلطت وحدة وحدة.
  العازل جوّا المكوّن، وعلى عنصر سطري دائمًا: `dir` على عنصر كتلة بيحلّ
  `text-align: start` باتجاه `ltr` وبيهرّب المحتوى للطرف التاني.

  **العازل بيشتغل على EN بس.** الأرقام العربية-الهندية صنفها AN، وAN **ما
  بينحلّ باتجاه الأب إطلاقًا** — `١٠:٤٤ – ١١:٢٩` بتنرسم نفس الشي مع العازل
  وبدونه، مقيس. اللغة مثبّتة على `ar` (بترجع أرقامًا لاتينية)، بينما `ar-EG`
  و`ar-PS` بيرجّعوا عربية-هندية. لو انضافت لغة تانية أو انكتب
  `toLocaleTimeString()` بلا معامل، المكوّن بيسكت بصمت والحارس بيضلّ ناجحًا —
  فـ[lib/localePinned.test.js](lib/localePinned.test.js) بيمنع الاثنين.

  **ومدى فيه نص عربي قويّ ما بينلمس.** `9:00 ص – 6:00 م` و
  `2 – 8 أغسطس 2026` بينرسموا صح لحالهم، والعازل **بيكسرهم** — مقيس. ولا
  تحطّ `⁦…⁩` بملفات الترجمة كمان: CLDR بيزرع علاماته لما يشوفها لازمة
  وقرر ما يزرعها هون، وودجت فيها مدى معزول وجنبه مدى غير معزول أسوأ من
  الاثنين.

  الحارس بـ[lib/timeRangeDirection.test.js](lib/timeRangeDirection.test.js)،
  وما بيعدّ ملفات بالاسم عمدًا — بيتأكّد إنه ما في إلا مُنسّق واحد وعازل واحد.

  وأي ادّعاء عن رسم أو تخطيط لازمه قياس من محرّك حقيقي (Chrome headless
  بلقطة)، مش فحصًا منطقيًا — لوح المتصفح المدمج بيرجّع كل المستطيلات صفرًا
  فبيجاوب غلط بدل ما يفشل.

  **⚠️ وأبعد من الرسم: React ما بتنشبّك أصلًا بلوح المتصفح المدمج.** مقيس
  سلوكيًّا — زرّ التوسيع ما بيفتح شي، وزرّ العكس ما بيفتح صندوقًا، **وعلى
  حالة الـcommit كمان** (وقّفت السيرفر وشغّلته على الشجرة النظيفة فطلعت نفس
  النتيجة، يعني مش تغييري). فـ**أي سَوق هناك بيقيس HTML ميتًا**، وبيقول «ما
  في صندوق» عن مكوّن سليم.

  **والقياس الحقيقي بـChrome headless عبر CDP** (`--headless=new
  --remote-debugging-port`، و`WebSocket` صار عالميًا بـNode 24) — نفس الصفحة
  ونفس النقرة اشتغلوا هناك وفتحوا الصندوق.

  **وحارس التشبيك: ليف React على عقدة DOM حقيقية (`__reactFiber$…` عبر
  `getOwnPropertyNames`) — لا `__REACT_DEVTOOLS_GLOBAL_HOOK__`.** الخُطّاف
  بيقول `renderers.size = 0` **على صفحة حيّة فعلًا** (نفس التحميل اللي فتحت
  فيه النقرةُ الصندوق)، فبيجاوب «لأ» عن صفحة شغّالة. **وأنا استنتجت منه
  بهالجولة إن React ما اشتغلت، والاستنتاج غلط** — اللي أثبت موت اللوح هو
  الزرّ الميت لا الرقم.
- أخطاء قاعدة البيانات ما بتوصل الشاشة خام: مرّرها على
  `reportDbError(error, context)` من [lib/dbErrors.js](lib/dbErrors.js) —
  بترجع مفتاح ترجمة، وبتسجّل غير المعروف بالكونسول بدل ما يضيع.

## الاختبارات

- **اختبار بيكشف العطل بتعليق التشغيل بيعمي كل اختبار تحته بنفس الملف.**
  حلقة متزامنة ما بتسلّم الخيط بتخلّي مهلة Jest عاجزة عن الانطلاق أصلًا، فاللي
  بيصير مش "فشل" بل **تجمّد**: `exit=124` بصفر مخرجات. وهاد بينقرأ كجهاز بطيء
  أو CI مزاجي قبل ما ينقرأ كحارس مكسور، وأسوأ نهاياته إنه ينحذف عشان الحزمة
  تمشي. **والأخطر إنه بيظلّل:** Jest بيوقف الملف عند التعليق، فأي اختبار بعده
  **ما بيشتغل** — يعني أي طفرة شُغّلت على اختبارات تحته **كانت بتثبت لا شي**.

  صار فعلًا: `isCategoryArchived` كان محروسًا باختبار بيمرّر كائنًا عاديًا
  ويتوقّع `false`. الطفرة علّقت ١٢٠ ثانية بصفر مخرجات، وحارس أقوى مكتوب تحته
  ما وصل يشتغل. الحل: خلّي المسار **مقيسًا** — كائن بـ`getter` بيعدّ القراءات
  ويرمي باسم صريح بعد سقف معقول. صار `exit=1` بخمس ثوانٍ ورسالة مسمّاة.

- **⚠️ صفحة الفحص بتقيس المكوّن، وما بتقيس إن اللي بيوصله هو اللي بيوصل من
  القاعدة.** ولمّا يكون الفرق **بالشكل لا بالمنطق**، بتمرّ خضراء وهي عمياء.

  صار تلات مرات بجولة وحدة، وكلها انكشفت من لقطات المالك لا من فحوصي:
  مستند عكس صنعته بإيدي **بلا حركات** (والحقيقي بيكتب حركة) · تاريخ كتبته نصًّا
  `'2026-08-04'` (والحقيقي `timestamptz`: `2026-08-04T00:00:00+00:00`) · وشاشة
  رسمتها بمفاتيح ما كانت محمَّلة.

  **الدواء مش عين بشرية كل جولة، دواءان:**
  1. **بيانات الفحص تجيها من الدوال نفسها حيث أمكن** — استدعِ الدالة واقرأ
     نتيجتها، لا تصنع صفًّا **بيشبه** نتيجتها. وحيث ما بيمكن (RLS)، **انسخ
     الأشكال الحرفية من صفوف المالك** ولا تخترعها.
  2. **حارس المفتاح الخام**، وهو اللي كان بيمسك المفتاحين اللي وصلوا شاشة
     المالك: بعد الرسم، **أي عقدة نصّية بتطابق
     `/^[a-z][A-Za-z0-9]*(\.[A-Za-z0-9_]+)+$/` هي مفتاح خام**. سطر واحد،
     ما بيلزمه يعرف مفاتيحك، وبيشتغل على أي شاشة — وبيمسك **الحالتين معًا**:
     مفتاح ناقص من الملف، ومفتاح موجود والسيرفر ما انعاد تشغيله.

  و[lib/translationKeys.test.js](lib/translationKeys.test.js) بيسدّ النصّ
  الأول (المفاتيح الحرفية + القوالب بقائمة صريحة)، فبيبقى المفتاح الخام
  عالشاشة معناه **«أعِد التشغيل»** لا شي تاني.

  **⚠️ وتجهيزة صفحة الفحص ما بتنكتب بالإيد — بتنبنى من
  [test-utils/stockFixtures.js](test-utils/stockFixtures.js).**

  الثابتة (`entered × factor === base`) انفُرضت أول مرة **جوّا صفحة الفحص
  نفسها**، والصفحة بتنحذف قبل كل commit — **فالحارس وُلد ومات بنفس الجولة**،
  والجولة الجاية بتبني تجهيزة جديدة بلا شي بيحمل القاعدة إلها.

  **وهاد وجه خامس لنفس الدرس، وأول واحد بيخصّ عُمر الحارس لا نطاقه:**

  | الحارس | اللي كان محدودًا |
  |---|---|
  | حارس العدد | أسماء المتغيّرات |
  | حارس البدائيّات | شكل العقدة (ابن وحيد) |
  | `translationKeys` | المجلّدات |
  | **ثابتة التجهيزة** | **الجولة الواحدة** |

  والبانِي **بيحسب `quantity_base` من المُدخَل بالمعامل**، **وبيرفض اللي
  بترفضه شاشة الإدخال** (`0.5` عبوة من منتج معامله ١ ⇒ استثناء، لأن «القطع ما
  بتتجزّأ»)، **وبيقرّب `unit_cost` لأربع منازل زي العمود**. فالصفحة الظرفية
  بتستورد ولا بتخترع، **ولا شي بينشحن للإنتاج لأن ما بيستورده إلا فحص**.

  ⚠️ **والمقيس منفصل عن المفترَض بالملف صراحةً:** `OWNER_PRODUCTS` منتجات
  المالك التلاتة **وكلها `pcs`** بمعاملات `15 · 1 · 1` (قاسها هو)، و
  `HYPOTHETICAL_PRODUCTS` منفصلة لأن **ما في منتج `ml` ولا `g` بقاعدته
  إطلاقًا** (بند ٣٦). خلطهن بيخلّي فرضيّة تُقرأ سلوكًا مقيسًا — وهو بالضبط
  اللي عمله `تكلفة المل: 0.05 ₪` بلقطة سابقة.

  **و`OWNER_HISTORY` بيحمل الحالات اللي ما بتنكتب بإيد**: رصيد سالب (`-75`) ·
  زوج مسموم انلغى بالبسط والمقام معًا · ومتوسط طلع صحّ **بعد** التسميم.

- **الفحص اللي بيقول ✓ ما بينصدَّق قبل ما تشغّله على حالة لازم يسقط فيها.**
  معدّل الخطأ على طبقة الفحص أعلى من معدّله على الكود — مقيس بهالجلسة: ثلاث
  ✗ كاذبة بجولة وحدة (`.opacity-75` جوّا `<style>` · تعبير أقصر من أيقونة SVG
  · `disabled:pointer-events-none` بقائمة الأصناف)، **و✓ كاذبتين قبلهن**
  (`__NEXT_DATA__` قال ✓ أربع مرات، وطفرة «ما قتلت شي» وهي ما انطبّقت أصلًا).

  **والاتجاه اللي بيسقط فيه الفحص صدفة لا خاصيّة** — ✗ كاذبة بتكلّف وقت، و✓
  كاذبة بتشحن علّة. فالعادة: **بيّنة مضادّة قبل التصديق.** انزع الشي اللي عم
  تفحصه وشغّل الفحص: لو ضلّ ✓ فهو أعمى. صار مع حارس العدد (`_few` بيمرّ ·
  بلا لاحقة بيسقط · `sectionOther` بيسقط) ومع عمود الاتجاه، وكانوا أنظف
  قياسين بالجلسة.

  **🔴 وسادسةٌ، ومكانُها هنا لأنها ✓ كاذبةٌ من رقمٍ متوقَّعٍ كُتب بيد — لا من
  إبرةٍ ولا من انتقاء.**

  ٠٩٥ب/ب توقّع أن يبقى في الدفعة الثانية بالمصدر **٠** بعد نقلِ ١٣ من ١٠+٦.
  **والصوابُ ٣.** فأظهر ✗ على `transfer_stock` سليمة — والمالكُ هو من حسبها.

  ⚠️ **والاتّجاهُ هو العطل لا الرقم:** نقلٌ معطوبٌ يستنزف الدفعةَ الثانية كاملةً
  (٦ بدل ٣) كان يترك `0` **فيُقرأ ✓**. **فالحارسُ كان أعمى عن العطل الذي وُضع
  لأجله بالضبط**، ويصرخ على الحالة السليمة.

  ⚠️ **ونفسُ الحساب كُتب صحيحًا في ٠٩٥ب/أ بنفس الجولة** (`expect 0 , 3`) — فليس
  جهلًا بالحساب، **بل توقُّعًا نُسخ من سياقٍ وعُدِّل نصفَ تعديل.** وهذا ما يجعل
  «اكتب الشرط لا العدد» أصعبَ مما تبدو: العددُ هنا **كان** الشرط.

  ✅ **والذي أنقذها أن الرقم الخامّ يُطبع بجانب الحكم** (`src=0,3`) — **فالمخرَجُ
  حمل بيّنةَ تكذيبِ حكمِه معه.** ولولاه لكان `✗` وحدَه بدايةَ جولةِ تشخيصٍ في
  دالّةٍ سليمة.

  > **فكلُّ حارسٍ يطبع ما قاسه بجانب ما حكم به** — والقارئُ يستطيع أن يخالفه.

  **وجرّد `<script>` و`<style>` قبل أي بحث نصّي بمخرجات المتصفّح** — الأول
  فيه القاموس والتاني فيه أسماء الأصناف، وكلاهما كذب بفحص هون.

  **⚠️ وادّعاءٌ بصيغة «الشيفرة ما بتقدر تنتج هيك» بينفحص ضدّ الشيفرة، لا ضدّ
  القيد اللي رح يرفضه.**

  انكتب اختبارٌ بيقول «الحمولة ما بتقدر تطلع بالشكل اللي بيرفضه
  `products_consignment_supplier_check`» — **والحمولة بتقدر:** خانةُ الأمانة
  مؤشَّرةً بمورّدٍ فاضٍ بتعطي `true` مع `null`. اللي بيمنعها **التحقّقُ
  بالنموذج**، لا بناءُ الحمولة.

  ⚠️ **والاختبار كان ناجحًا** لأن الحالة المخالفة ما كانت بقائمة حالاته —
  **فادّعاءٌ بصيغة «ما بتقدر» انفحص بحالاتٍ كلُّها بتقدر.** ✓ كاذبة بأنظف
  صورها: ما في سطرٌ غلط، **في نطاقٌ أضيقُ من الجملة اللي فوقه**.

  > **فلمّا تكتب «هاد ما بيقدر يصير»، اسأل أيُّ طبقةٍ بتمنعه — وافحص تلك.**
  > **وقولُ أيّ طبقةٍ بتمسكها هو الفرق بين دفاعٍ ومصادفة**، وهو نفس تمييز
  > «القيدُ بيحرس المدى والشاشةُ بتحرس المعنى».

- **السَّوق بالمتصفّح بيستهدف الهُويّة لا الموضع.** كل الفحوص الكاذبة بهالجلسة
  (ستّ) وقعت بطبقة وحدة — السَّوق بالمتصفّح — **والاختبارات الوحدوية ما كذبت
  ولا مرّة**. فالطبقة معلومة، والسبب انتقاء لا منطق.

  آخرها بيسمّيه بدقّة: «بيضغط **أول زرّ عكس مفعَّل**». هذا وصف لـ**مكان** الشي
  لا لـ**هُويّته**، فبينكسر بأول تغيّر بالحالة — والزرّ اللي انتقاه كان سليمًا
  منطقيًا، وخطأ الفحص إنه ما قال **أي مستند** بدّه.

  ⚠️ **والاتجاه اللي بيسقط فيه صدفة لا خاصيّة:** ✗ كاذبة بتنقرأ فبتنصحّح، و✓
  كاذبة من **نفس الانتقاء** بتمرّ — «أول زرّ مفعَّل» بيلاقي اللي توقّعه بمستند
  تاني فبيعلن نجاحًا ما وقع.

  **فالعناصر اللي بينضغط عليها بفحص بتحمل هُويّتها بالـDOM**
  (`data-doc-id` · `data-reverse-for` · `data-confirming-doc-id` بـ
  [components/StockDocumentsList.js](components/StockDocumentsList.js))،
  والسَّوق بيستهدف صفًّا بعينه. **وهو «اكتب الشرط لا العدد» على الـDOM.**

- **اللقطة اللي بتنلتقط وما بتنقرا صورة لا قياس — واللي بتنقرا وما بتنذكر أسوأ.**
  صار: لقطة فحص فيها `Unhandled Runtime Error` بأعلاها انضمّت لتقرير بيقول
  «أربع حالات مقيسة بمحرّك حقيقي»، والخطأ ما انذكر. أنا قريته وشخّصته وصلّحته
  قبل ما أبعت — **وهاد بالضبط اللي بيخلّي السكوت عنه أسوأ:** اللي بيقرأ التقرير
  ما بيقدر يميّز «شفته وعالجته» عن «ما نظرت أصلًا»، فبيحسب القياس أنظف مما كان.

  والقاعدة: **اذكر اللي ظهر بالقياس، وبضمنه اللي صلّحته قبل ما تعرضه.** ومن
  صنف واحد مع فحص `__NEXT_DATA__` فوق — الاثنان إجراء بيطلّع ✓ بلا ما يبصّ على
  اللي بيبصّ عليه الإنسان.

- **الحارس اللي بيعدّ المعروف بيفشل مفتوحًا، واللي بيوصف الشكل بيفشل مغلقًا.**
  دفعنا ثمن هالدرس تلات مرات بنفس الموديول، بتلات ألبسة:

  1. **«اكتب الشرط لا العدد»** — تحقّق مبني على `count(*) = 5` بينكسر بأول مفتاح
     خامس مشروع، والشرط (`سطران ينتهيان بـREFERENCES products(id, kind)`) ما
     بينكسر.
  2. **«القاعدة لا الحصر»** — ترويسة `productAdminIO.js` كانت بتعدّ الجداول
     («ما في حذف بهاد الملف»)، فأبطلتها أول دالة حذف انكتبت بعدها. صارت تقول
     القاعدة: صفّ يشير إليه سجلّ يُؤرشَف، وصفّ تعريفي لا يشير إليه شي يُحذف.
  3. **«الشكل لا قائمة الأسماء»** — حارس نحو العدد كان بيطابق أسماء متغيّرات
     (`count`, `n`)، فأوّل مفتاح انكتب بعده استعمل `{{lines}}` **ومرق من
     تحته**. صار يطابق **الشكل**: `{{أي شي}}` وبعده كلمة عربية.

  **والاتجاه هو كل شي:** قائمة بـ«اللي ممنوع» بتفشل مفتوحة — كل حالة جديدة
  ثغرة صامتة. وقائمة بـ«اللي مستثنى» بتفشل مغلقة — الحالة الجديدة بتنمسك،
  وحدا بيبصّ عليها مرّة وبيضيفها بإيده. **لما تكتب حارسًا، اسأل: شو بيصير لما
  تظهر حالة ما خطرت عالبال؟**

- **🔴 مدى الحارس بينقاس منفصلًا عن حكمه — ونيّةٌ صحيحةٌ بمدًى لا يطابقها وقعت
  أربع مرّات.**

  البند اللي فوق بيسأل عن **شكل** الحارس (بيعدّ ولا بيوصف). وهاد بيسأل سؤالًا
  تاني بالكامل: **وين بيوصل فعلًا؟** والاتنان مستقلّان — حارسٌ شكلُه سليمٌ تمامًا
  بيسكت عن نصف الشجرة، أو بيعضّ كودًا برّا نطاقه، **والحزمة خضراء بالحالتين.**

  ```
  ESLint dirs        القائمة فيها خمس مجلّدات، و`app` و`hooks` مش منهن
  حارس المال         مشية غير عوديّة على `components/` — و`ref` و`ui` و`pages` برّاها
  حارس الاتّجاه       طلب حركةً سالبةً لـ«من» وهي **المورّد**، ولا حركةَ له ⟵ عضّ كودًا سليمًا
  قصُّ اللوح          قصّ من اللوح **لآخر الملفّ**، فالتقط حقلَ سببٍ بنافذةٍ تحته
  ```

  ⚠️ **والاتّجاهان بيفشلوا بشكلين، والتاني بيبان «اكتشافًا»:** مدًى **أضيق**
  بيسكت (فحصٌ أعمى)، ومدًى **أوسع** بيصرخ على شي سليم — **وهاد بيسحبك لتعديل
  الكود الصحيح إرضاءً للحارس**، وهو بالضبط اللي رفضناه لمّا عضّ حارسُ المال
  عرضَ `paid_amount`.

  **⚠️ والقاسم المشترك إن التعليق كان بيقول الصواب بتلاتة من أربعة.** ترويسةُ
  حارس الاتّجاه بتقول حرفيًّا «يُقاس نصفُ المستودع وحدَه» **والشرطُ تحتها بيقيس
  الطرفين**. فالجملةُ الوصفيّة **مش حارسًا** — هي نيّةٌ مكتوبة، ونفسُ صنف
  «تعليقٌ بيصف نيّةً كأنها واقع».

  > **فالقاعدة: اكتب نطاقَ الحارس بجملة، وبعدين ثبّت وصولَه الفعليَّ بحلقةٍ
  > بتسمّي اللي سقط — منفصلةً عن اختبار صحّة الحكم.**

  ```js
  for (const [name, shape] of [['pages', /[\\/]pages[\\/]/], ['hooks', /[\\/]hooks[\\/]/]]) {
    expect(`${name} ⟵ ${scanned.some((f) => shape.test(f))}`).toBe(`${name} ⟵ true`)
  }
  ```

  **والحلقةُ بتسمّي المجلّدَ الساقط**، فالفشلُ بيقول «`hooks` ما انمشيت» لا
  «توقّعت ٥ ولقيت ٤». **وللمدى الأعلى نفسُ الشي:** قصٌّ بحدَّين، **والحدُّ
  الأعلى مثبَّتٌ باسمه** (`indexOf('<Dialog open=')`) لا متروكًا لنهاية الملفّ.

  ⚠️ **وهي أختُ عائلةٍ تانية بنفس الشكل: «قدرةٌ مركزيّةٌ موجودةٌ ومستهلكٌ جديدٌ
  ما انربط فيها»** — `closeOperation` وصلت كلَّ عمليّةٍ إلا قائمةَ المستندات،
  و`REFERENCE_FORM_VIEWS` وصلت كلَّ شاشةٍ إلا `return_to_supplier`. **بالاتنين
  الآلةُ سليمةٌ ومستهلكٌ واحدٌ برّاها، ولا شي بيشتكي.**

- **⚠️ تمييزٌ كلّفنا جولةً لنرسيه ما بينترك بتعليق — بينحرس باختبار اسمه بيقول
  القاعدة.**

  التعليق **شرحٌ لمن بيقرأ**، والاختبار **حارسٌ لمن ما بيقرأ** — **واللي ما
  بيقرأ هو اللي رح يغيّره**.

  | | شو بيصير لما ينحذف |
  |---|---|
  | سلوك محروس | اختبار بيسقط · لا بيمرّ |
  | تعليق | **ولا إشي** |

  **وصار فعلًا: سطر بيفسّر ليش «الرصيد السالب مش بضاعة غير مقوَّمة» انحذف
  أثناء إعادة تنظيم صحيحة** (توحيد `hasKnownValue`)، **والحزمة ضلّت خضراء**.
  **وهاد أخطر أصناف الفقد لأن الـcommit بيبان تحسينًا خالصًا:** مراجعة الفرق
  بتدوّر على اللي انفسد، **وحذفُ معرفةٍ مش إفسادًا لشي** — الكود بعده أنظف
  وما في سطر بيشتكي.

  **والتمييز كان قابلًا للاختبار من الأصل:** سطران بيعرضوا `—` والعدّاد بيقول
  **١ لا ٢**. صار اختبارًا، **واللي «بيصحّحه» لـ٢ بعد شهر بيسقّط اختبارًا
  باسمه** بدل ما يمرّ. **واسم الاختبار بيوصل لمن يقرأ الفشل بلا ما يفتح ملفًا.**

  ⚠️ **والتعليق ما بينُنزع** — الاثنان بيشتغلوا مع بعض: التعليق بيشرح **ليش**،
  والاختبار بيمنع **إنه ينُنسى**. **اللي فشل مش وجودُه بل الاعتماد عليه وحده.**

- **⚠️ مستوى الدليل بيتحدّد بنمط العطل المحروس منه، لا بقاعدة ثابتة «لازم رقم».**

  تلات درجات، والتالتة هي اللي بتنفلت:

  | نمط العطل | شو بيحسمه | مثال مقيس |
  |---|---|---|
  | **رقمٌ بديلٌ معقول** | **رقم وحده** | ملخّص الجرد قال «٣ معدود من ٦» — و٢ كانت رح تبان طبيعية تمامًا |
  | **حضورٌ أو غياب** | **«طابق» بتكفي** | «كل المنتجات» بلا اختيار: قبل الإصلاح **جدول فاضي**، والبحث بيقول «ما في نتائج». الملاحظة بتقلب قيمة مش بتقرّب رقم |
  | **⚠️ ما بيميّزه الناظر أصلًا** | **قياس آليّ — وما في بديل** | «الكلّ» مقابل «الأكثر»: ولا إنسان بيميّزهن بقائمة طويلة |

  **والدرجة التالتة هي الوحيدة اللي ما بيقفلها تكرار الاختبار البشري مهما تكرّر**
  — فمش «الأفضل نقيسها آليًا» بل **«يستحيل غيره بنيويًا»**. صارت فعلًا: المالك
  قال صراحةً إنه ما انتبه للأرقام، **وهاد الجواب الصادق** — فانعدّت الصفوف عند
  الرسم بدل انتظار عدٍّ يدويّ بيصحّ مرّة وحدة.

- **⚠️ واختبار المكتبة مش اختبار الرسم — والفرق بينهن صنف أعطال كامل.**

  «`catalogueRows` **بترجّع** كل منتج» ادّعاء، و«الجدول **بيرسم** اللي انعطاله»
  ادّعاء تاني. **شريحة، أو حلقة متداخلة غلط، أو مجموعة أخيرة ضايعة — بتمرق من
  الأول وبتسقط بالتاني.** مقيس: حذف آخر مجموعة بيسقّط تلاتة من أربعة.

  **والعدّ لحاله أعمى عن حالة وحدة:** صفّ انرسم مرتين وصفّ سقط — **المجموع
  بيضلّ صح**. فبيلزم معه «كل اسم مرّة وحدة»، وهي بالضبط اللي بتنتجها حلقة
  متداخلة غلط.

- **الطفرة اللي ما بتقتل اختبارًا بتقول إن اختباراتك ناقصة، لا إن الكود سليم.**
  صار مرتين بنفس الجلسة: حارس دورة ما كان بينقتل لأنه غير قابل للوصول أصلًا،
  وترشيح شجرة قبل البناء مقابل بعده مرّ بكل الاختبارات لأن كلها حطّت الحالة
  المؤرشفة **بمستوى الجذر** — والفرق ما بيظهر إلا لما تكون متداخلة تحت جذر حيّ.
  لما طفرة تمرّ، دوّر على الحالة اللي الاختبارات ما وصلتها قبل ما تستنتج إن
  الكود متين.

## سير العمل

- **⚠️ قبل ما تجاوب سؤالًا، وقبل ما تسمّي مجهولًا بتقريرك — فتّش المستودع
  أولًا.** صارت **أربع مرات** الجواب مكتوبًا ومُختبَرًا عندنا وانعامل سؤالًا
  مفتوحًا، وكل مرّة كلّفت جولة:

  | السؤال | وين كان الجواب |
  |---|---|
  | السؤال | وين كان الجواب |
  |---|---|
  | إطار حركة الجرد | تعليق بـ`stockDocumentList.test.js` من جولات |
  | حدّ صفحة الفحص | مكتوب قاعدةً ثم انخُرق بعدها |
  | ليش `unit_cost NOT NULL` | اختبار مكتوب من commitين |
  | **نصّ دوال المخزون** | **استعلام التوليد برأس `db-functions.sql`** |
  | **هل `accounting_direction` مجالُه مفتوح؟** | **`DATABASE_DIAGRAM:526` · ومقيسًا `:447` · وADR-047 · و`serviceForm.test.js:126`** |
  | **شو بتعرض قائمةُ الاتّجاه بالشاشة؟** | **`ACCOUNTING_DIRECTIONS` مبنيّةٌ ومشتركةٌ ومحروسةٌ من زمان** |

  ⚠️ **والسادسة أكبرُ من كلّهن، لأنها ما كانت تفصيلةً بل خطَّ قلقٍ كامل:**
  صُرفت جولتان على «شو بتكتب القائمةُ المنسدلة؟» و«هل نسكّر المجال؟» —
  **والقائمةُ مبنيّةٌ أصلًا**: `ACCOUNTING_DIRECTIONS = ['common', ...BUSINESS_TYPES]`
  **مشتركةٌ بين المنتجات والخدمات بنفس المرجع** (`toBe` لا `toEqual`،
  `productForm.test.js:378`) **ومثبَّتةٌ حرفًا بحرف مقابل الـCHECK «مقروءًا راجعًا
  من القاعدة الحيّة»** (`serviceForm.test.js:125`). **و«مبيعات الصالون» ما اقترحها
  كودٌ قطّ — اقترحتها محاكاةُ تصميم.**

  > **فالسؤال «شو بتعرض الشاشة؟» جوابُه بالشاشة، لا بالكتالوج ولا بالوثيقة.**
  > **والقياسُ على القاعدة ما بيجاوب سؤالًا عن الواجهة**، وهاد اللي خلّى جولتين
  > بتدوّروا بالمكان الصحيح **للسؤال الغلط.**

  **والخلل بالترتيب لا بالملفات:** السؤال بيجي أولًا والبحث بعده. **فليسبقه.**

  ⚠️ **والخامسة أسوأهن، ولهيك مكتوبة بالخطّ العريض:** الجواب كان **بمكانين
  بالخريطة**، ومشروحًا **بـADR كامل**، **ومثبَّتًا باختبار بيقول عن نفسه إنه
  «مقروءٌ راجعًا من القاعدة الحيّة»**. وانبنى فوق الغياب المفترَض ملفُّ مسحٍ
  كامل، **وترويسةٌ بتقول إن هالحقل «ما كان معه إشي يرفض فيه» — وهو الحقل
  الوحيد بالقائمة اللي معه.**

  **والفرق عن الأربعة اللي قبلها إن هاي ما كانت معرفةً منسيّة، كانت معرفةً
  موثَّقةً بأربع نسخ.**

- **⚠️ السابقة المحسومة بتنعاد على شرط التطابق، لا على تشابه الشكل.**

  البند ٥١ حسم «المستند بينعكس مرّتين» بفهرس تفرّد على `reverses_document_id`
  — قرار مُثبَت، والغريزة الصح إنك تعيد استعماله. **فاقترحته حرفيًا للطلبيّة،
  وكان بيمنع الميزة نفسها اللي الطلبيّة موجودة لأجلها.**

  | | العكس | الطلبيّة |
  |---|---|---|
  | الشكل | «امنع تكرار حدث» | «امنع تكرار حدث» |
  | **الجوهر** | **حدث بيقع مرّة بطبيعته** | **قالب بينعاد استعماله بطبيعته** |

  **والمثال اللي بيحسم:** طلبيّة شهريّة ثابتة بتنكتب مرّة وبتنستدعى كل شهر —
  **أشيع استعمال لقالب، مش استثناءه.** والمالك أكّدها بجملة: «عادي استخدم نفس
  طلب بضاعة لتوريدين مختلفين».

  **فقبل ما تستعيد قرارًا: هل الشرطان متطابقان، ولا متشابهان بس؟** والشكل
  لحاله ما بيكشف الفرق.

- **⚠️ أي تسهيل بيسرّع فعلًا مشروعًا بيسرّع تكراره بالغلط — فتّش هل «الأمان»
  الحالي احتكاك متخفّي.**

  تسجيل نفس التوريد مرّتين بالخطأ كان محروسًا عمليًا بشي مش حارسًا: **إعادة
  كتابة اثنعش سطر.** والقالب بيخلّيها **نقرة**. فما انفتحت ثغرة — **انسحب
  احتكاك كان بيحمي بلا قصد.**

  **والقاعدة: قبل إزالة أي احتكاك، اسأل شو كان بيمنع عرضًا.** والحرّاس الحقيقيّة
  (الزرّ المعطَّل أثناء الإرسال، تفريغ الأسطر بعد النجاح، تنبيه رقم الفاتورة
  المكرّر) بتضلّ — بس اللي كان بيحمل نصف الحمل بصمت بيروح.

- **⚠️ غيابُ اسمٍ مش غيابَ مفهوم — والبحث بالاسم بيفشل مفتوحًا.**

  دوّرت على عمود `reversed_document_id` بـ`stock_documents` وما لقيته، فسجّلت
  إن المفهوم غائب وبنيت عليه تعليلًا. **والعمود موجود واسمه
  `reverses_document_id`** — بحرفين.

  **والبحث بالاسم بيفشل مفتوحًا:** بيرجّع «ولا إشي» وبيبان حاسمًا، فما بيدفعك
  تكمّل. **والبحث بالمفهوم بيفشل مغلقًا:** «عمود `uuid` بيشير لنفس الجدول»
  كان بيطلع بأي سرد أعمدة، **ومعه أي تهجئة**.

  والسلوك بالكود كان صحيحًا طول الوقت (السؤال بينُسأل للمجموعة)، **بس التعليل
  كان مقلوبًا**: بينُسأل للمجموعة **لأن هذا اتّجاه العمود الصحيح** (العكس
  بيشير لأصله لا الأصل لعاكسه)، لا لأن العمود غائب. **وتعليل غلط تحت سلوك صح
  بيصمد لحد ما حدا يبني عليه.**

  وهو أخو «ما انقاس ≠ ما موجود» بلبوس المخطّط: **قبل ما تسجّل غياب شي، دوّر
  عليه بشكله لا باسمه.**

- **⚠️ العطل المصلَّح لسّه نقطيًّا لحدّ ما تسأل: شو **صنفه**، ووين بيعيش كمان؟**

  مراجعة مسكت إن `line_discount_kind = 'percent'` جوّا `CHECK` **ما بترفض**
  الصفّ اللي نوعه `NULL` — لأن المقارنة بترجع UNKNOWN، والـ`CHECK` ما بيرفض
  إلا على FALSE. الإصلاح النقطي سطر واحد. **بس السؤال «شو صنفه؟» جاوب:
  «مساواةٌ على عمودٍ يقبل العدم، جوّا قيد»** — وهاد الشكل موجود بـ٠٤٤ كمان
  (`doc_type = 'reversal'`)، وما حدا كان سأل عنه.

  **فطلعوا تلات مخرجات من عطلٍ واحد**، والتالت هو اللي بيمنع التكرار:

  ```
  ١. الإصلاح           ⇐ الملفات التلاتة
  ٢. الموضع التاني      ⇐ ٠٤٤، ما كان مقصودًا بالمراجعة
  ٣. حارسٌ للصنف        ⇐ كل مساواة جوّا CHECK لازمها is not null أو coalesce
  ```

  ⚠️ **والفرق مقيس لا مذوَّق:** الحارس بيمسك **كل** حالة جاية من هالصنف، والإصلاح
  بيمسك الحالة الوحيدة اللي انشافت. **وقائمة استثناءاته بتفشل مغلقة** — الملف
  والعمود والسبب، فاللي بيجي جديد بيوقف حدا مرّة.

- **🔴 تعليقٌ بيصف حالةً مؤقّتة بيحمل شرطَ إعادةِ قراءته معه — لا جملةً بتصير
  كذبًا بصمت.**

  **«لسّه ما صار كذا» بينكتب صحيحًا وبيتقادم بلا ما يشتكي شي** — والأسوأ إنه
  **بيمنع الشغلَ اللي بيوصفه**: اللي بيقرأ «ما في حركات فما في عمود رصيد»
  **ما بيبني العمود**، لأن السطرَ بينقرأ قرارًا لا تاريخًا.

  **أربعةٌ انشالوا بيومٍ واحد:**

  ```
  ProductsBrowser      «ما في حركات، فعمودُ أصفارٍ بيقول لا شي باقٍ»
  useProductCatalog    «الشاشة ما إلها علاقة بالمستودعات لحدّ ما تصير حركات»
  storageForm          «الغرامة بتنحسب عند نقص… وهاد الكود مش موجود»   ← وهو موجود (٠٥٦ج)
  079a                 «٠٨١ رح يقيس… ولحدّ ما يشتغلوا هالسطرُ ادّعاء»    ← واشتغلوا
  ```

  ⚠️ **والتلاتة الأولى انلقيوا بالصدفة، والرابع بمسحةٍ عن الشكل نفسِه** — وهاد
  الفرق: **الصدفةُ بتلاقي واحدًا كلّ شهر، والمسحةُ بتعدّهن مرّة.**

  **⚠️ والتمييزُ اللي بيمنع المسحةَ تصير ضجيجًا:**

  | | بيتقادم؟ |
  |---|---|
  | «صالونٌ ما إله موظّفين **بعد**» · «ما في مستندات **بعد**» | ❌ **لأ** — بيوصف **حالةَ تشغيلٍ** بيعالجها الكود |
  | «هالكود **مش موجود**» · «الشاشة ما إلها علاقة **لحدّ ما**» | ✅ **آه** — بيوصف **طورَ مشروع** |

  **والصيغةُ اللي بتحمل شرطَها:** «لمّا تظهر أوّل حركة، اقرأ هالسطر من جديد»
  بدل «ما في حركات». ⚠️ **والأقوى منها إن الشرطَ يصير اختبارًا** — زي ما
  `raisedCodes.test.js` بيقرأ `docs/sql` فبيسقط لحظةَ ما يوصل الرمزُ الجديد،
  بدل ما يستنّى حدا يقرأ تعليقًا.

- **🔴 «ما في رسالة» بيعني «الحالةُ ما إلها صنف» — لا «ما في حالة».**

  «إدخال من فاتورة» بشاشة الإرجاع بدت ساكتة. **والسببُ إنو `fillOutcome` كانت
  بتسأل «هل انملى شي؟» وبترجع `null` لو انملى** — فالملءُ **الناقص** بيصمت
  تمامًا، والتعليقُ المبرِّر («شرحٌ فوق نتيجةٍ ظاهرةٍ ضجيج») **صحيحٌ عن ملءٍ
  تامّ وخاطئٌ عن ناقص: الناقصُ بيبان تامًّا حرفيًّا.**

  ```
  الفاتورة كلها مستنفَدة   ⟵ بيحكي — بس سردَ أرقامٍ تشخيصيّ لا جملةً تنقرا
  جزء منها مستنفَد         ⟵ 🔴 صمتٌ كامل
  ```

  **⇒ والإصلاحُ صنفٌ مسمًّى لا رقمٌ إضافيّ:** `fillReport` بترجع
  `{kind}` من `FILL_KINDS`، وكلُّ صنفٍ إلُه جملتُه — **وحارسٌ بيشتقّ القائمةَ
  فبيسقط يومَ ينضاف صنفٌ بلا جملة**، بدل ما ينعرض مفتاحٌ خامٌّ للمستخدم.

  ⚠️ **والعدُّ بالمنتجات لا بالدفعات، وهاد مش تفصيلًا:** «امتلأ ٢ من ٥ منتجات»
  جملةٌ بيفهمها صاحبُ الصالون، **و«٧ دفعات مستنفَدة» لأ** — وتلاتُ دفعاتٍ لمنتجٍ
  واحدٍ بتخلّي عدَّ الدفعات يبالغ تلاتَ أضعاف.

  🔴 **والصنفان بينفصلوا عمدًا: «مستنفَد» و«مخفيٌّ بمرشِّح».** الأوّلُ ما بينحلّ
  إطلاقًا والتاني بينحلّ بإزالة تأشير — **فجملةٌ وحدة إلهن بتبعت القارئَ
  للطريق الغلط.**

  **والثقبُ كان بالشطب كمان** (نفس الدالّة)، وانصلح فيه بنفس الجولة: **«العطلُ
  المصلَّحُ بيضلّ نقطيًّا لحدّ ما تسأل وين بيعيش كمان».**

- **🔴 «المسارُ بيردّ 200» مش دليلًا على أيّ مكوّنٍ بينرسم — والشاشةُ القديمة بتردّ 200 كمان.**

  انبنت `ReturnToSupplierScreen` ووُصل فرعُها، **وقدّمتُ `?op=return_to_supplier ⟵ 200` دليلَ تشغيل.** والمالكُ فتحها فطلعت الشاشةُ **القديمة**: الاسمُ ما دخل `REFERENCE_FORM_VIEWS`، فبقيت `usesSharedDocumentScreen` صادقةً، **وانرسمت شاشتان والقديمةُ فوق الجديدة.**

  ⚠️ **والصنفُ أعمُّ من التوجيه: فحصٌ بيعطي نفسَ الجواب بالحالتين — المكسورةِ والمصلَّحة — مش فحصًا.** و«٢٠٠» بتقيس إن الصفحةَ ما رمت، **لا إنّ اللي فيها هو اللي بنيناه.**

  **⚠️ و`curl` ما بيقدر يقيسها هون أصلًا، وهاد مقيس:** الصفحةُ وراء `AuthGate` فالمخدومُ بوّابةُ دخول، **و`serverSideTranslations` بتحطّ كلَّ `products.json` جوّا `__NEXT_DATA__`** — فأيُّ نصٍّ عربيٍّ بتلاقيه بالـHTML موجودٌ سواءٌ انرسمت الشاشةُ أو لأ. **فالبحثُ عن جملةٍ بالمخرَج بيفشل مفتوحًا مرّتين.**

  ⇒ **اللي بينقاس من هون: منطقُ التوجيه بحارسٍ يُحقن ويُقاس إنه بيعضّ.** واللي **ما بينقاس**: أيُّ مكوّنٍ ظهر فعلًا — **وهاد بيضلّ عند بشار، وبينقال إنه عنده لا إنه «مثبَّت».**

- **🔴 حارسٌ بيقول عن نفسه إنه «مشتقّ» وهو بيقرا قائمةً يدويّة — أخطرُ من غيابه.**

  `screenPropsWired` انبنى ليمسك «خاصّيّةٌ ما انمرّرت»، **وترويستُه بتقول
  حرفيًّا: «أسماءُ الخصائص تُشتقّ… وقائمةُ الشاشات كذلك — فشاشةٌ رابعةٌ تُبنى
  غدًا تدخل الفحصَ يومَ تُوصَل».** والأسماءُ كانت مشتقّةً فعلًا، **والقائمةُ
  كانت سطرًا مكتوبًا بيد** مثبَّتًا على ثلاث.

  ⇒ **فوُصلت الشاشةُ الرابعة، ومرّ الحارسُ أخضرَ وهو لا يراها.**

  ```
  المكتوب   const SCREENS = ['Order…', 'Supply…', 'WriteOff…']   ← وexpect(length).toBe(3)
  المقيس    الصفحة بتركّب ستّ شاشات
  الإصلاح   SCREENS مشتقّةٌ من `<XScreen` بالصفحة · حدٌّ أدنى بدل عددٍ مثبَّت · واتنين مثبَّتين بالاسم
  ```

  ⚠️ **والتثبيتُ على عددٍ كان جزءًا من العطل لا حارسًا:** `toBe(3)` بيفشل لمّا
  **القائمةُ** تكبر، **وما بيفشل لمّا الشجرةُ تكبر** — فبيدفع لتحديث الرقم بدل
  النظر. **والحدُّ الأدنى بيمسك «المشيةُ ما لقيت شي» بلا ما يغري بالتزييف.**

  🔴 **والصنفُ أوسعُ من هالملفّ:** «تعليقٌ بيصف نيّةً كأنها واقع» — وهو أخو
  «ترويسة كانت صحيحة يوم انكتبت»، **بس هاد أسوأ لأنه بينقرا ضمانةً فبينبني
  فوقه.** ⇒ **أي حارسٍ بيدّعي الاشتقاق، اقرا سطرَ الاشتقاق نفسَه.**

- **🔴 كلُّ رقمٍ بينقتبس بحبّته ملتصقةً فيه — «٢٨٦٥٠ بتجريبي» لا «٢٨٦٥٠».**

  **الحبّةُ جزءٌ من الرقم لا سياقٌ حواليه**، لأن رقمًا منقولًا بلا حبّة
  **بينقرأ بالحبّة اللي القارئُ ماسكها.**

  **صارت مرّتين بجولتين، وحدةٌ من كلّ طرف، ونفسُ الشكل:**

  ```
  حدُّ النفاد    حدٌّ حبّتُه المنتج، انقارن برصيدِ مستودعٍ واحد
  الـ٢٨٦٥٠      رصيدُ مستودعٍ واحد، انقرأ رصيدَ منتج
  ```

  ⚠️ **والتانية بدت «عشرُ وحداتٍ ظهرت بلا مستند»** — وما ظهرت ولا وحدة.
  `067_1` مجمَّعٌ بحبّة (مستودع، منتج) فبيعطي صفَّين (تجريبي ٢٨٦٥٠ · عامّ ١٠)،
  و٠٧٩ب_٣ بيجمع عبر المستودعات عمدًا فبيعطي ٢٨٦٦٠، **و`movements_ever = 8`
  بنفس الصفّ = ٣ + ٥.** كلُّ رقمٍ بيقفل.

  > **⚠️ والسجلّاتُ الأصليّة كانت صحيحة:** ترويسةُ ٠٧٠ بتقول «holds 28650 **in
  > the test storage**»، و`PROJECT_HANDOFF:619` بيسمّي المستودع. **الحبّةُ
  > ضاعت بإعادة الرواية** — وهاد وين بتضيع دايمًا.

  ✅ **والتصالحُ كلُّه انعمل من مخرجاتٍ بإيدينا، بلا تشغيلِ شي.**

- **🔴 صفوفُ هالقاعدة اختباريّة — فبتجاوب أسئلةً بنيويّة، وما بتجاوب سؤالَ
  تواتُرٍ أبدًا.**

  **بتجاوب:** هل الطريقُ موجود · هل الحارسُ بيرفض · هل القيدُ سارٍ · شو بيصير
  لهالصفّ لمّا تنطبّق القاعدة. **وما بتجاوب:** كم مرّة بيصير هاد · هل هي حالةٌ
  شائعة · هل الميزةُ تستاهل.

  **وقعنا فيها مرّتين:**

  | | الرقم | اللي انقرأ | والحقيقة |
  |---|---|---|---|
  | ٢٨٦٥٠ | رصيدٌ ضخم | «صنفٌ إله حركةٌ كثيفة» | كبسةٌ بجردَين متتاليَين |
  | ٠٨٣ | ٥ من ٨ بلا سعر | «الحالةُ شائعةٌ اليوم» | صفوفٌ انتعبّت لتجريب النظام |

  ⚠️ **والتانية أخطر لأنها بدت قياسًا نظيفًا** — شاهدٌ داخليّ، وأسماءٌ، ومجموعٌ
  بيقفل. **والعطلُ مش بالاستعلام، بالجملة اللي انبنت فوقه.**

  > **فأيُّ عدٍّ فوق هالصفوف بينقرأ واقعةَ استعمالٍ هو نفسُ البدالة اللي
  > بنلاحقها، بلبوس رقم.** والبيّنةُ الوحيدة الممكنة عن التواتُر **منتجاتٌ
  > حقيقيّةٌ بصالونٍ شغّال**، ولحدّ ما توجد **يُسجَّل السؤالُ مؤجَّلًا لا
  > محسومًا.**

  ✅ **والصياغةُ الصامدة بتبقى بنيويّة:** «الحالةُ الرخوة انولدت خمس مرّاتٍ
  **بلا قصد**، من واحدٍ عم يعبّي حقولًا» — **فالطريقُ ما بيلزمه نيّة، وما في
  طبقةٌ بتوقفه.** وهاد بيبرّر الحارس **بلا ما يدّعي تواتُرًا.**

- **⚠️ لما المستودع ما بيقدر يجاوب سؤالًا عن القاعدة — ما تستنتج، علّق القياس
  على سكربتٍ ماشي أصلًا.**

  «هل `doc_type` بيقبل العدم؟» ما بينحسم من المستودع: الخريطة بتسرد العمود بلا
  قابليّته للعدم، **و«الوثائق بتوجّهك والقاعدة بتقرّر»**. وما عندي وصول.

  **والحلّ مش تخمينًا ولا سكربتًا جديدًا:** عمود واحد بـ٠٥٢ج، وهو سكربت تحقّق
  **رح ينشغّل بأي حال**. كلفته صفر، وبيحوّل افتراضًا لحقيقة بنفس الجولة.

  **والقاعدة العامّة: أرخص قياسٍ هو اللي بيركب مع شغلٍ ماشي.** والبديل إنك
  تكتب افتراضًا بتعليق، وبعد شهور بيقراه حدا كأنّه مقيس — وهاد نفس صنف
  «ترويسة كانت صحيحة يوم انكتبت».

- **اللي ما بينوصل بالسَّوق بينقرا بالنصّ — والنصّ متاح دايمًا وأرخص.**

  صرفنا جولات منقود المتصفّح على رموز رفض بموديول المخزون، والقائمة وقفت عند
  أربعة. **والسبب مش اجتهادًا ناقصًا، السبب بنيوي:** RLS بتخفي كل مستودع
  حقيقي، فأول بحث بيجاوب `storage_not_found` **وكل شي وراه غير قابل للوصول
  من برّا مهما نادَيت**. `pg_get_functiondef` قرأهن كلهن بسطر واحد.

  **والأثقل إن اتنين منهن — `stock_line_zero` و`unit_cost_required` — كانوا
  بيوصلوا الشاشة جملةً عامة**: حارسان قائمان بالقاعدة وصامتان للمستخدم.

  فقبل ما تسوق متصفّحًا لتكتشف سلوك القاعدة، اسأل: **في نصّ يُقرأ؟** والفرق
  بين «ما انقاس» و«ما موجود» بيضلّ قايمًا — بس القراءة بتحسمه أرخص من السَّوق
  بمراتب.

  **وهو تقسيم العمل الصحيح:** المراجع بيشوف اللي ما بشوفه (اللقطة · صفوف
  المالك · التناقض بين الموصوف والظاهر)، **وأنا بشوف اللي ما بيشوفه: اللي
  مكتوب فعلًا** — بيقرأ الملفات كلها بأمر واحد وهو لأ.

  ⚠️ **وقاعدة مرافقة انكشفت بنفس البحث: غياب الشي من كودنا ما بيثبت غيابه من
  القاعدة.** `RAISED_CODES` **مقيسة لا مشتقّة** — بتحتوي اللي انشاف بينطلق
  فعلًا، والترويسة بتقولها صراحةً. فـ«ما لقيت رمزًا لكذا» بتعني **«ما انقاس»**
  لا «ما موجود».

- **🔴 المحتوى ما بيمرّ بصدفة — والكتابة ما بتنقال «تمّت» إلا بقراءةٍ راجعة.**

  صفٌّ للسجلّ انكتب بـ`echo` جوّا نصٍّ بعلامتَي تنصيص مزدوجتين، **فالصدفة نفّذت
  اللي بين العلامات الخلفيّة** وفرّغت ستّة صفوف من أسماء أعمدتها. **والمقيس أسوأ
  من الموصوف:**

  ```
  $ echo "| uses `true` and `test` and `printf` |"
    printf: usage: printf [-v var] format [arguments]      ← stderr، لـ printf وحدها
  | uses  and  and  |
  EXIT: 0
  ```

  | الكاشفُ المغري | ليش ما بينفع — **مقيسًا** |
  |---|---|
  | «رمزُ الخروج» | **`EXIT: 0`** — الصدفة بتعتبرها نجاحًا |
  | «توازنُ العلامات الخلفيّة بالسطر» | 🔴 **بتنشال جوزًا** — `\| uses  and  and  \|` ماركداونٌ سليمٌ ومتوازن |
  | «في ضجيج على stderr» | **`true` و`test` ما طلّعوا حرفًا.** والضجيجُ بيطلع بس لمّا الاسمُ يصادف أمرًا بيشتكي |

  ⚠️ **وأخطرُها إن الاسمَ يصادف أمرًا بينجح**: `` `date` `` بتحطّ تاريخًا مكان
  الاسم — **قيمةٌ معقولةُ الشكل بمحلّ الصح.** ووثائقُنا مليانة `select` و`test`
  و`join` و`sort` و`read` و`time`.

  **⇒ قاعدتان، وحدة بتمنع وواحدة بتكشف:**

  ```
  المنع    محتوى فيه ` أو $ أو \ أو " أو سطر جديد ⇐ بأداة الكتابة، لا بوسيطٍ لسطر أمر
           (أداةُ الكتابة ما فيها صدفة أصلًا — فالسؤال بينحلّ لا بينحَل)
  الكشف    ما بينقال «انكتب» إلا بقراءةٍ راجعة من الملف — لا بنجاح الكاتب
  ```

  ⚠️ **وهاد ما بيصير حارسَ Jest، والفرق بنيويّ لازم ينقال:** التلاتة التانية
  اللي انمسكوا بنفس اليوم **خصائصُ لكودنا** — الاختبار بيقرأ الشجرة وبيسأل.
  **وهاد خاصّيّةٌ لطريقِ البايت من راسي للملفّ، وما بيضلّ منه أثرٌ بالمستودع
  ينقرأ.** فمحلُّه قاعدةُ سلوك، **ومحاولةُ تلبيسه حارسًا بتعطي طمأنينةً كاذبة**
  — زي فحص التوازن فوق، اللي مقيسٌ إنه **ما بيعضّ**.

  **🔴 والصنفُ المشترك مش «محتوى فيه رمز خاص» — أضيق من اللزوم.** اتنين من
  أربعة بس بيعتمدوا على المحتوى (`Number(null)` وهاد)، والتانيين مكسورين من
  أوّل لحظة. **اللي بيجمع الأربعة إن الخللَ بيطلّع قيمةً صالحةَ الشكل بدل ما
  يطلّع خطأ:**

  | العطل | الناتج | وليش بيمرّ |
  |---|---|---|
  | `Number(null)` | `0` | **رقمٌ مشروع** |
  | خاصّيّةٌ ما انمرّرت | `undefined` ⇐ «لا بيانات» | **حالةُ فراغٍ مشروعة** |
  | عمودٌ ناقصٌ بالـ`select` | `undefined` ⇐ ما طابق شي | **نتيجةٌ فارغةٌ مشروعة** |
  | علاماتٌ خلفيّة بصدفة | صفٌّ بلا اسم | **ماركداونٌ سليم · `EXIT: 0`** |

  **والوجهُ التاني للأربعة: بكلّهن الكاتبُ أبلغ عن نجاحِ نفسِه** — `EXIT: 0`،
  و`replace()` طبعت «wired» وهي ما طابقت شي، والرسمُ ما رمى، والـ`select` ما
  اشتكى. ⚠️ **فالقراءةُ الراجعةُ مش تشدّدًا، هي الكاشفُ الوحيدُ اللي ما بيشارك
  الكاتبَ افتراضَه.** وهي عينُ قاعدةِ البيّنة المضادّة («الحارس بينقاس إنه
  بيعضّ») مطبَّقةً على الكتابة بدل الفحص.

- فرع واحد لكل تغيير، ودمج بعد تأكيد المالك للاختبار بالمتصفح.

  🔴 **ولا يُجمع في فرعٍ واحدٍ عملان يختلف ما يحبسهما.** **الفرعُ يُقسَّم بحسب
  ما ينتظره لا بحسب ما يشبهه** — **فأبطأُ حابسٍ يصير حابسَ الجميع، ويضيع ما
  كان جاهزًا.** ⚠️ **وشاهدُها (٢٥ آب ٢٠٢٦): خمسُ قواعدَ توثيقٍ لا تبلغ شاشةً
  كانت ستُحبس بزيارةِ متصفّحٍ لبندَين لا تخصّانها.** ✅ **وثمنُها مراجعةٌ
  ثانية، وهو أقلُّ من ضياع الحصاد — والزيارةُ تبقى واحدةً في الحالين.**

  ⚠️ **ونطاقُه يُكتب لأنّ غيابَه صار يُقرأ إلغاءً:** التأكيدُ بالمتصفّح **لكلّ
  تغييرٍ يبدّل ما يراه المستخدمُ على شاشةٍ حيّة، أو ما يستطيع فعلَه بها، أو ما
  يُكتب في القاعدة حين يفعله** — **رسمًا أو نصًّا أو سلوكَ ضغطةٍ أو أثرًا
  صامتًا.** **والتغييرُ الذي لا يبلغ شاشةً ولا تكتبه شاشةٌ لا متصفّحَ له —
  ويُقال ذلك في الطلب صراحةً بدل أن يُسكت عنه**، فالفرقُ بين «لا ينطبق»
  و«أُهمل» إن لم يُكتب صار غيابُ التأكيد عادة.

  🔴 **والصنفُ الثالثُ أُضيف لأنّ النطاقَ بدونه يُخرج عطلًا شرحُه في هذا الملفّ
  نفسِه:** `saveCount` تكتب `upsert` بستّة حقولٍ بلا `counted_at` ⇒ **الصفُّ
  يقول «٧٥» بختمِ لحظةِ «٣»** — **والرسمُ كما هو، والنصُّ كما هو، والضغطةُ
  تنجح وتُحفظ.** ⇒ **فبالصنفَين الأوّلَين وحدَهما: لا تأكيد.**
  ⚠️ **وكلفتُه تُقال ولا تُخفى:** الأثرُ الصامتُ **لا يُرى في المتصفّح** ⇒
  **فتأكيدُه فعلٌ من الشاشة ثمّ نظرةٌ إلى الصفّ** — خطوتان لا واحدة، **وذلك
  ثمنُ ألّا يسقط الصنفُ كلُّه من النطاق.**

  🔴 **وكان النطاقُ يُعدّ ثلاثةَ مجلّدات** (`lib` · `components` · `pages`)
  **فورث حدَّ حادثته: سطرٌ في `public/locales` يُرسم على شاشةٍ حيّةٍ ولا يقع في
  أيٍّ منها** — مقيسًا (٢٤ آب ٢٠٢٦): `coverage.colMatched` يُرسم في
  [components/StocktakeCoverage.js](components/StocktakeCoverage.js):94.
  **وكذلك `next.config.js` و`next-i18next.config.js`.** ⚠️ **و«ستُّ الجولات»
  تُنسب بدقّة: سطرُ `reloadOnPrerender` في الثاني هو ما أنهاها، والذي كلّفها
  غيابُه** — والحادثةُ بنصّها أعلى هذا الملفّ («صارت ستّ مرات عبر تلات
  صياغات… والسادسة وصلت شاشة المالك لا شاشتي»). ⇒ **فيُسمّى ما يحميه لا ما
  يعُدُّه، وهو الحادثةُ ⑧ مطبَّقةً على سطرٍ كتبناه نحن** (شاهدٌ رابع).

  🔴 **ولا تُعفي قاعدةٌ إيداعَها الأوّلَ إلّا بشرطَين معًا:** أن تكون الواقعةُ
  الحاسمةُ **موضوعيّةً ومطبوعةً في وصف الإيداع**، **ومستقلّةً عن صياغة القاعدة
  نفسِها.** وإلّا فالتأكيدُ يُطلب ولو بدا شكليًّا — **كلفةُ تأكيدٍ زائدٍ يومٌ،
  وكلفةُ قاعدةٍ تُعفي نفسَها هي القاعدةُ كلُّها.**

  🔴 **ولا تُغني عنه الحزمةُ أدناه:** تلك تجيب «هل حالةُ المستودع هي ما نظنّ؟»،
  **وهذا يجيب «هل يعمل عند من يستعمله؟»** — **وحزمةٌ خضراءُ كاملةٌ على شاشةٍ لا
  تفتح تبقى خضراءَ كاملة.**

  🔴 **وكلُّ خطوةٍ غيرِ قابلةٍ للإرجاع في وصفةِ مالكٍ تُسبق برقمٍ معروضٍ على
  الشاشة يُقارَن بعددٍ نكتبه في الوصفة قبل التنفيذ** — **والتوقّفُ، لا
  المتابعة، هو التصرّفُ عند عدم المطابقة.**

  ⚠️ **والفرقُ بين «هل أنت متأكّد؟» وبين رقمٍ يُقارَن: الأولى تسأل عن العزم،
  والثاني يسأل عن الهُويّة** — **«أهذا الشيءُ الذي أقصده؟» لا «أأنا جادّ؟».**

  ✅ **وشاهدُها (٢٦ آب ٢٠٢٦):** لوحُ «ألغِ الجرد» عرض «المنتجات المعدودة: **٢**»
  والوصفةُ كانت تقول **٢** ⇒ **فالمحذوفُ جلستُنا وحدَها، لا جلسةٌ ورثناها** —
  **حذفٌ على قاعدة الإنتاج بعد مطابقةٍ عدديّة، لا بعد ظنِّ ملكيّة.**

  🔴 **وما جعله ممكنًا قرارُ عرضٍ سابق: الرقمُ في نصّ اللوح لا في الزرّ**
  (`StocktakingSheet.js:232`) — **ولو كان في الزرّ لما صلح للمقارنة قبل الضغط.**
  ⇒ **فقرارُ «أين يوضع الرقم» صار حارسًا للمالك، وهو أوسعُ ممّا كُتب له.**

- خطوات صغيرة متدرّجة، والشرح بالعربي البسيط.

## 🔴 قبل الدمج وبعده — حزمتان تُجمعان بلا أن تُطلبا

🔴 **وقبلهما جميعًا: إذنُ الدمج يصدر عن مصدرٍ واحد، بصيغةٍ واحدةٍ ثابتة، ولا
يُشترط بشرطٍ يتحقّق عند غيره.** ⇒ **وموضعُه هنا لا في القسم السابق: هو يحكم
لحظةَ إطلاق الحزمتين، لا بندٌ فيهما** — **ومن يجمعهما يفتح هذا القسمَ ولا يمرّ
بما قبله.**

⚠️ **وإشارةٌ بمصدرين إشارةٌ بلا مصدر. وقعت (٢٦ آب ٢٠٢٦):** قيل للمالك «إن
ردّوا بكلمة ادمج ⟶ اضغط Merge» ⇒ **فصارت تصدر من اثنين — شرطًا من المراجع
وإطلاقًا من الفريق** — **ولا يستطيع المالكُ أن يميّز الإذنَ من نقلِ الإذن، ولا
أن يعرف أيَّهما وصله.** ⇒ **دُمج قبل أن يصل قياسٌ كان شرطًا معلَّقًا**
(`ls public/locales/`).

✅ **والقاعدةُ العامّة: شرطٌ معلَّقٌ على فعلِ طرفٍ ثالثٍ ليس حارسًا، بل تأجيلٌ
للحظةِ الالتباس.** ⇒ **وما احتاج قياسًا عندنا يُقاس قبل أن يُرسل الإذنُ لا
بعده.**

---

**الفاعلُ في الحزمتين أنا لا المالك**، وهي قاعدةُ «مين بينفّذ الفعل؟» — ولهذا
موضعُهما هنا لا في `PROJECT_HANDOFF`.

**والمراجعُ لا يملك وصولًا إلى GitHub ولا إلى القاعدة ولا إلى الطرفيّة — لا شيءَ
إطلاقًا.** فكلُّ ما يُرسَل هو البيّنةُ الوحيدةُ الموجودة، **ولذلك تُرسل خامًّا لا
ملخَّصةً: رقمًا يُقابَل، لا حكمًا يُصدَّق.** والمالكُ يحمل الكتلَ بين المحادثتين
ويضغط الأزرار، **ولا يُطلب منه أن يقرأ الأرقام.**

### حزمةُ ما قبل الدمج — على رأس الطلب بعينه، لا على الفرع

**① CI أخضرُ على sha الرأس نفسِه** — `/commits/<sha>/check-runs`، ويُقرأ
`incomplete = 0` **وكلُّ فحصٍ يحمل `head_sha` بنفسه** لا وراثةً عن الفرع.
⚠️ **ومعه `mergeable_state`، لا بديلًا عنه:** check-runs والـStatuses سطحان
**متكاملان لا يحوي أحدُهما الآخر** (النشرُ في Statuses وحدَه)، و`mergeable_state`
الحقلُ الوحيدُ الذي يطويهما. **و`unstable` إشارةُ «اسأل السطحَ الآخر» لا فشل.**

🔴 **وأربعةُ أسطرٍ خرجت من فرعٍ واحدٍ (٢٥ آب ٢٠٢٦)، وكلُّها أوّلُ تشغيلٍ للبند
في حالته:**

**(أ) الحزمةُ ذاتُ مرحلتَين بطبعها.** `mergeable_state` **لا يوجد إلّا على
طلبٍ مفتوح**، والطلبَ يفتحه المالك. ⇒ **ما قبل الطلب: ② ③ ④ ⑤ · وما بعده:
①.** ⚠️ **ويُعلَن «① نصفُه» صراحةً بدل تقديم الأخضرَين حزمةً.**

**(ب) و`incomplete` انتظارٌ لا حكم.** «الفحصُ لم يفشل» ليست «الفحصُ نجح»،
**وبينهما فقط أنّ أحدَهما لم يقع بعد.** ⇒ **يُعاد القراءةُ حتى يستقرّ، ولا
يُبلَّغ لونٌ لم يُرَ.** ✅ **وأختُها القاعدةُ (ح) في «مصدرُ كلِّ دعوى»** —
**كلتاهما: مخرَجٌ قُرئ بما نتوقّعه لا بما فيه.**

**(ج) و`total_count` يتحرّك في الاتّجاهين بحسب السياق** — مقيسًا في يومٍ واحد:
**٢ عند الدفع ⟶ ٣ بفتح الطلب** (تشغيلٌ ثانٍ لنفس الفحص على نفس الـsha) **⟶ ١
على إيداع الدمج.** ⇒ **الحكمُ `incomplete = 0` وخضرةُ الكلّ، لا ثباتُ العدد**
— **ورقمٌ يقفز بلا تفسيرٍ يُقرأ عطلًا في القراءة التالية** (وهو
`internal_triggers` بعينه: `2` مقابل `24`، والاثنان صحيحان).

🔴 **(د) والسطحان يختلفان في العضويّة لا في العرض فقط.** سقط
`Vercel Preview Comments` من `main` تمامًا بعد الدمج — **معقولٌ (لا معاينةَ
لطلبٍ مغلق)، لكنّه اختفاءٌ لا نقصان.** ⚠️ **وفحصٌ يختفي لا يترك أثرًا في
`incomplete`: الغائبُ لا يكون أحمرَ ولا أخضر** ⇒ **فلو اختفى فحصٌ لازمٌ يومًا
لَقُرئت الحزمةُ خضراءَ كاملةً وهي ناقصة.** **وهذه حجّةُ قراءةِ السطحين، أمتنُ
من «النشرُ يسكن في Statuses وحدَه».**

**② الفرقُ الحرفيُّ كاملًا** — ومع `--numstat` ليقفل الحساب بيد المراجع.
⚠️ **يُنقل ولا يُهيَّأ للقراءة:** فرقٌ بلا بادئات `--` لا يُعاد تطبيقُه.
**والمطابقةُ بـ`git hash-object` مقابلَ `git rev-parse HEAD:<path>`** — لا
بمقارنة سطور، فأداةُ المقارنة تُدخل ترميزَها.
🔴 **وحدُّها يُقال: تقفل السلسلةَ حين يكون الملفُّ صغيرًا بما يُنقل كاملًا.**
هذا الملفُّ ألفان ومئتان ونيّف — **فبصمتُه لا يقدر أحدٌ على مقابلتها مهما
أُرسل**، لأنها للملفّ كلِّه لا للقسم. ⇒ **فالسلسلةُ تُقفل على الجزء المراجَع
لا على الملفّ، ويُقال ذلك بدل أن تُقتبس بصمةٌ لم يقابلها أحد.**

🔴 **ومرحلتان لا واحدة: فرقٌ لكلّ إيداعٍ أثناءَ العمل، ثمّ الفرقُ التراكميُّ
الكاملُ عند اكتمال الفرع — ملفًّا، ببصمته، بلا أن يُطلب.**

⚠️ **والعلّةُ ليست الشكَّ في الأجزاء:** **اتّحادُ أجزاءٍ قُرئت في جولاتٍ
متفرّقةٍ ليس هو النصَّ النهائيَّ إلّا ببناءٍ ذهنيٍّ عند المراجع** — **وذاك
عملٌ يُطلب منه، لا قياسٌ يُسلَّم إليه.**

**وشاهدُها الحاسم (٢٦ آب ٢٠٢٦):** سجلُّ المراجع قال **خمسةَ إيداعات** والفرعُ
ستّةٌ، **والرأسُ نفسُه لم يتحرّك** ⇒ **فالخللُ في الدفتر لا في الشجرة، ولم
يستطع أحدٌ حسمَه** — **لا هو (لا يملك المستودع) ولا نحن (لا نملك دفترَه).**
✅ **والفرقُ التراكميُّ أسقط المسألةَ من الطريق بدل أن يحلَّها: نصٌّ واحدٌ
يُقرأ مرّةً، وبصمةٌ تربطه بالرأس ربطًا حسابيًّا لا ذهنيًّا.**

⚠️ **ويُرسَل ملفًّا لا كتلةً في المحادثة** — **فالكتلةُ تمرّ بمن يكتبها، وذلك
عينُ ما يمنعه البند (ي)**، والملفُّ نقلٌ آليٌّ لا يحتاج مقابلةً (ز).

**③ النَّسَب** — `git merge-base` = `origin/main` الحاليّة، **و`--is-ancestor`**،
**ودفعةٌ أولى `[new branch]` تنفي `force` بنيويًّا.**

⚠️ **وذلك نصُّ الدفعة الأولى وحدَها، فيرث حدَّ حادثته.** **وللدفعة الثانية
فصاعدًا أربعُ حجَجٍ لا اثنتان:**

```
① الأبُ المباشر    git rev-parse <new>^   ==   <sha المراجَع>
② مخرَجُ الدفع     <old>..<new>   ⟵ نقطتان، **بلا `+` وبلا (forced update)**
③ أبٌ واحدٌ لا اثنان  git rev-list --parents -n1 <new>
                    ⇒ ينفي الدمجَ كما ينفي الأمر
④ والمراجَعُ قائمٌ  git branch -r --contains <sha المراجَع>  ⟶ الفرعُ يسمّيه
```

🔴 **و④ أقواها: لا يكفي أنّ الفرقَ حُسب ضدّ المراجَع، بل أن يكون مطبوعًا على
الخادم لم يُمحَ.** ⚠️ **وتُقفل السلسلةُ على إيداعات الفرع كلِّها لا على
الأخير** — `git rev-list --parents origin/main..HEAD`.

**④ عددُ الاختبارات بالتشغيل على شجرةٍ نظيفة** — `git status --porcelain` فارغةً
أوّلًا، **ولا يُشتقّ من فرقٍ ولا يُنقل من جولة.**

**⑤ زرُّ حفظ الجرد لا يزال `<span>` بلا معالِج** — يُقاس حيًّا كلَّ مرّة
(`data-save-disabled` · صفرُ `on[A-Z]…=` · صفرُ `rpc(`)، **لا يُذكر من ذاكرة.**
🔴 **ما دامت البوّابةُ قائمة.** ⚠️ **ويومَ ترتفع يُحذف هذا البندُ من الحزمة ولا
يُستوفى** — فالزرُّ حينها **يجب** أن يصير زرًّا حقيقيًّا، **وبقاءُ الوسم بعدها
عطلٌ لا شرط.**

### وحزمةُ ما بعد الدمج

⚠️ **كانت «الثلاثيّة» ثمّ «الرباعيّة» ثمّ خمسةً — ثلاثةُ أسماءٍ في يومين**،
**وكلُّ واحدٍ أبات الذي قبله.** ⇒ **فسقط العددُ من الاسم، والبنودُ مرقَّمةٌ
بنفسها فهي المرجع.** **والتمييزُ عن أختها بالظرف لا بالعدد.**

**① إيداعُ الدمج** — 🔴 **`merge_commit_sha` لا يُقتبس إلّا مع `merged_at`:**
على الطلب المفتوح يملؤه GitHub بمِزجةِ اختبارٍ يحسبها لـ`mergeable`، **ولا تصير
هي إيّاه عند الدمج.**

✅ **والأزواجُ المقيسةُ كلُّها بصفر تطابق ⇒ سلوكٌ موصوفٌ لا تحوُّط، وتُسجَّل
معًا لا الأخيرُ وحدَه** — **فزوجٌ واحدٌ يُقرأ صدفةً، والقائمةُ تُقرأ قاعدة:**

```
                 قبل الدمج (merged_at = null)   ⟶   بعده (merged_at حاضر)
طلبٌ سابق        b625587                        ⟶   708b894
PR #35           eb9ae1e1981de53fb434e55236c…   ⟶   308984d55c6ffd40af9c7726d1…
PR #36           1d8e68cbe871472e4c2f2e79e6b…   ⟶   e0f86ab43451d1a1febb97945a…
PR #37           4ab93324…                      ⟶   a44e8ca…
PR #38           23103579…                      ⟶   526dcb7…
PR #39           1c334da28aefe7eb8b8a6e7a403…   ⟶   0d140f10222f0c0acfcd7f59b1…
```

🔴 **ولا عددَ في السطر فوقها، وذاك تصحيحٌ لا تنسيق:** كان مكتوبًا «ثلاثةُ
أزواج» فصارت ستًّا، **وتصير سبعًا بالفرع القادم** ⇒ **عددٌ يشيخ بالبناء لا
بالإهمال، ويُبطل نفسَه كلَّ جولة.**

🔴 **ولم يكن العددُ بائتًا وحدَه: القائمةُ نفسُها كانت ناقصةً ثلاثةَ أزواجٍ
قِيست ولم تُسجَّل** — **وذلك أخطر: العددُ الخاطئُ يُكذّبه العدّ، والقائمةُ
الناقصةُ تبدو تامّةً لمن لم يقس.** ⇒ **فما قِيس يُسجَّل حين يُقاس، لا حين
يُحتاج إليه.**

⚠️ **والقاعدةُ هي الحكمُ لا طولُ القائمة:
`merge_commit_sha` على طلبٍ مفتوحٍ دمجٌ اختباريّ، ولا يُقتبس إلّا مع
`merged_at` غيرِ العدم** — **وتصدق على زوجٍ وعلى مئة.**

⚠️ **والاقتباسُ المبكّرُ يُثبت رقمًا لا وجودَ له** — **ولا يُصحَّح بعدُ، لأنّ
مَن يقرؤه لا يعرف أنّه خطأ.**

**② ورأسُ الفرع سلفٌ لـ`main`** — والأقوى أنّ إيداعَ الدمج **يسمّيه أبًا ثانيًا**،
فينفي `squash` و`rebase` معًا لا يُثبت النَّسَبَ وحدَه.

**③ وCI على إيداع الدمج نفسِه** — لا على رأس الفرع قبله، **وبالسطحين.**

**④ والفرعُ محذوفٌ من الخادم ومحلّيًّا** — و`-d` لا `-D`، **فنجاحُه بيّنةُ
دمجٍ بذاته** (يرفض غيرَ المدموج)، ومعه `git remote prune`.

**⑤ وشجرةُ `main` تساوي المراجَعَ** — `git diff --stat <المراجَع> origin/main`.

🔴 **وهذا يقيس ما لا يقيسه ② إطلاقًا:** النَّسَبُ يثبت أنّ المراجَعَ **سلف**،
**ولا يثبت أنّ إيداعَ الدمج لم يحمل معه شيئًا آخر** — حلَّ تعارضٍ غيّر مضمونًا،
أو تعديلًا أُقحم في الدمج. ⇒ **أبٌ ثانٍ صحيحٌ وشجرةٌ مختلفةٌ يمرّان من ② معًا.**

⚠️ **والفراغُ ليس الجوابَ الصحيحَ دائمًا، ويُقال شرطُه وإلّا صار الحفظُ عطلًا:**

```
main لم تتقدّم منذ نقطة التفرّع   ⟶ الفرقُ **فارغ**، وهو المتوقَّع
main تقدّمت                      ⟶ 🔴 الحكمُ ينقلب، ولا يصحّ توقُّعُ الفراغ
                                    والصيغةُ حينئذٍ:
                                    git diff --numstat <الأبُ الأوّل> <إيداعُ الدمج>
                                    == `--numstat` المراجَعَ حرفًا بحرف
```

🔴 **ومن يحفظ «فارغ» بلا شرطِه سيقرأ فرقًا مشروعًا عطلًا — أو، وهو الأسوأ،
سيرى فراغًا حيث كان يجب أن يرى فرقًا.**

✅ **وشاهدٌ مجّانيٌّ يُضمّ إليه ولا يقوم مقامَه: تشغيلُ الحزمة على `main` بعد
الدمج** (`131 · 2633` هي هي) — **تأكيدٌ رابعٌ مستقلٌّ لتساوي الشجرتين.**
⚠️ **وهو `main` بعد الدمج لا قبله**، فلا يُقابَل بعددٍ محفوظٍ عن `main` سابقة.

> 🔴 **وبنودُ الحزمتين أعلاه تُجمع كلُّها في كلّ مرّة حتى حين يبدو التغييرُ
> تافهًا** — فالبندُ الذي يُسقَط لصغره هو الذي لا يُقاس أبدًا.
>
> **ولا واحدةٌ ممّا أدناه جاءت من تغييرٍ يمسّ كودًا يعمل.** ⚠️ **ولا تُكتب
> «كلُّها تحت `docs/`»** — فهذا الملفُّ نفسُه في الجذر، وحادثتان منها وقعتا في
> صياغة هذا القسم. **وشرطُ المسار يُفحص بـ`grep` ويسقط؛ ووصفُ الصنف يصمد.**
>
> ⇒ **والسببُ ليس مصادفة: حيث لا شيءَ يُكسَر لا شيءَ يصرخ، فتُقاس الأشياءُ
> بالنظر إليها وحدَه.** **فالتوثيقُ والسكربتاتُ ليست منطقةً آمنةً بل منطقةً بلا
> إنذار — والحزمةُ تعوّض الإنذارَ الغائب.**

## 🔴 حوادثُ مؤرَّخة — عن الفرق بين ما قِسناه وما ظننّا أنّنا قِسناه

⚠️ **ولا عددَ في هذا العنوان عمدًا: كان «تسعُ حوادث» وصارت إحدى عشرة، ولم
يتحرّك العنوان.** ⇒ **عددٌ يُكتب في عنوانٍ ثمّ ينمو ما تحته يبيت** — **والسلسلةُ
مرقَّمةٌ بنفسها (① ② ③ …) فهي تعدّ نفسَها.** 🔴 **ويُطبَّق على كلّ عنوانٍ يعدّ
محتواه، لا على هذا وحدَه** — **وقد بات ثلاثةً في يومين قبل أن تُكتب هذه
القاعدة.**

🔴 **وكان هنا استثناءٌ يقول «يبقى العددُ حيث يكون عقدًا لا تعدادًا» ويستشهد
بـ«الخماسيّة» و«الرباعيّة» — وقد سقط المثالان معًا (٢٦ آب ٢٠٢٦):** الأولى
عُدِّل ركنُها ② والثانية تحرّك اسمُها ثلاث مرّات في يومين. ⇒ **فـ«العقدُ» ليس
ضمانةَ ثبات — هو ضمانةُ أنّ الزيادةَ تُقرَّر لا تتراكم، والمقرَّرُ يتحرّك.**

✅ **والاستثناءُ يضيق ولا يسقط: يبقى العددُ حيث لم يتحرّك منذ كُتب** —
**و«ثلاث قواعد لا تُخرَق» مثالُه الوحيدُ الصادق** (**ومقيسةٌ دقيقة: ثلاثُ
قواعدَ وشرحان موسومان «لا بندَ تنفيذ»**). ⚠️ **ومثالٌ واحدٌ صادقٌ خيرٌ من
ثلاثةٍ يسقط اثنان منها بعد يومين.**

**وستّةُ أصنافٍ خرجت من كنسةِ الملفّ (٢٥ آب ٢٠٢٦)، والحكمُ يتبع الصنفَ لا
الرقم:**

```
يعدّ قائمةً تنمو تحته            ⟶ 🔴 يُسقط
يصف واقعةً ماضيةً مغلقة          ⟶ ✅ يبقى   («لقطاتُها الستّ»)
يحرس نفسَه بنصّه أو يحمل تاريخَه  ⟶ ✅ يبقى   («لا تتغيّر بتغيّر العدد» · «مقيسة» بتاريخها)
يقتبس عددًا قديمًا ليروي تغيُّره   ⟶ ✅ يبقى   (هذا السطرُ نفسُه)
🔴 يدّعي تطابقًا مع شيءٍ ينمو    ⟶ **أخطرُها: لا يبيت وحدَه بل يُبطل الدعوى معه**
🔴 صحيحٌ بإسنادٍ يُعلن أنّه غيرُ مقيس ⟶ يُوسَم بمصدره أو يُقاس — **ويُبحث في
                                     المستودع قبل أن يُطلب قياسٌ من أحد**
```

⚠️ **والصنفُ الأوّلُ وحدَه يُسقط** — **وتعميمُ الإسقاط على الستّة يهدم صحيحًا،
وقد قِيست `:952` فوُجدت دقيقةً فتُركت.**

> ⚠️ **سجلُّ الحوادث لا شريعة.** كلُّ قاعدةٍ فيه وُلدت من حادثةٍ واحدة،
> **وكلُّ واحدةٍ معرَّضةٌ لأن تضيق كما ضاقت ⑧ بعد أسبوعٍ من كتابتها.**
> ⇒ **الحدثُ فوق القاعدة عمدًا: من بلغ القاعدةَ مرّ بحدودها.** والتفصيلُ في
> الترويسات وفي الحزمتين أعلاه، **لا هنا** — نسختان تتباعدان، وتلك علّةٌ
> عولجت مرّتين.

🔴 **وخطوةٌ تسبق هذا القسمَ كلَّه، وليست بندًا فيه — لأن العطلَ ليس نقصَ قواعدَ
بل تعذُّرَ استحضارها.** أُضيف تسعُ قواعدَ إلى ملفٍّ يعاني أصلًا من أنّ ما فيه لا
يُستحضَر، **وفي جولةٍ واحدةٍ طُلب ثلاثَ مرّاتٍ موضعٌ لقاعدةٍ لها موضع:**

```
⑤ الأعمدةُ من إدراجٍ يعمل   ⟵ الجواب كان ٠٩٥:٧٧٩، ثلاثةَ أسطرٍ فوق ما نُسخ نصفُه
③ الحارسُ يطبع ما قاسه      ⟵ القاعدةُ في هذا الملفّ بنصّها، بشاهدٍ آخر (`src=0,3`)
٤ب «اقرأ الفئةَ كلَّها»     ⟵ كُتبت ستّةُ أعمدةٍ مسمّاةً ثمّ ضُيّقت إلى أربعة،
                              ولا واحدٌ كتب `select *` على جدولٍ فيه أربعةُ صفوف
```

> **⇒ قبل كتابة أيّ قاعدةٍ في `CLAUDE.md`: يُبحث الملفُّ عنها أوّلًا** — فثلاثُ
> مرّاتٍ في جولةٍ واحدةٍ طُلب موضعٌ لقاعدةٍ لها موضع (⑤ · ③ · ٤ب).
> **وقبل طلب أيّ قياس:**
> **يُراجَع المحصَّلُ أوّلًا** — **فأسماءُ الأعمدة كانت في مخرَج `select *` بين
> يدَي المراجع، فأرسل عددَها (١٦) لا أسماءَها**، والمقابلةُ بقائمة الوثيقة (١٥)
> كانت تكفي لحظتَها؛ وأصلُ الـ`0.00` كان في لفظ المالك «وقت ما كتبتهم».
> **وكلاهما طُلب قياسًا وهو محصَّل.**
>
> ⚠️ **و`DATABASE_DIAGRAM` ليست موضعَ الجواب هنا — هي الناقصةُ بعينها** (تسرد
> خمسةَ عشرَ ولا تعرف `created_at`). **فالشاهدُ «خامٌّ محصَّلٌ أُرسل ملخَّصًا»
> لا «قاعدةٌ لم تُقرأ»** — وهو ② لا ٤ب.

⚠️ **وثالثةُ الشواهد ٤ب وقعت على الطرفين معًا** — ولذلك تُكتب: **قاعدةٌ في هذا
الملفّ لا يستحضرها كاتبُ الملفّ ولا مراجعُه ليست قاعدةً ضعيفةَ الصياغة، بل
قاعدةً بلا خطوةٍ تستدعيها.**

---

🔴 **وشكلُ الخلاصة نفسِها يقرّر هل تسافر بشروطها: كلُّ خلاصةٍ تُسيَّج للاقتباس
تفتتح بحالتها ثمّ تحكم — فما يُقتبس وحدَه يجب أن يصحّ وحدَه.**

**والنموذجُ والنقيضُ في هذا المستودع، لا في تعليل:**

```
✅ النموذج  SAFETY-GATE:322  «إن احتاج أحدٌ يومًا أن يعكس مستندَ جردٍ عليه
                              غرامة — يتوقّف فورًا…»  ⟵ الحالةُ أوّلًا، ثمّ الحكم
🔴 النقيض   HANDOFF:728      «فالصواب غرامةٌ عاكسة، لا غرامةٌ ممحوّة.»
                              ⟵ حكمٌ بلا مجال. وفقرتُه فوقه تقيّده صراحةً
                                («حسم المسألة نفسها **للمستندات**») —
                              **والتسييجُ منح الخلاصةَ بروزًا وجرّدها من مجالها
                              في الفعل نفسِه.** فسافرت بلا شروطها، **وبُني على
                              إطلاقها تعارضٌ بين قرارين لا وجودَ له.**
```

⚠️ **وهو ⑨ لا بندٌ جديد:** التسييجُ **تحسينُ عرض**، والمجالُ **الخاصّيّةُ
الممحوّة** — **وتحسينٌ يمحو خاصّيّةً لم تُعلَن يبدو تحسينًا خالصًا.**

**✅ والفارقُ المقيسُ بين ما ينجو وما يطير — والصياغةُ الأدقّ:**

> **الخلاصةُ التي تحمل قياسًا تحمل مجالَها معه مجّانًا.**
> **والمجرَّدةُ تطير لأنها لا تُمسك بشيء.**

**وعيّنةٌ تقيس انتشارَ العيب، منتقاةٌ بالموضع لا بالنظر** (كلُّ عاشرةٍ من
الكتل المسيَّجة مرتَّبةً بالملفّ والسطر):

```
الفئةُ المعدودة   ١١٧ كتلةً مسيَّجةً في ١٧ ملفّ markdown
العيّنة           ١٢، بالموضع — وتناسبُ حصّةِ PROJECT_HANDOFF فيها صدفةٌ لا ترجيح
النتيجة          **٤ من ١٢ معيبةٌ قطعًا · ٦ من ١٢ بالمعيار الصارم**
                 ⇒ مدًى تقريبيٌّ ٢٠–٦٠ من ١١٧ · **ولا نقطةَ واحدة** (البند ①)
                 ⚠️ **و١١٢ معدودةٌ لا مفحوصة** — لم تُقرأ، ولا تُقال سليمة
```

⚠️ **والفرضيّةُ ارتباطٌ لا سببيّة:** طُوبقت على سبعَ عشرةَ حالةً من كاتبين
وزمنين (١٢ من العيّنة + ٥ من خلاصات هذه الجولة) **فوافقتها كلُّها** — **ولا
تُرفع فوق ذلك.** ⇒ **وتصلح فرزًا يرتّب القراءة لا مصفاةً تُبرّئ:** خلاصةٌ بلا
رقمٍ ولا معرِّفٍ ولا اسمِ ملفٍّ **مرشَّحةٌ للعيب**، ⚠️ **وحاملةُ الرقم قد تبقى
معيبةً رغمه** (`HANDOFF:826` على الحدّ وهي تحمل `0`) — **فلا تُعلَن البقيّةُ
سليمةً لأنّها لم تُرشَّح** (البند ٤ب).

✅ **والنصُّ يشهد على نفسه من الجهة الأخرى:** المسيَّجةُ أعلاه («الخلاصةُ التي
تحمل قياسًا…») **لا تحمل رقمًا ولا معرِّفًا ولا اسمَ ملفّ ⇒ فالفرزُ يرشّحها**،
**وتنجو بالقراءة** لأن موضوعَها مسمًّى في أوّل لفظٍ منها. ⇒ **فمرشَّحٌ بريء
ومُبرَّأٌ معيب (`HANDOFF:826`) في نصٍّ واحد — ونقيضان داخل النصّ أقوى من تحفّظٍ
مكتوبٍ تحته.**

**① `null_is_void` أعطى `1·1` ثمّ `1·1` ثمّ `2·2` على ثلاث حالاتٍ معزولة،
و`null_document_id` أعطى `0` على الثانية وحدَها.**
⇒ قياسٌ على **نوعٍ واحدٍ** يُثبت أنّ العدَّ يمسك الحالة، لا أنّه يفصلها. [`106c`]

**② `Compare-Object` قالت «مختلف» عن ثلاثة ملفّاتٍ متطابقة، وأعدادُ سطورها
`129/129 · 119/119 · 116/116`.**
⇒ الفرقُ في **أداة القياس** يسبق الفرقَ في المقيس. [ما قبل الدمج ②]

**③ ما أوقفني في ② لم يكن انتباهًا: `129/129` كان مطبوعًا بجانب `False`.**
⇒ **الحارسُ يطبع ما قاسه بجانب ما حكم به** — والحكمُ وحدَه لا يُناقَض.
⚠️ **والقاعدةُ قائمةٌ في هذا الملفّ من قبل، بنصّها، بشاهدٍ آخر** (`src=0,3` في
٠٩٥ب/ب) — **وهذه شاهدٌ ثانٍ لها لا قاعدةٌ جديدة**، ولو كُتبت سطرًا مستقلًّا
لصارت نسختين تتباعدان. [«فكلُّ حارسٍ يطبع ما قاسه» — قسمُ الاختبارات]

**④ `079a:162` يحمل `(reverses_document_id is null and r.id is null)` وفوقه
«Both halves are needed» — واستُشهد بالسطر `163` الملاصق.**
⇒ **اسمان متجاوران ليسا سؤالًا واحدًا:** `is_void` ليست `not is_live`. [`107`]

**⑤ `23502`: قائمةُ إدراج `stock_fines` من تعريف الجدول أسقطت `attribution`،
والإدراجُ العامل `095:779` يكتبه ويشرح لماذا فوقه بثلاثة أسطر.**
⇒ **الأعمدةُ من إدراجٍ يعمل لا من تعريف جدول.** [`107c`]

**⑥ الطلبُ المفتوح أعلن `merge_commit_sha = b625587`، وإيداعُ الدمج `708b894`.
وثانيةٌ: «لا صيغةَ ‹انسخ للفريق›» اقتباسٌ صحيحٌ عن صيغةِ المخرَج، استُعمل
حكمًا على النقل — فكاد يشطب وصفًا قائمًا في `PROJECT_HANDOFF`.**
⇒ **المملوءُ أخطرُ من الغائب:** الغائبُ يُسأل عنه، والمملوءُ يُقتبس. **وقولٌ
مُقتبَسٌ حقلٌ مملوء.** [ما بعد الدمج ①]

**⑦ مرتكزُ `stock_document_liveness` كان معيبًا (`create or replace` يحفظ
المُعرِّف)، و`20114` جاءت ملاصقةً لـ`20126` فكان سيُصيب.**
⇒ **الطريقةُ تُحاكَم قبل نتيجتها لا بها** — ولا يكشفها المخرَج، لأنه صحيح. [`108`]

**⑧ «اقرأ `/check-runs` لا `/status`» سُنَّت لفحصٍ `in_progress`، فأخفت النشرَ
الذي لا يظهر إلّا في Statuses (`unstable` مع `3/3 success` ⟶ `clean`). وثانيةٌ
في نفس اليوم: `internal_triggers` توقَّعناها `2` فجاءت `24`، والاثنتان صحيحتان
— ورقمُ نسخةِ مراجعةٍ نُقل شرطَ قبولٍ عن قاعدةٍ أخرى، فوقع الخطأُ عند من قاس
وعند من نقل معًا. وثالثةٌ: البندُ ⑤ في حزمة ما قبل الدمج كُتب بلا نطاقٍ في
وثيقةٍ تحمل هذا البندَ نفسَه. ورابعةٌ: نطاقُ «تأكيد المالك بالمتصفّح» كُتب
**عدًّا لثلاثة مجلّدات** فورث حدَّ حادثته — و`public/locales` يُرسم على شاشةٍ
حيّةٍ وليس فيها (`coverage.colMatched` ⟶ `StocktakeCoverage.js:94`، مقيسًا
٢٤ آب ٢٠٢٦)، **فصار يُسمّي ما يحميه لا ما يعُدُّه.** ⚠️ **ثمّ وقع فيها مرّةً
ثانيةً وهو يُصلحها:** الصياغةُ الجديدةُ **أسقطت الكتابةَ الصامتة** —
و`saveCount` مثالُها في الإيداع نفسِه — فأُضيف صنفٌ ثالث. **وقاعدةٌ تُصحَّح
مرّتين ليست قاعدةً سيّئة، هي قاعدةٌ تُقاس؛ والقاعدةُ السيّئةُ من لا يُقاس.**
⇒ **القاعدةُ المسنونةُ لسدِّ عمًى ترث حدودَ حادثتها** — فتُكتب معها حالتُها.
🔴 **والدعوى تُقاس مستقلّةً عن طريقِ قائلها:** فإن فسدت الحجّةُ **سقطت الحجّةُ
وحدَها**؛ **وسحبُ الدعوى معها خطأٌ ثانٍ يستر الأوّلَ ويُفقد صوابًا كان في
اليد.** ⚠️ **ووقع هنا بعينه:** «`public/locales` يُرسم» قيلت بلا قياسٍ فسُحبت
كلُّها، **ثمّ قِيست فصحّت** ⇒ **فالفاسدُ كان الطريقَ والنسبةَ إلى الحالة، لا
القضيّة.**
**وهذا البندُ يحكم على الثمانية الأخرى، ولا ينجو من كاتبه ولا من الوثيقة التي
تحذّر منه.** [`108b` · ما قبل الدمج ①⑤]

**⑨ فرقٌ حُذفت منه بادئاتُ `--` ليقرأ أوضح، فصار غيرَ قابلٍ لإعادة التطبيق —
وطُلب النصّان كاملين (`98` · `104`) نقلًا لا بناءً. وثانيةٌ: سطرُ «تأكيدِ المالك
بالمتصفّح» بدا مستبدَلًا وهو لم يُستدعَ، فكاد يُشطب. وثالثةٌ: `PROJECT_HANDOFF`
تُرك عمدًا بلا مسّ، **ولا أثرَ لذلك في `--numstat`** فأُبلغ صراحةً.**
⇒ **تحسينٌ يمحو خاصّيّةً لم تُعلَن يبدو تحسينًا خالصًا**، ومراجعةُ الفرق تبحث
عمّا فسد لا عمّا نقص. **والضررُ يقع على من لا يملك الأصل.** ⇒ **وما لم يُفعل
عمدًا يُبلَّغ، وإلّا قُرئ سهوًا** — **وصفرٌ لا يُرى ليس صفرًا مُبلَّغًا.**
[ما قبل الدمج ②]

**⑩ ووجهٌ ثانٍ للحادثة ⑧، معكوسٌ عنها: قاعدةٌ سُنَّت قبل أن تقع حادثتُها.**
البندُ ① في حزمة ما قبل الدمج كُتب احتياطًا **ولم يُشغَّل في حالته حتّى ٢٥ آب
٢٠٢٦**، **فأخرج أربعةَ أسطرٍ في يومٍ واحد** (مرحلتان · انتظارُ `incomplete` ·
تقلُّبُ `total_count` · اختلافُ عضويّة السطحين).
⇒ **فالقاعدةُ لا تُختبر إلّا حين تُشغَّل في الحالة التي كُتبت لأجلها** —
⚠️ **وأخطرُ من ذلك أنّ استيفاءَها المتكرّرَ قبل ذلك يُقرأ رصيدَ ثقة، وهو صمت.**

**⑪ ووصفٌ مقيسٌ من لقطةٍ حُمل حكمًا على تصميم.** قِيس أنّ صندوقَ التحويل لا
يُظهر مستودعَ المصدر، **فسُمّي الغيابُ عطلًا** — **والمالكُ صحّحه: الشاشةُ
تُفتح من داخل مستودعٍ مختار، فالمصدرُ سياقُ الصفحة لا حقلٌ محذوف.**
⇒ **اللقطةُ تُظهر شاشةً ولا تُظهر مسارَ استعمالها** — **فالوصفُ منها يصحّ،
والحكمُ على التصميم لا يصحّ، وهما يخرجان في فقرةٍ واحدةٍ بنبرةٍ واحدة.**

**⑫ ورقمٌ ظلّ يُقتبَس في رسائل إيداعٍ متتالية ولم يُقَس على طرفَيه قطّ.**
كُتب «٢٦١٣ ⟶ ٢٦٣٣» في أكثرَ من إيداع، **و«٢٦١٣» لم تُقَس على `main` مرّةً
واحدة** — نُقلت من جولةٍ إلى جولة. **والمقيسُ حين قِيس أخيرًا:**

```
git grep "^\s*it(" على الطرفين   ⟶  main 1906 ⟵ HEAD 1928   = +٢٢
والحزمةُ بالتشغيل على هذا الرأس   ⟶  2633
⚠️ ولا يُطابَقان: عدّادُ المشغِّل يشمل مولَّداتِ الحلقة، و`git grep` لا يشملها
```

⇒ **الرقمُ المُقتبَسُ من جولةٍ سابقةٍ اقتباسٌ لا قياس**، **ويُعاد قياسُه أو
يُذكر بوصفه منقولًا.** 🔴 **والتكرارُ لا يمنح سلطة: رقمٌ نُقل خمسَ مرّاتٍ ليس
أمتنَ من رقمٍ نُقل مرّة، وإنّما يبدو كذلك.**

⚠️ **وموضعُ التصحيح ليس حيث وقع الخطأ:** الرقمُ المسحوبُ مكتوبٌ في رسائل
إيداعٍ **مدموجةٍ في `main`، ولا تُحرَّر** ⇒ **فيُصحَّح حيث يُقرأ لا حيث كُتب،
وهذا موضعُه.**

---

## 🔴 مصدرُ كلِّ دعوى — قواعدُ من جولةٍ واحدة

**العملُ طرفان بمصدرَين لا يتقاطعان:** **المستودعُ عند الكاتب، والمحضرُ عند
المراجع** — ولا وصولَ لأحدهما إلى مصدر الآخر. **وهذه القواعدُ كلُّها فرعٌ من
ذلك.**

⚠️ **وأعدادُ الشواهد أدناه ثلاثُ سلاسلَ لا واحدة، ولكلٍّ معيارُها في سطرها:**
**عددُ (أ)** يعدّ *بناءً قائمًا طُلب بناؤه* · **وعددُ (ج)** يعدّ *دعوى عن مصدرٍ
لا يملكها قائلُها* · **والحوادثُ المرقَّمةُ ①–⑪ أعلاه سلسلةٌ ثالثةٌ بالكامل.**
🔴 **ولا تُجمع الثلاثةُ ولا تُقابَل** — **فقارئٌ يجمعها يُخرج عددًا لا يعني
شيئًا، أو يظنُّها سلسلةً واحدةً تناقضت.**

**(أ) ما يقوله المراجعُ عن بناءٍ في المستودع يُرسَل سؤالًا لا مطلبًا حتّى
يُقاس.** ⚠️ **فلا وصولَ له إلى الملفّات، وصيغةُ الجزم عنده ادّعاءُ قياسٍ لم
يقع** — **وقد وقعت ستَّ مرّاتٍ في جولتين، كلُّها بالشكل نفسِه: بناءٌ قائمٌ
طُلب بناؤه** (التغطية · الأرصدة · الشرط ④ · الشرط ② · مِعلاقُ المسؤوليّة ·
«دعوى بلا مصدر» وهو مسنَدٌ في هذا الملفّ). ✅ **وهي أرخصُ قاعدةٍ هنا: تكلّف
علامةَ استفهام.**

**(ب) وكلامُ المالك يصل الكاتبَ بنصّه، لا بخلاصة المراجع.** الخلاصةُ تعليقٌ
يُقرأ بجانب النصّ، **ولا تكون مصدرًا لبناءٍ ولا لوثيقة.** 🔴 **فقد صحّح النصُّ
الخامُّ خلاصتَه مرّتين في جولةٍ واحدة** («بلا بيانات» ⟶ «بلا سجلّ» · وموافقةٌ
واحدة ⟶ موافقاتٌ عددُها ملتبس) — ⚠️ **وحذفُ إعادةِ الصوغ غيرُ منتظمٍ فلا
يُكتشف بالمقابلة.**

**(ج) ويُسأل مَن يملك المصدر.** **دعوى عن سطرٍ في ملفٍّ لا يقيسها إلّا الكاتب،
ودعوى عن ترتيب كلام المالك لا يقيسها إلّا المراجع.** ⚠️ **وقد وقع العبورُ في
الاتّجاهين: حوادثُ من المراجع إلى المستودع، وواحدةٌ من الكاتب إلى المحضر**
(ترتيبُ قولَي المالك، كُتب بصيغة الجزم فصُدِّق بالجوار).

🔴 **وهذا السطرُ نفسُه شاهدُ القاعدة أعلاه، فيُروى ولا يُطمس:** كُتب **«تسعُ
حوادثَ»** فبات **في اليوم نفسِه** — سمّى المراجعُ بعده «العاشرةَ من صنفي» ثمّ
«الحاديةَ عشرةَ». ⇒ **فعددٌ يبيت بيد كاتبِه لا بيد الزمن**، **وأسرعُ ما يبيت
ما يُكتب عن سلسلةٍ ما زالت تجري.**

**(د) وكلُّ نموذجٍ يصفه المالكُ يُتبَع بسؤالٍ ثانٍ: «هل هذا ما يجري عندك اليومَ
فعلًا؟»** — **سؤالٌ ثانٍ لا فرعٌ من الأوّل.** 🔴 **فقاعدةُ «يُسأل عن المواقف لا
عن الشاشات» تُخرج جوابًا غنيًّا مرّتين ولا تضمن أنّ الجوابَ وصفُ واقع:**
المرّةُ الأولى أخرجت **إجراءً يفعله**، والثانيةُ **نموذجًا معياريًّا مع تصريحٍ
بأنّه ليس واقعَه** («المستودعُ اسميٌّ فقط» · «هيك بتخيّل»).

🔴 **(د٢) وإذنٌ يُعطى على حالةٍ واحدةٍ لا يُبنى عليه حتّى يُعرَض بحالةٍ تكشف
كلفتَه.** ⚠️ **فالمالكُ يجيب عن المثال الذي وصله، لا عن القاعدة التي نشتقّها
منه — والقاعدةُ أوسعُ من مثالها دائمًا.**

```
عُرض ١٥ ⟶ صفر (الحالةُ التي تبرّر البناء)   ⟶ «ممكن يحذّر عادي»
ثمّ  ٣ ⟶ صفر (الحالةُ التي تكشف الكلفة)     ⟶ «لا، خلّيها زي ما هي»
```

⇒ **والإذنُ الأوّلُ كان صريحًا غيرَ ملتبس، وكان سيُنتج قاعدةً لا يريدها.**
✅ **والناقصُ في السؤال لا في الجواب** — ⇒ **فحالتان لا واحدة: التي تبرّر
البناء، والتي تجعله مزعجًا.** **وكلفتُها سؤالٌ واحدٌ إضافيّ.**

🔴 **وحدُّ ما يُضاف بلا سؤال، ويُكتب كي لا يعتمد على تقديرٍ في كلّ مرّة:**

```
سطرٌ يسدّ فجوةً في قاعدةٍ أُقرّت   ⟶ يُضاف **مع إعلانه وحجّته**
سطرٌ يُدخل قاعدةً جديدة            ⟶ **يُسأل عنه، ولا يُضاف**
```

⚠️ **والفارقُ ليس حجمَ السطر بل أهو داخلَ ما أُقرّ أم خارجَه** — **وإيداعٌ
عُدَّ بندًا بندًا لا يحمل بندًا سادسًا صامتًا.**

**(هـ) والنقلُ الحرفيُّ قبل الإيداع يكشف للكاتب ما لا تكشفه الكتابة.**
**العينُ تقرأ ما كتبته كما أرادته، وتقرأ ما تنقله كما هو.** ⇒ **يُنقل النصُّ
كاملًا ولو لم يُطلب.** ✅ **وثلاثةُ شواهدَ من صفحةٍ واحدةٍ رُوجعت ستَّ مرّات:**
عطلُ ترتيبٍ («ثالثًا» قبل «ثانيًا») · وعددٌ بائتٌ داخل السطر الذي أُضيف لتصحيح
العدّ · وصياغةٌ كانت تعامل أيَّ بناءٍ نقضًا للتأجيل والقرارُ يقول عكسَه.

**(و) والنصُّ يُكتب إلى الملفّ بأدوات الملفّات، ولا يُركَّب عبر الصدفة.**
⚠️ **فالشرطةُ الخلفيّةُ والفوقيّةُ و`$` محارفُ نصٍّ عندنا وأوامرُ عندها**،
**ونثرُنا العربيُّ مرصَّعٌ بالشيفرة في كلّ سطر.**
⇒ **ملفٌّ ثمّ إدراج، لا `heredoc` يحمل نصًّا فيه شيفرة.**

🔴 **(ز) وسلامةُ شكلِ الناتج ليست دليلًا على سلامة نقله.** ⇒ **ما كُتب آليًّا
يُقابَل بمصدره مقابلةً، لا يُقرأ قراءة** — **فالقراءةُ تمسك الركاكةَ ولا تمسك
الحذف.** ✅ **وهو أخو `hash-object` في مستوى السطر: المقابلةُ تكشف ما لا
تكشفه العين.**

⚠️ **وهذه ناقصةٌ، وأتمُّ منها: ما يُنقل آليًّا لا يحتاج مقابلةً أصلًا.**
⇒ **فالمقابلةُ حارسٌ يحتاج قارئًا (ح)، والنقلُ الآليُّ حارسٌ يمنع** — **وكلُّ
ما أمكن أن يمنع فليمنع.**

**وحادثتُهما تُكتب معهما، وإلّا قُرئتا تشدُّدًا (٢٥ آب ٢٠٢٦):**

```
المصدرُ:  `stock_doc_type` · `entry_uom` · `fine_basis` · `storage_kind`
          بلا `create type` مُودَع
المكتوبُ:  «—  ·  ·  ·  بلا  مُودَع.»
```

🔴 **أربعةُ أسماءٍ وتعبيرٌ اختفت، والجملةُ بقيت نحويّةً سليمة** ⇒ **تُقرأ
ركاكةَ صياغةٍ لا فقدَ محتوًى**، **فالمراجعُ يمرّ عليها ويظنّها أسلوبًا رديئًا.**
⚠️ **والصنفُ لا يُمسك بقراءة الناتج مهما دُقّق: الناتجُ سليمُ الشكل.**
🔴 **واكتشافُه كان عرَضًا — اشتكت الصدفةُ خمسَ مرّات** (`command not found`)
⇒ **فالحارسُ الذي عمل ليس حارسًا بل أثرٌ جانبيّ، ولو ابتلعت الأداةُ شكواها
لَمرّ صامتًا ولا شيءَ في مسارنا كان سيمسكه.**

🔴 **(ح) والحارسُ الذي يَمنع لا يحتاج قارئًا؛ والذي يَطبع يحتاجه** —
**وقارئٌ يعرف ما يتوقّع يقرأ توقُّعَه لا الرقم.**

⇒ **ما استطاع أن يمنع فليمنع** (خروجٌ بخطأ · بندٌ في الحزمة يُنطق حكمُه)،
**وما بقي طابعًا فرقمُه يُقتبس في وصف الإيداع لا يُقرأ في المخرَج** —
**فالاقتباسُ يُجبر على النظر، والمخرَجُ يمرّ.**

⚠️ **وحادثتُها (٢٥ آب ٢٠٢٦): `فوق 80: 1` طُبع قبل الإيداع مباشرةً، وقُرئ على
أنّه السطرُ المُصلَح للتوّ** — **والحقيقةُ أنّ الإصلاحَ دفع نصفَ السطر إلى
التالي فبلغ ٨٨.** 🔴 **ورقمٌ يُطبع ولا يُقرأ ليس حارسًا، هو ضجيج.**

✅ **وهي أختُ البند ①(ب) في حزمة ما قبل الدمج** (`incomplete` انتظارٌ لا حكم) —
**كلتاهما: مخرَجٌ قُرئ بما نتوقّعه لا بما فيه.** ⚠️ **وأشدُّ من (و) لا أخفّ:
هناك لم يكن حارسٌ أصلًا، وهنا كان وأدّى عملَه وضاع في المخرَج.**

🔴 **والرقمُ المقتبَسُ يحمل تعريفَه، وإلّا صار رقمًا لا يُقابَل بشيء — وذاك
ضجيجٌ مقتبَس، وهو ما تمنعه هذه القاعدةُ نفسُها.** **وحارسُ العرض يعدّ:**

```
أسطرَ `+` في `git diff -U0` بلا `+++`   ·   وأسطرَ `-` بلا `---`
⇒ **وهو تعريفُ `numstat` نفسُه** — السطرُ الفارغُ سطرٌ مضاف
```

✅ **وقوبل: `numstat` أعطى `17 1` والحارسُ أعطى `17` و`1`** — **رقمان من
مصدرين، وقد قوبلا (٢٥ آب ٢٠٢٦).**

🔴 **(ط) والإعلانُ يجعل الشيءَ مراجَعًا، ولا يجعله جائزًا.** ⇒ **ما تمنعه
قاعدةٌ يبقى ممنوعًا وإن أُعلن؛ وإنّما يُعلَن ما يجوز وله حدّ** (قياسٌ ناقص ·
مصدرٌ لا نملكه · اختيارٌ بين مسارين).

⚠️ **وخطرُها من عادتنا نحن: نكافئ الإعلانَ — بحقّ — وبلا حدٍّ يصير «أُعلنه»
إذنًا عامًّا.** **وشاهدُها (٢٥ آب ٢٠٢٦): فرقٌ حرفيٌّ اختُصر ووُسم «معدٌّ
للقراءة لا للتطبيق»، والبند ② يمنع الاختصارَ نصًّا** ⇒ **فالوسمُ سمّى العطلَ
ولم يُصلحه.**

**(ي) وما كان مخرَجَ أمرٍ يُلصَق ولا يُكتب.** ترويساتُ القِطَع · البصمات ·
`numstat` · حقولُ CI و`mergeable_state` · مخرَجُ الاختبارات. ⚠️ **ولا يُعاد
صوغُه ولا يُرتَّب ولا يُختصَر — وإن لزم شرحٌ فبجانبه لا داخلَه.**

🔴 **وشاهدُها أثقلُ ممّا يبدو، لأنّه يمسّ الوسيطَ لا الملفّ:** كُتبت ترويستا
قِطعتَين بيدٍ فاختلفت ثلاثةُ أرقام (`2210` بدل `2216` · `22` بدل `21` ·
والصافي `18` بدل `17`)، **وشكلُ الترويسة صحيحٌ في الحالتين** — ⚠️ **فترويسةٌ
مُلصَقةٌ وأخرى مكتوبةٌ بيدٍ تبدوان سواءً على شاشة المراجع.**

⚠️ **ويلزم منه حدُّ أداةِ التحقُّق، ويُكتب مع الأداة:** **التحقُّقُ الحسابيُّ
يُثبت اتّساقَ الرسالة مع نفسها — أو يُنبّه إن اختلّ.** 🔴 **ولا يُثبت أنّ
المستودعَ كما تقول الرسالة: مصادرُه كلُّها رسالةٌ واحدة.** ⇒ **فقيمتُه كاشفةٌ
لفسادِ النقل لا مصدِّقةٌ للمستودع.** ✅ **والفضلُ للتكرار لا للحساب: ثلاثةُ
طرقٍ إلى الرقم نفسِه، فانفرد واحدٌ فظهر — ولو أُرسل رقمٌ واحدٌ لَمرّ.**

> ✅ **ومرشَّحٌ مقدورٌ عليه لا أمنية:** سكربتٌ يجمع حزمةَ ما قبل الدمج بأمرٍ
> واحدٍ ويطبع **بنودَها بجانب أحكامها** (③). **الوصولُ إلى GitHub API متاحٌ
> لي**، والمانعُ الوحيدُ أنّ الحزمةَ تُكتب أوّلًا فتحدّد ما يجمعه — **وحارسٌ
> يسبق تعريفَ ما يحرسه هو «حارسٌ بلا علامة» (البند ح).**

## 🔴 قواعدُ الأدوات — تُقرأ قبل كتابة أيّ أداةِ قياس

**أضعفُ حلقةٍ عندنا هي التي نحكم بها على البقيّة.** ⚠️ **في يومٍ واحدٍ
(٢٧ آب ٢٠٢٦) عُدَّت باليد عشرُ حالاتِ عطلٍ في أدواتِ قياسٍ كُتبت لأنفسنا.**
🔴 **والعددُ زِيد جولةً جولة ولم يُقَس بأمر — فيُقرأ حدًّا أدنى لما رُئي، لا
حصرًا** (⑰). ⚠️ **و«لم تُرَ» ليست «لم تقع» (⑭).**

🔴 **ولا يُقابَل هذا بشيفرة التطبيق، ولا يُقرأ ثناءً عليها: لم تُمَسّ في ذلك
اليوم أصلًا.** مقيسًا بـ`git diff --name-only 8394c23 origin/main`:
**أربعةُ ملفّاتٍ تغيّرت، ولا واحدٌ منها ملفُّ مصدرٍ للتطبيق** — `CLAUDE.md` ·
`docs/sql/README.md` · `docs/sql/111_*.sql` · `lib/sqlStatusRows.test.js`.
⇒ **فـ«لم يُرَ فيها عطبٌ» صادقٌ وفارغ: معناه «لم يُنظَر»، لا «سلمت».**

⚠️ **وكانت هنا جملةٌ تقول «ولم تُرَ في اليوم نفسِه واحدةٌ في شيفرة المشروع»،
تُقرأ ثناءً، وكُرّرت في كلّ خلاصةٍ ذلك اليوم قبل أن تُقاس.** 🔴 **وهي أخبثُ
من رقمٍ خاطئ: الخاطئُ يُكذّبه القياس، وهذه صادقةٌ وتُورث اطمئنانًا لم يُكتسب.**

⚠️ **وما أُرِّخ من الشواهد أدناه ليس يومًا واحدًا** (أقدمُه ٢٥ آب) — **فالعشرُ
مؤرَّخةٌ بيومها وحدَه، والقواعدُ أدناه ليست كلُّها مستخرَجةً منه.**

🔴 **وأكثرُها غيرُ مؤرَّخٍ أصلًا: اثنتان من أربعَ عشرةَ تحملان تاريخًا
(مقيسًا).** ⇒ **فيُعرف موضعُ الحادثة ولا يُعرف زمنُها** — **وقاعدةٌ بلا
تاريخِ حادثتها لا يُعرف أحيّةٌ علّتُها أم زالت، وهذا القسمُ بعينه يُقاس به
الشيخوخةُ في غيره.** ⏸ **ولا تُؤرَّخ الاثنتا عشرةَ من الذاكرة — ذاك ⑬
بلفظه.** ⇒ **تُؤرَّخ من السجلّ في دفعةٍ تالية، أو تبقى مُعلَنةَ الغياب.**

🔴 **وحدُّ الحراسة الآليّة مقيسٌ لا مقدَّر:** الطلبُ ٤٦ رُدَّ خمسَ مرّاتٍ
بخمسة أسبابٍ مختلفة، **والحزمةُ خضراءُ في الخمس جميعًا.** ⇒ **فقراءةُ النصّ
الحرفيّ ليست بطءًا يُحتمَل: هي الحارسُ الذي عمل.**

⚙️ **وموضعُ استدعاء هذا القسم كلِّه: قبل كتابة أداةٍ يُبنى على مخرَجها حكمٌ**
— رقمًا كان أم قائمةً أم نعم/لا. **ويُقرأ كلُّه؛ فانتقاءُ قاعدةٍ منه يعيد
المشكلةَ التي أنشأته.**

⚠️ **وثلاثٌ من قواعده مكتوبةٌ في مواضعها ولا تُنسخ هنا — فنسختان تتباعدان:**
**⑤** كلُّ عددٍ يُكتب مع ما يعدّه (البند ١٠) · **⑦** النصُّ يُكتب بأدوات
الملفّات لا عبر الصدفة (و) · **⑨** ما نُقل يُسمَّى موضعُه الثاني (البند ١٠).

---

**① لتُنتج الأداةُ شاهدَها قبل حكمِها** — فلا تُخرج «موجود/غائب» وحدَها، بل
ما رأته حتّى حكمت.
🔎 **شاهدُها:** مسبارُ صلاحيّاتٍ طبع «`http.extraheader`؟ = 🔴 موجود». والمقيسُ
بطريقٍ ثانٍ: ٢٦ مفتاحَ ضبطٍ، ولا واحدٌ يطابق. ⇒ **أداةُ جردٍ اخترعت ترويسةَ
مصادقةٍ لا وجودَ لها.** ✅ **ومسبارٌ يطبع «٢٦ مفتاحًا، ولا واحدَ يطابق» لا
يُصدَّق ولا يُكذَّب — يُقرأ.**
⚙️ **تُستدعى:** عند كتابة أيّ دالّةٍ تعيد حكمًا مختصرًا.

**② ويُحفظ مخرَجُ الأداة خامًّا لا ملخَّصًا** — ولو لم يُقرأ منه إلّا حقلٌ واحد.
🔎 **شاهدُها:** استجابةُ الطلب ٤٦ حُفظت وهو مفتوحٌ ولم يُقرأ فيها
`merge_commit_sha`. وبعد الدمج قُوبل الحقلان من ملفَّين: `0222c3c` ≠ `eea0eb0`
— ⇒ **فالحقلُ الذي لا يُقرأ اليومَ شاهدُ الغد، والمقابلةُ لم تكن مقصودةً
وقتَ الحفظ.**
⚙️ **تُستدعى:** بعد كلّ نداءِ API أو أمرٍ ذي مخرَجٍ مركَّب.

**③ ويُعلَن التوقُّعُ في رأس الأداة قبل تشغيلها، وتمتنع الأداةُ عند الخلاف.**
🔎 **شاهدُها:** من الحالات التي أراها في سياقي، عددتُ **بيدي** ثمانيةً كُشفت
بتوقُّعٍ مُعلَنٍ خالف المقيس (٢٧ آب ٢٠٢٦) — **ولم يُقَس ذلك بأمر، ولم أرَ
فيها أداةً اشتكت من نفسها.**
⚠️ **ونسبةُ هذه الثمانيةِ إلى العشرِ في الرأس غيرُ مقيسة: عَدّان باليد في
مدَيَين لم يُقابَلا** — **فلا تُقرأ جزءًا منها ولا مدًى مستقلًّا حتّى
يُقابَلا.**
🔴 **وثمنُه هو قيمتُه:** توقُّعٌ يُكتب **بعد** القياس لا يفرّق بين معرفةٍ سبقت
وترتيبٍ لحق — **ولا يفرّق فيه صاحبُه على نفسه.**
⚙️ **تُستدعى:** قبل تشغيل أداةٍ يُبنى على رقمها قرار.

**④ وكلُّ قائمةٍ تُعلن عددَها، والعددُ يُقاس بأمرٍ مسمًّى في موضعه.**
🔎 **شاهدُها:** كُتب «ثلاثةُ صفوفٍ لا تقابل ملفًّا» وكانت أربعةً — سقط `١١٠`.
ثمّ كُتبت «أربعة» **فشاخت بالإيداع الذي كتبها**: أُغلق توسيعُ المدى في اللحظة
نفسِها فصارت خمسة. 🔴 **ومَن كشف الأولى سؤالٌ عن العدد، والثانيةَ إعمالُ
الأمرِ المسمّى — لا قراءةُ القائمة في الحالين.**
⚙️ **تُستدعى:** كلّما كُتبت قائمةٌ في نثرٍ أو تعليق.

**⑥ والبصمةُ تُسمّى بالأمر الذي صنعها، وإلّا لم تكن بصمة.**
🔎 **شاهدُها:** المحتوى نفسُه أعطى بصمتين صحيحتين مختلفتين —
`sha1` من `node:crypto` = `605597ee…` · و`git hash-object` = `034f34db…`
(لأنّ الثاني يُدخل ترويسةً في الحساب). ⇒ **فبصمةٌ بلا اسم أمرها لا تُقابَل
بشيء، والمقابلةُ هي كلُّ غرضها.**
⚙️ **تُستدعى:** عند إرسال أيّ بصمةٍ إلى الطرف الآخر. ✅ **وأختُها في مستوى
الملفّ مكتوبةٌ في حزمة ما قبل الدمج.**

**⑧ وما لُفَّ لفًّا ليّنًا يُصيَّر قبل الموافقة** — فالسطرُ على شاشتين اثنان.
🔎 **شاهدُها:** `فوق 80: 1` طُبع قبل الإيداع وقُرئ على أنّه السطرُ المُصلَح
للتوّ، والإصلاحُ كان قد دفع نصفَ السطر إلى التالي فبلغ ٨٨ (٢٥ آب ٢٠٢٦).
⚙️ **تُستدعى:** قبل إرسال نصٍّ يُحكَم على شكله.

**⑩ وفشلُ التنفيذ ليس قيمةً — ويُفصَل عن المخرَج في نوعه لا في نصّه.**
🔎 **شاهدُها:** دالّةٌ تعيد نصَّ الخطأ عند الفشل، و`git config --get-regexp`
يخرج بـ`1` حين **لا يجد** — فصار «لم أجد» نصًّا غيرَ فارغٍ فقُرئ «وجدتُ».
⇒ **حُوِّل النفيُ إلى إثبات، في أداةِ جردِ صلاحيّات.**
✅ **وهي أختُ `EXIT: 0` في «المحتوى ما بيمرّ بصدفة» مقلوبةً:** هناك نجاحٌ
كاذبٌ ستر فسادًا، وهنا فشلٌ صار قيمةً صادقة.
⚙️ **تُستدعى:** عند لفّ أيّ أمرٍ خارجيٍّ في دالّة.

**⑪ والأخضرُ يُعاد قياسُه كما يُعاد الأحمر — أو تُنتج الأداةُ شاهدَها فيُغني.**
🔎 **شاهدُها:** أُعيد قياسُ `extraheader` **لأنّ نتيجتَه أزعجت**، ولو قالت
الأداةُ «نظيف» لَشُحنت. ⇒ **شكٌّ غيرُ متماثل: يُستقصى الأحمرُ ويُصدَّق
الأخضر** — **وهو يبطل «الرقمُ الموافقُ أخطر»، إذ يجعل حراستَنا مشروطةً بأن
تعطب الأداةُ في الاتّجاه المزعج.** ✅ **والقاعدةُ ① أرخصُ منها وتُغني عنها.**
⚙️ **تُستدعى:** عند قراءة أيّ مخرَجٍ يوافق ما نرجوه.

**⑫ وتعليلٌ مقبولٌ في موضعِ قياسٍ ليس قياسًا.**
🔎 **شاهدُها:** `total_count: 3` وسطران باسم «الفحوص» كانا مطبوعَين في حزمةٍ
سابقة، فقُرئا وفُسّرا «مهمّتان في حزمةٍ واحدة» ولم يُقاسا. **والتفسيرُ معقولٌ
وكان خطأً** — وقُرئ السببُ لاحقًا من `on:` في `ci.yml`.
⚙️ **تُستدعى:** كلّما جاء في الذهن «هذا معناه أنّ…» أمام مخرَجٍ مطبوع.

**⑬ ودعوى عن فعلٍ سابقٍ تُلصَق بسطرها، لا تُروى من الذاكرة.**
🔎 **شاهدُها:** قيل «لم أُشغّل `push origin --delete` قطُّ»، والسجلُّ يقول
**اثني عشرَ فرعًا محذوفًا من الخادم**، آخرُها قبل القول بخمسٍ وتسعين دقيقة.
⇒ **وقُوبلت ذاكرةٌ بذاكرةٍ حيث كان يلزم أن يُقابَل نصٌّ بنصّ.**
⚙️ **تُستدعى:** قبل أيّ جملةٍ فعلُها ماضٍ وفاعلُها نحن.

**⑭ وحدُّ ما أراه ليس حدَّ ما جرى** — فيُقيَّد النفيُ بمداه المقيس.
🔎 **شاهدُها:** الدعوى أعلاه قيلت بقيد «في هذه الجلسة»، **والجلسةُ واحدةٌ
برقمٍ واحدٍ وملفٍّ واحد** — وما سبق طيَّها منها هو هي. ⇒ **فصار حدُّ السياق
حدًّا للواقع، ونُطق به نفيًا مطلقًا.**
🔴 **ولا يُعالَج بالانتباه: يقع عند كلّ طيّ.** ⇒ **«لا أثرَ له في ما أراه»
لا «لم يقع»، و«عددتُها بيدي» لا «عددُها كذا».**
⚙️ **تُستدعى:** عند كلّ نفيٍ أو إجمالٍ عن مدًى أوسعَ من السياق الحاضر.

**⑮ وأداةٌ تقيس مجالًا هي داخلَه تقرأ صدى نفسِها.**
🔎 **شاهدُها:** أداةُ مسحٍ على سجلّ الجلسة عدَّت مخرَجَ تشغيلها السابق —
٤٢ صفًّا وهميًّا، لأنّ السجلَّ يسجّل ما تطبعه. ✅ **واستُبعدت بالشكل (صفُّ
`git` يبدأ بـ`- [deleted]`) وطُبع المستبعَد ولم يُسقَط صمتًا.**
⚠️ **ومداها ضيّقٌ ويُقال: تخصّ القياسَ على السجلّ وما يشبهه، لا كلَّ أداة.**
⚙️ **تُستدعى:** حين يكون المقيسُ ملفًّا تكتب فيه أدواتُنا.

**⑯ وأداةُ الجرد تصنّف بالفعل لا بالشكل، وتطبع تصنيفَها — وتُسمّي وحدةَ ما
تعدّه: أبايتٌ أم حرفٌ أم عمودُ عرض؟**
🔎 **وشاهدُ الوحدة:** قِيس عرضُ سطور هذا القسم بـ`awk` فأعطى «١٤٣»، وبأداةٍ
تعدّ الحروف: **صفرٌ فوق ٨٠، وأطولُها ٨٠ بالضبط.** 🔴 **و`awk` يعدّ بايتات،
والحرفُ العربيُّ بايتان** — ⇒ **فكاد يُتَّهم نصٌّ بكسرِ قاعدةٍ يلتزمها، بأداةٍ
تعدّ وحدةً غيرَ الوحدة المقيسة.** ⚠️ **والثلاثةُ تختلف على العربيّة كلِّها،
لا في النادر منها.**
🔎 **شاهدُها:** `- [deleted]  X` يطبعه `push --delete` (كتابةٌ على الخادم)،
و`- [deleted]  (none) -> origin/X` يطبعه `fetch --prune` (تنظيفٌ محلّيّ لا
يمسّ الخادم). **والشكلُ واحد، والأثرُ نقيضان.** ⇒ **لو خُلطا لَصار العددُ
٢٣ بدل ١٢ — في تقريرٍ عن حدود صلاحيّاتنا.**
🔴 **والاتّجاهُ ليس صدفة: أدواتُ الجرد تُطابق أشكالًا، والأشكالُ تتشابه عبر
أفعالٍ مختلفةِ الأثر — فتُخطئ نحو التضخيم.**
⚙️ **تُستدعى:** عند كتابة أيّ مسحٍ يعدّ أفعالًا من آثارها النصّيّة.

**⑰ والإجمالُ يُقيَّد بمصدره كما يُقيَّد النفيُ بمداه.**
🔎 **شاهدُها:** «عشرُ حالاتٍ، وصفرٌ في شيفرة المشروع» — **العددُ زِيد بيدٍ
جولةً جولة ولم يُقَس، و«صفرٌ» نفيٌ مطلقٌ على مدًى لا يُرى كلُّه.** ⇒ **وهو
في أكثر أرقامنا تكرارًا وأشدِّها ثقة.**
✅ **ولا يُسحب الحكمُ لأجل عدده: يُقيَّد العددُ ويبقى الحكم.**
⚙️ **تُستدعى:** عند كتابة أيّ رقمٍ يُلخّص سلسلةً ما زالت تجري.

---

⚠️ **وهذا القسمُ نفسُه سلسلةٌ تجري، فعددُه أعلاه مقيَّدٌ بمصدره (⑰) —
وحدودُه أنّ كلَّ قاعدةٍ فيه قاعدةُ سلوكٍ لا حارسٌ يمنع.** 🔴 **ولا يُلبَّس
حارسًا: ما لا أثرَ له في المستودع لا يقرؤه اختبار** (البند «المحتوى ما بيمرّ
بصدفة»). ⇒ **وما استطاع منها أن يصير حارسًا فليصر (ح)، والباقي يُقرأ.**
