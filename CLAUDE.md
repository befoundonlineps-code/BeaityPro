# BeaityPro

## الوثائق — وين تدوّر

- شكل جدول أو عمود؟ [docs/DATABASE_DIAGRAM.md](docs/DATABASE_DIAGRAM.md)
- توقيع دالة، رمز رفض، `SECURITY DEFINER`، `search_path`؟
  [docs/db-functions.sql](docs/db-functions.sql)
- "ليش انعملت هيك"؟ [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md)
- سياق المشروع وحالته؟ [docs/PROJECT_HANDOFF.md](docs/PROJECT_HANDOFF.md)
- خلفية عن المجال؟ [docs/BEAUTY_PRO_REFERENCE.md](docs/BEAUTY_PRO_REFERENCE.md)

**الوثائق بتوجّهك، والقاعدة بتقرّر.** هاي الملفات صورة عن حالة، مش الحالة
نفسها. شغّل الاستعلام الرخيص وتأكّد قبل أي SQL جديد حتى لو الوثيقة واضحة —
وثيقة واثقة وغلط أسوأ من لا وثيقة، لأنها بتخليك تتخطّى التحقّق بثقة. هذا
صار فعلًا: نسخة قديمة من المخطط قادت لاستنتاج غلط عن حالة القاعدة، وللقول
إن التوثيق متأخر وهو مش متأخر.

أول أربعة بتتحدّث **بنفس الـcommit** تبع التغيير اللي غيّرهن. الـSQL مش
مُدار بالإصدارات هون، فالـcommit هو السجل الوحيد ولازم يحمل الاثنين مع بعض.
`BEAUTY_PRO_REFERENCE.md` مستثنى: بيوصف المجال لا النظام، فما بيتقادم لما
يتغيّر الكود.

`db-functions.sql` ما بينكتب بالإيد — بينولّد، والاستعلام برأس الملف.

## سكربتات قاعدة البيانات — قوانين ثابتة

المالك **ما عنده صلاحيات إدارية** على قاعدة البيانات (publishable key فقط).
كل تعديل على القاعدة بينفّذه هو يدويًا بمحرّر SQL. يعني: جهّز السكربت
واعرضه، ولا تفترض إنك تقدر تشغّله بنفسك.

### 1. لا تجمع DDL دائم مع `RAISE EXCEPTION` بنفس السكربت

الـDDL ببوستجرس **معاملة زي أي شي تاني**، ومحرّر SQL بينفّذ النص المُرسَل
كله بمعاملة ضمنية واحدة. فأي `RAISE EXCEPTION` — حتى لو مقصود عشان يرجّع
بيانات اختبار — بيُسقط المعاملة كاملة، **وبضمنها `CREATE OR REPLACE`
اللي فوقه**.

هذا صار فعلاً: سكربت أصلح دالة ثم فحصها بنجاح ثم محا إصلاحه، فبقيت
النسخة المكسورة حيّة والفحص بيقول "نجح". كلّف دورة تشخيص كاملة، وبدا
شكله زي تأخر كاش أو مشكلة بيانات — وما كان لا هذا ولا هذاك.

**الصح:** رجّع بيانات الاختبار عبر كتلة `begin … exception when others
then … end` داخلية. هاي معاملة فرعية، فالاستثناء جوّاها بيرجّع شغلها هي
بس والمعاملة الكاملة بتكمل:

```sql
begin
  perform my_function(...);
  select status::text into v_status from ... ;
  raise exception 'ROLLBACK_MARKER';
exception when others then
  if sqlerrm <> 'ROLLBACK_MARKER' then v_code := sqlstate; v_msg := sqlerrm; end if;
end;
```

**والنقطة اللي بتنفلت:** «الإرجاع بكتلة داخلية» يعني الإرجاع **عبر ما ينجو
منها** — لا الكتابة جوّاها. الكتلة معاملة فرعية، فعند التقاط الاستثناء
**بتُلغى كل تغييرات القاعدة اللي صارت جوّاها**، وبضمنها الإدراج بجدول
مؤقّت. جدول مؤقّت لسّه «حالة قاعدة». فلو راكمت التقرير جوّا الكتلة، بيرجع
`select` الأخير **فاضيًا** وإنت مفكّر إن الفحص ما اشتغل.

**اللي بينجو هو متغيّرات PL/pgSQL وحدها.** راكم بمصفوفة نصّية، وأدرجها
**بعد** الكتلة:

```sql
declare v_log text[] := '{}';
begin
  begin
    …
    v_log := v_log || format('① … cost=%s', v_cost);
    raise exception 'ROLLBACK_MARKER';
  exception when others then
    if sqlerrm <> 'ROLLBACK_MARKER' then
      v_log := v_log || ('💥 ' || sqlstate || ' — ' || sqlerrm);
    end if;
  end;
  insert into probe select unnest(v_log);   -- خارج الملغاة
end $$;
```

الكتلة الخارجية بلا `EXCEPTION` فمش معاملة فرعية، فاللي بعدها بيبقى.
و`RAISE NOTICE` ما بينعرض بمحرّر Supabase، وهذا بالضبط اللي بيغري
باستخدام الاستثناء من الأساس.

**وقانون مرافق: `auth.uid()` فاضية بمحرّر SQL.** المحرّر ما بيحمل JWT
وبيشتغل بدور المالك، فأي `where id = auth.uid()` بيرجع صفر صفوف وأول
`insert` بيسقط بـ`23502`. خُد الصالون بـ`select id from salons limit 1`.
**والأثر التاني أهم: RLS متجاوَزة بالكامل هناك** — فسكربت بينجح بالمحرّر
**ما بيثبت إشي عن العزل**، بيثبت الحساب فقط. صرّح فيها برأس السكربت،
لأن اللي بيقرأ نتيجته لاحقًا رح يظنّها أثبتت الاثنين.

### 2. افحص محتوى الدالة، لا وجودها

`select count(*) from pg_proc where proname = …` **عاجز بنيويًا** عن كشف
إصلاح انرجع — النسخة القديمة إلها نفس الاسم ونفس التوقيع بالضبط.

افحص `prosrc` نفسه:

```sql
select (length(prosrc) - length(replace(prosrc, ')::appointment_status', '')))
       / length(')::appointment_status')
from pg_proc where proname = '…';
```

وانتبه: أي عدّاد نصّي بيلقط كمان **التعليقات** جوّا الجسم. لو التعليق
بيذكر التحويل باسمه، العدّ بيزيد واحد. استخدم جزءًا ما بيظهر إلا
بالكود الفعلي (مثلاً القوس بـ`)::type`).

### 3. الفحص لازم ينفّذ الدالة فعلاً

PL/pgSQL بيخزّن الجسم **نصًا** وبيخطّط كل جملة أول مرة **تُنفَّذ**. فدالة
ممكن تُنشأ بنجاح وتفشل أول استدعاء. و"نجحت مرة" ما بتعني كل فروعها
سليمة — فرع `CASE` ما مرّ عليه التنفيذ لسّه ما انخطّط.

اختبر كل فرع بيكتب لعمود: الأساسي والمشروط وحلقات الإدراج.

### 4. الفحص الحاسم ما بيجوز يعتمد على مطابقة أسماء

`where name = 'قص شعر للأطفال'` بيفشل بصمت لأن الاسم الحقيقي
`'قص شعر للأطفال (حتى 6 سنوات)'`، فبينسكت الفحص كله. خُد صفًا حقيقيًا
وخُد خدمته وموظفه **من نفس الصف**، وخلّي البحث بالاسم للتشخيص فقط
(بـ`like`، وبيعرض الموجود فعلاً).

### 5. هوية الدالة = عدد المعاملات وأنواعها

إضافة معامل جديد — حتى لو إله `default` — بتعمل **overload** مش استبدال.
بتصير نسختان، والاستدعاء بيصير غامضًا. احذف القديمة صراحة:

```sql
drop function if exists my_function(uuid, timestamptz, uuid);
```

وتأكد إن `count(*) = 1` بعدها.

### 6. إعادة تسمية عمود بتكسر أجسام الدوال بصمت

الأجسام مخزّنة نصًا وبتنحلّ وقت التنفيذ. أي `rename column` بيلزمه إعادة
إنشاء كل دالة بتذكر العمود، **بنفس السكربت**.

## الكتابات — وين تنحطّ الضمانة

كل كتابة جديدة بتقع بوحدة من ثلاث، والفرق بينهن **مين بيتضرّر لو الكتابة
وقفت بالنص**:

1. **نداء عادي من العميل** لما تكون الكتابة صفًّا واحدًا. الصف بيكتب كله أو
   ما بيكتب — بوستجرس بيضمنها مجانًا، ودالة بتلفّها بتكرّر ضمانًا موجودًا.
2. **RPC** لما تكون صفوفًا متعددة **ويُشتقّ منها شي تاني**، أو لما تقرأ ثم
   تكتب بناءً على اللي قريته (`post_stock_document` عم بتعمل الاثنين).
3. **قيد أو مُشغّل بالقاعدة** لما تكون القاعدة عن **صحّة صف واحد** لا عن
   علاقة بين كتابات — لأن حارس بالواجهة بينحلّ بأول نداء من مكان تاني.

**والحد اللي بينفلت هو بين ١ و٢، وما بينحسم بعدد الصفوف.** جدولان الاثنان
بيتكتبوا بعدة نداءات، وواحد لزمه RPC والتاني لأ:

- `product_set_components` **بيان تعريفي**: وصف كتبه إنسان. لو انحذف السطر
  ونجح، وفشل الإدراج، بيرجع الطقم ناقص مكوّنات، **وما حدا اشتقّ إشي من الحالة
  الناقصة**، **وإعادة الحفظ بتصلحها لأن الفرق بينحسب من قراءة جديدة للجدول**
  لا من صورة عن اللي كان.

  ⚠️ **ومش لأنها «مرئية».** النافذة بتعرض اللي كتبه الشخص لا اللي بالجدول،
  فبعد فشل جزئي بيشوف قائمته كاملة وفوقها شريط خطأ، وما بيعرف أي صف نجا إلا
  لما يسكّر ويفتح. الرؤية ما كانت يومًا جزءًا من البرهان.
- `stock_movements` **سجلّ**: ما حدا بيقرأ الحركات، بيقرأوا **رصيدًا محسوبًا
  منها**. فنصف مستند بيخلّي رقمًا غلطًا، **مقنعًا**، ودائمًا — وما في شاشة
  بتقدر تعرضه كنقص لأنه بيبان رصيدًا سليمًا.

فالسؤال مش "كم صفًا؟" بل: **الحالة الناقصة مرئية وقابلة للتصحيح، ولا صامتة
ودائمة؟** ولما تختار ١ لكتابة متعددة، اكتب السبب فوق الدالة — بلا سطرين
بيلاقي اللي بعدك سابقتين متناقضتين وما فيه شي يفصل بينهن.

## المخزون — ثلاث قواعد لا تُخرَق

- **ما في رقم بلا وحدته قدّام إنسان — والوحدة قبل الرقم لا بعده.**

  المالك قرأ حركته ما تعرّف عليها: أدخل **٥ عبوات** والصفّ بيقول **٧٥**، فقال
  «ما بتذكر إني عملت تحويل 75». الاثنان صح — `entered_quantity = 5` و
  `entered_uom = package` و`quantity_base = 75`. **والمشكلة إن صاحب النظام
  نفسه ما تعرّف على حركته**، فموظّفة الاستقبال بعد ستة شهور أولى.

  **فكل كمّية بتنعرض بتحمل الإطار اللي انكتبت فيه:** `بالعبوة: 5 · بالقطعة: 75`
  — لا `75` وحدها (اللي أدخلها ما بيعرفها) ولا `5` وحدها (ما بتنجمع مع سطر
  انكتب بالقطعة). **الاثنان معًا وحدهما بيصدقوا مع القارئ ومع الحساب.**

  **والوحدة قبل الرقم** لأن `5 عبوة` غلط و`5 عبوات` صح، وما منريد فرعًا نحويًا
  — نفس قاعدة العدد فوق. `بالعبوة: 5` بتصحّ عند ١ و٢ و٥ و١٥.

  وبينسحب على الرصيد وشاشة الجرد وأي تقرير جاي. وهو أخو البند ٣١
  (`nominal_purchase_price` رقم بلا وحدة بيُقرأ بوجهين) بلبوس الكمّية بدل السعر.

## المخزون — القاعدتان الأصليتان

- **حركة مُسجَّلة خطأً تُعالَج بعكس مستندها ثم إعادة التسجيل. ما في تعديل
  لصفّ بـ`stock_movements` ولا لـ`unit_cost` عليه.** (ADR-051)

  السبب هو المبدأ الحاكم للموديول: **الرصيد مجموع حركات، لا عمودًا يُصحَّح.**
  والتعديل المباشر بيخالفه بتلات طرق: بيمحو اللي صار (الصرفيات مختومة برقم ما
  عاد إله مصدر) · بيكسر التتبّع (العكس بيسيب مستندين بيقولوا شو صار وليش) ·
  وبيفتح بابًا ما بينسكّر (ما دام مقبولًا مرّة، بيصير مقبولًا كل ما بدا الرصيد
  «غلط»). **وبقاء مستندين بدل واحد ميزة لا عيب** — التاريخ بيقول إن غلطًا صار
  وانصحّح، لا إنه ما صار.

- **المُشغّل اللي بيرفع رفضًا بيخصّ إنسانًا بيكتب `using hint = '…'` بلغة
  الإنسان.** الحقل بيوصل الواجهة، والشاشة بتعرضه لما ما يكون للرمز مفتاح
  مسمّى بعد. يعني **من يكتب مُشغّلًا اليوم بيحصل على رسالة مفهومة من لحظة
  كتابته**، بلا مفتاح ترجمة وبلا انتظار جولة مراجعة.

  والترتيب: **مفتاح مسمّى ← `hint` ← الجملة العامّة.** المفتاح أولًا لأنه
  قابل للترجمة و`hint` عربي ساكن بالقاعدة. والشرط **وجود `hint`** لا وجود
  رسالة — استثناء داخلي برسالة تقنية بلا `hint` ما بينعرض كأنه مكتوب لحدا.

  **وكل الشاشات بتستعمل `dbErrorSentence(error, t, context, overrides)`** من
  [lib/dbErrors.js](lib/dbErrors.js) — **طريق واحد، مش تلاتة**. إرجاع مفتاح ما
  بيقدر إلا يرمي الـ`hint`، فلازم المُساعِد ياخد `t` ويرجّع جملة.

  والحارس بـ[lib/oneErrorEntryPoint.test.js](lib/oneErrorEntryPoint.test.js)
  بيمنع رجوع الشوكة: أي شاشة بتلفّ مفتاح خطأ بـ`t()` بإيدها بتسقّطه.

## البناء والسيرفر

- **`next build` بيكتب فوق نفس `.next` اللي سيرفر التطوير عم يخدم منه**، فأي
  بناء والسيرفر شغّال بيخلّي كل المسارات `500`
  (`Cannot find module './chunks/vendor-chunks/next.js'`). صار.

  **الحل مش قاعدة تتذكّرها — مجلّد منفصل:**

  ```
  NEXT_BUILD_DIR=.next-check npx next build
  ```

  `next.config.js` بيقرأ المتغيّر، وبلاه بيبقى `.next` العادي فنشر الإنتاج ما
  بيتغيّر. **القاعدة بتنُنسى، والمسار المنفصل ما بينُنسى.**

- **⚠️ و`next build` ما بيمسك اللي بيبان إنه بيمسكه — مقيس.** حُقن خطاف مشروط
  (`if (…) useState()`) و**نجح البناء**: ما في ESLint مثبَّت ولا TypeScript،
  وكل المسارات `ƒ` فما بتنرسم وقت البناء. يعني خطوة «Linting and checking
  validity of types» فاضية عمليًا. **اللي بيسدّ الفجوة `eslint` +
  `eslint-config-next`** (فيه `rules-of-hooks`) — بند ٣٢.

## الواجهة

- كل نص جديد بالواجهة بيجي من ملفات i18n من أول سطر كود — صفر عربي مكتوب
  بالكود.
- **إضافة مفتاح ترجمة فعل من خطوتين، لا خطوة وقاعدة تتذكّرها: تكتب المفتاح،
  وتعيد تشغيل السيرفر بنفس الخطوة.** `next-i18next` بيقرأ `public/locales/`
  **مرة واحدة عند الإقلاع**، فمفتاح تضيفه والسيرفر شغّال بيوصل المتصفح
  **باسمه خامًا** (`categoryDialog.typeInherit`) رغم إنه بالملف وبالـcommit —
  وبيبان تمامًا زي مفتاح ناقص، فبتدوّر بالمكان الغلط.

  **صار خمس مرات، وهذا مش نسيانًا بل قاعدة موضوعة بالمكان الخطأ.** اللي
  بينُسى خمس مرات ما بينعالج بكتابته سادسة — بينعالج بجعله جزءًا من الفعل.
  اقرنهن: `Write` على ملف اللغة ⇐ `preview_stop` + `preview_start` فورًا،
  قبل أي قياس.

  **وبعد إعادة التشغيل، افحص العقد المرسومة لا حمولة `__NEXT_DATA__`.** كل
  الـnamespace بينتسلسل جوّا الحمولة، فـ`includes()` على الصفحة الخام بتلاقي
  **القاموس** وبتقول ✓ عن نص ما انرسم أبدًا — صار، وقال ✓ أربع مرات كذبًا.
  جرّد `<script>` أولًا. **وفحص كاذب أسوأ من غياب فحص**، زي تعليق صحيح صار
  خاطئًا بالضبط: الغياب بيدفعك تقرأ، والكذب بيعفيك.

- **العدد بالعربية ما بينحطّ بمحلّ بيحكمه إعراب.** المعدود بيوافق عدده بخمس
  صور (`مسؤول واحد` · `مسؤولان` · `خمسة مسؤولين` · `١٥ مسؤولًا` ·
  `٢٥٠ مسؤولٍ`)، ونصّ واحد ما بيرضيهن كلهن. `"{{n}} مسؤولًا"` صحيحة من ١١
  لـ٩٩ وغلط بكل ما عداها — وبضمنها ١ و٢، وهنّ أشيع الأعداد بجملة تأكيد.
  ووصلت الشاشة فعلًا.

  **والحل لا صيغ جمع ولا `count` بـi18next** — ست صور جمع عربية بمفتاح ناقص
  بترجعك لنفس المكان. الحل: اكتب اللي ما بيتغيّر بتغيّر العدد، وحطّ الرقم بعد
  نقطتين أو جوّا قوسين:

  ```
  ✗  لهذا المستودع {{n}} مسؤولًا ماليًا
  ✓  لهذا المستودع مسؤولون ماليًا مسجّلون (العدد: {{n}})
  ```

  **رمز الوحدة استثناء** (`د`, `مل`, `كغ`) — اختصار بلا جمع ولا إعراب، فـ`3 د`
  و`45 د` صحيحتان. والوحدة المكتوبة كاملة **ليست** استثناءً: `3 دقيقة` غلط.

  والحارس بـ[lib/arabicCounts.test.js](lib/arabicCounts.test.js) بيمسح كل
  ملفات اللغة، لأن سطرًا هون بينقرأ مرة والجملة الجاية فيها رقم بينكتبها حدا
  بعد شهر ما شافه.

- **صيغة المخاطبة: النصّ الجديد ما بيخاطب أحدًا بجنس.** الحالة مقيسة —
  **١١٠ نصًّا بيخاطبوا المستخدم، كلهم بالمذكّر، وصفر بالمؤنّث وصفر مختلط**
  عبر تمان namespaces. يعني ما في ارتجال، في **قرار انتُخذ بالتقصير** ولا حدا
  اتخذه: أغلب صاحبات الصالونات نساء.

  **للنصّ الجديد:** استعمل المصدر لا الأمر — «إضافة مستودع» لا «ضيف مستودع»،
  و«يمكن البدء بمستودع عام واحد» لا «ابدأ». هيك ما بتخاطب أحدًا فما بتخطئ
  فيه — **نفس حيلة قاعدة العدد فوق: أخرج الكلمة من موضع الإلزام.**

  **⚠️ وتحويل الـ١١٠ القائمة تغيير صوت للمنتج كله، لا لموديول.** تحويل
  namespace واحد بيصنع بالضبط الارتجال اللي عم نتجنّبه: شاشة محايدة وجارتها
  بالمذكّر. فهو بند مستقل يمشي على الملفات التمانية دفعة وحدة، وقرار المالك
  لأن الصيغة المحايدة أطول بكل تلميح.
- **أي مدى ساعات بيمرّ على [components/TimeRange.js](components/TimeRange.js)
  — ولا تكتب واحدًا بإيدك.** الأرقام اللاتينية أوروبية (EN) والفاصل بينها
  محايد، فخوارزمية bidi بتعطي المحايد اتجاه الفقرة وبتبدّل النصفين: المخزَّن
  `10:44 – 11:29` والمرسوم `11:29 – 10:44`. النص والـDOM سليمان، والمقلوب هو
  الرسم — فما بينكشف بقراءة أي قيمة، وهيك ستّ نسخ محلية غلطت وحدة وحدة.
  العازل جوّا المكوّن، وعلى عنصر سطري دائمًا: `dir` على عنصر كتلة بيحلّ
  `text-align: start` باتجاه `ltr` وبيهرّب المحتوى للطرف التاني.

  **العازل بيشتغل على EN بس.** الأرقام العربية-الهندية صنفها AN، وAN **ما
  بينحلّ باتجاه الأب إطلاقًا** — `١٠:٤٤ – ١١:٢٩` بتنرسم نفس الشي مع العازل
  وبدونه، مقيس. اللغة مثبّتة على `ar` (بترجع أرقامًا لاتينية)، بينما `ar-EG`
  و`ar-PS` بيرجّعوا عربية-هندية. لو انضافت لغة تانية أو انكتب
  `toLocaleTimeString()` بلا معامل، المكوّن بيسكت بصمت والحارس بيضلّ ناجحًا —
  فـ[lib/localePinned.test.js](lib/localePinned.test.js) بيمنع الاثنين.

  **ومدى فيه نص عربي قويّ ما بينلمس.** `9:00 ص – 6:00 م` و
  `2 – 8 أغسطس 2026` بينرسموا صح لحالهم، والعازل **بيكسرهم** — مقيس. ولا
  تحطّ `⁦…⁩` بملفات الترجمة كمان: CLDR بيزرع علاماته لما يشوفها لازمة
  وقرر ما يزرعها هون، وودجت فيها مدى معزول وجنبه مدى غير معزول أسوأ من
  الاثنين.

  الحارس بـ[lib/timeRangeDirection.test.js](lib/timeRangeDirection.test.js)،
  وما بيعدّ ملفات بالاسم عمدًا — بيتأكّد إنه ما في إلا مُنسّق واحد وعازل واحد.

  وأي ادّعاء عن رسم أو تخطيط لازمه قياس من محرّك حقيقي (Chrome headless
  بلقطة)، مش فحصًا منطقيًا — لوح المتصفح المدمج بيرجّع كل المستطيلات صفرًا
  فبيجاوب غلط بدل ما يفشل.
- أخطاء قاعدة البيانات ما بتوصل الشاشة خام: مرّرها على
  `reportDbError(error, context)` من [lib/dbErrors.js](lib/dbErrors.js) —
  بترجع مفتاح ترجمة، وبتسجّل غير المعروف بالكونسول بدل ما يضيع.

## الاختبارات

- **اختبار بيكشف العطل بتعليق التشغيل بيعمي كل اختبار تحته بنفس الملف.**
  حلقة متزامنة ما بتسلّم الخيط بتخلّي مهلة Jest عاجزة عن الانطلاق أصلًا، فاللي
  بيصير مش "فشل" بل **تجمّد**: `exit=124` بصفر مخرجات. وهاد بينقرأ كجهاز بطيء
  أو CI مزاجي قبل ما ينقرأ كحارس مكسور، وأسوأ نهاياته إنه ينحذف عشان الحزمة
  تمشي. **والأخطر إنه بيظلّل:** Jest بيوقف الملف عند التعليق، فأي اختبار بعده
  **ما بيشتغل** — يعني أي طفرة شُغّلت على اختبارات تحته **كانت بتثبت لا شي**.

  صار فعلًا: `isCategoryArchived` كان محروسًا باختبار بيمرّر كائنًا عاديًا
  ويتوقّع `false`. الطفرة علّقت ١٢٠ ثانية بصفر مخرجات، وحارس أقوى مكتوب تحته
  ما وصل يشتغل. الحل: خلّي المسار **مقيسًا** — كائن بـ`getter` بيعدّ القراءات
  ويرمي باسم صريح بعد سقف معقول. صار `exit=1` بخمس ثوانٍ ورسالة مسمّاة.

- **⚠️ صفحة الفحص بتقيس المكوّن، وما بتقيس إن اللي بيوصله هو اللي بيوصل من
  القاعدة.** ولمّا يكون الفرق **بالشكل لا بالمنطق**، بتمرّ خضراء وهي عمياء.

  صار تلات مرات بجولة وحدة، وكلها انكشفت من لقطات المالك لا من فحوصي:
  مستند عكس صنعته بإيدي **بلا حركات** (والحقيقي بيكتب حركة) · تاريخ كتبته نصًّا
  `'2026-08-04'` (والحقيقي `timestamptz`: `2026-08-04T00:00:00+00:00`) · وشاشة
  رسمتها بمفاتيح ما كانت محمَّلة.

  **الدواء مش عين بشرية كل جولة، دواءان:**
  1. **بيانات الفحص تجيها من الدوال نفسها حيث أمكن** — استدعِ الدالة واقرأ
     نتيجتها، لا تصنع صفًّا **بيشبه** نتيجتها. وحيث ما بيمكن (RLS)، **انسخ
     الأشكال الحرفية من صفوف المالك** ولا تخترعها.
  2. **حارس المفتاح الخام**، وهو اللي كان بيمسك المفتاحين اللي وصلوا شاشة
     المالك: بعد الرسم، **أي عقدة نصّية بتطابق
     `/^[a-z][A-Za-z0-9]*(\.[A-Za-z0-9_]+)+$/` هي مفتاح خام**. سطر واحد،
     ما بيلزمه يعرف مفاتيحك، وبيشتغل على أي شاشة — وبيمسك **الحالتين معًا**:
     مفتاح ناقص من الملف، ومفتاح موجود والسيرفر ما انعاد تشغيله.

  و[lib/translationKeys.test.js](lib/translationKeys.test.js) بيسدّ النصّ
  الأول (المفاتيح الحرفية + القوالب بقائمة صريحة)، فبيبقى المفتاح الخام
  عالشاشة معناه **«أعِد التشغيل»** لا شي تاني.

- **الفحص اللي بيقول ✓ ما بينصدَّق قبل ما تشغّله على حالة لازم يسقط فيها.**
  معدّل الخطأ على طبقة الفحص أعلى من معدّله على الكود — مقيس بهالجلسة: ثلاث
  ✗ كاذبة بجولة وحدة (`.opacity-75` جوّا `<style>` · تعبير أقصر من أيقونة SVG
  · `disabled:pointer-events-none` بقائمة الأصناف)، **و✓ كاذبتين قبلهن**
  (`__NEXT_DATA__` قال ✓ أربع مرات، وطفرة «ما قتلت شي» وهي ما انطبّقت أصلًا).

  **والاتجاه اللي بيسقط فيه الفحص صدفة لا خاصيّة** — ✗ كاذبة بتكلّف وقت، و✓
  كاذبة بتشحن علّة. فالعادة: **بيّنة مضادّة قبل التصديق.** انزع الشي اللي عم
  تفحصه وشغّل الفحص: لو ضلّ ✓ فهو أعمى. صار مع حارس العدد (`_few` بيمرّ ·
  بلا لاحقة بيسقط · `sectionOther` بيسقط) ومع عمود الاتجاه، وكانوا أنظف
  قياسين بالجلسة.

  **وجرّد `<script>` و`<style>` قبل أي بحث نصّي بمخرجات المتصفّح** — الأول
  فيه القاموس والتاني فيه أسماء الأصناف، وكلاهما كذب بفحص هون.

- **اللقطة اللي بتنلتقط وما بتنقرا صورة لا قياس — واللي بتنقرا وما بتنذكر أسوأ.**
  صار: لقطة فحص فيها `Unhandled Runtime Error` بأعلاها انضمّت لتقرير بيقول
  «أربع حالات مقيسة بمحرّك حقيقي»، والخطأ ما انذكر. أنا قريته وشخّصته وصلّحته
  قبل ما أبعت — **وهاد بالضبط اللي بيخلّي السكوت عنه أسوأ:** اللي بيقرأ التقرير
  ما بيقدر يميّز «شفته وعالجته» عن «ما نظرت أصلًا»، فبيحسب القياس أنظف مما كان.

  والقاعدة: **اذكر اللي ظهر بالقياس، وبضمنه اللي صلّحته قبل ما تعرضه.** ومن
  صنف واحد مع فحص `__NEXT_DATA__` فوق — الاثنان إجراء بيطلّع ✓ بلا ما يبصّ على
  اللي بيبصّ عليه الإنسان.

- **الحارس اللي بيعدّ المعروف بيفشل مفتوحًا، واللي بيوصف الشكل بيفشل مغلقًا.**
  دفعنا ثمن هالدرس تلات مرات بنفس الموديول، بتلات ألبسة:

  1. **«اكتب الشرط لا العدد»** — تحقّق مبني على `count(*) = 5` بينكسر بأول مفتاح
     خامس مشروع، والشرط (`سطران ينتهيان بـREFERENCES products(id, kind)`) ما
     بينكسر.
  2. **«القاعدة لا الحصر»** — ترويسة `productAdminIO.js` كانت بتعدّ الجداول
     («ما في حذف بهاد الملف»)، فأبطلتها أول دالة حذف انكتبت بعدها. صارت تقول
     القاعدة: صفّ يشير إليه سجلّ يُؤرشَف، وصفّ تعريفي لا يشير إليه شي يُحذف.
  3. **«الشكل لا قائمة الأسماء»** — حارس نحو العدد كان بيطابق أسماء متغيّرات
     (`count`, `n`)، فأوّل مفتاح انكتب بعده استعمل `{{lines}}` **ومرق من
     تحته**. صار يطابق **الشكل**: `{{أي شي}}` وبعده كلمة عربية.

  **والاتجاه هو كل شي:** قائمة بـ«اللي ممنوع» بتفشل مفتوحة — كل حالة جديدة
  ثغرة صامتة. وقائمة بـ«اللي مستثنى» بتفشل مغلقة — الحالة الجديدة بتنمسك،
  وحدا بيبصّ عليها مرّة وبيضيفها بإيده. **لما تكتب حارسًا، اسأل: شو بيصير لما
  تظهر حالة ما خطرت عالبال؟**

- **الطفرة اللي ما بتقتل اختبارًا بتقول إن اختباراتك ناقصة، لا إن الكود سليم.**
  صار مرتين بنفس الجلسة: حارس دورة ما كان بينقتل لأنه غير قابل للوصول أصلًا،
  وترشيح شجرة قبل البناء مقابل بعده مرّ بكل الاختبارات لأن كلها حطّت الحالة
  المؤرشفة **بمستوى الجذر** — والفرق ما بيظهر إلا لما تكون متداخلة تحت جذر حيّ.
  لما طفرة تمرّ، دوّر على الحالة اللي الاختبارات ما وصلتها قبل ما تستنتج إن
  الكود متين.

## سير العمل

- فرع واحد لكل تغيير، ودمج بعد تأكيد المالك للاختبار بالمتصفح.
- خطوات صغيرة متدرّجة، والشرح بالعربي البسيط.
