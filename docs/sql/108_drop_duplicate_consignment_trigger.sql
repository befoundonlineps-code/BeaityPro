-- ==========================================================================
-- 108 -- CHANGE ONLY. No standalone SELECT in this file. Verification is 108b.
--
-- PREPARED, NOT RUN BY ME. The owner executes it.
--
-- 🔴 مشغّلٌ فائضٌ على `products`، لا يديره أيُّ ملفٍّ في `docs/sql`
--
-- **مشغّلان على نفس الجدول يستدعيان نفسَ الدالّة، وكلاهما مفعّل:**
--
--   freeze_consignment_after_use      BEFORE UPDATE ON products FOR EACH ROW
--                                     EXECUTE FUNCTION freeze_consignment_after_use()
--   trg_freeze_consignment_after_use  BEFORE UPDATE ON products FOR EACH ROW
--                                     EXECUTE FUNCTION freeze_consignment_after_use()
--
-- ✅ **متطابقان حرفًا عدا الاسم، مقيسًا بـ`pg_get_triggerdef` على قاعدة المالك**
-- — لا استنتاجًا من تشابه الاسم. **والدالّةُ فرعاها `raise exception` ولا كتابةَ
-- في أيٍّ منهما** ⇒ **فالتكرارُ متكافئٌ ولا يفسد بيانات، وكلفتُه استعلامان
-- زائدان لكلّ تعديلِ صفٍّ على `products`.**
--
-- ⚠️ **ولا يجعل ذلك بقاءَه مقبولًا:** كائنٌ لا يعرف أحدٌ لماذا هو موجود **يُغري
-- بحذف الخطأ منهما** — وأوّلُ من يقرأ الاثنين يرجّح أن الفائضَ هو المسمَّى في
-- المستودع، وهو العكس.
--
-- ---------------------------------------------------------------------------
-- 🔴 وأيُّهما الفائض — مقيسٌ بالمُعرِّفات، لا مرجَّحٌ بالاسم
--
-- **٠٧٩أ ينشئ كائناتِه بثلاث صيغٍ، وواحدةٌ منها وحدَها تصلح ختمًا زمنيًّا:**
--
--   152  create or replace view      ⟵ **يحفظ المُعرِّف** لو كان الكائنُ قائمًا
--   178  create or replace function  ⟵ **يحفظ المُعرِّف** كذلك
--   292  drop trigger if exists freeze_consignment_after_use on public.products;
--   294  create trigger freeze_consignment_after_use          ⟵ **مُعرِّفٌ جديدٌ قطعًا**
--
-- ⇒ **فالمشغّلُ غيرُ المسبوق وحدَه مختومٌ بتشغيل ٠٧٩أ**، وهو المرتكز.
-- ⚠️ **ومُعرِّفُ الـview لا يصلح مرتكزًا** — لو كان قائمًا قبل ٠٧٩أ لبقي مُعرِّفُه
-- قديمًا، **فتُقرأ المقارنةُ تأكيدًا وهي صمت.**
--
-- **والمقيسُ على قاعدة المالك:**
--
--   products                          جدول   19552
--   freeze_consignment_after_use()    دالّة   19748
--   trg_freeze_consignment_after_use  مشغّل   19749   ⟵ يلي الدالّةَ بواحد
--   stock_document_liveness           view    20114
--   freeze_consignment_after_use      مشغّل   20126   ⟵ ٣٧٨ بعد الدالّة
--   stock_fine_voidness               view    20240   ⟵ ١٠٦، اليوم
--
--   ① `19749 < 20126` ⇒ **`trg_` سبق تشغيلَ ٠٧٩أ.**
--   ② **والفجوةُ تحسم الباقي:** الدالّةُ والمشغّلُ `trg_` متجاوران بواحد ⇒
--      **وُلدا في معاملةٍ واحدة**، وبين الدالّة ومشغّل ٠٧٩أ ٣٧٨ ⇒ **فالدالّةُ
--      استُبدلت في ٠٧٩أ ولم تُنشأ فيه.** وحجّةُ «لا مشغّلَ بلا دالّة» صارت
--      مقيسةً لا مستنتَجة.
--
-- ⇒ **فالصورة:** الدالّةُ و`trg_…` وُلدا معًا في عهد المحرّر (قبل `docs/sql`) ·
-- ثمّ شُغّل ٠٧٩أ فاستبدل الدالّةَ وأنشأ مشغّلًا ثانيًا بلا بادئة ·
-- 🔴 **و`drop trigger if exists` فيه يسمّي غيرَ المسبوق وحدَه، فلم يرَ `trg_…`
-- إطلاقًا.** ⇒ **`if exists` تحمي من التكرار لا من التزاحم:** الملفُّ عديمُ الأثر
-- على نفسِه، **أعمى عمّا لم ينشئه** — ولو أُعيد تشغيلُه ألفَ مرّةٍ لبقي الفائض.
--
-- ⚠️ **وحدُّ الأداة يُقال، ولا يُبنى عليه حذفٌ وحدَه:** المُعرِّفاتُ تُصرف من
-- عدّادٍ تصاعديّ، **فترتيبُها ترتيبُ إنشاءٍ تقريبيّ** — كائناتُ معاملةٍ واحدةٍ
-- تتجاور، والعدّادُ يلتفّ نظريًّا. **⇒ مرجِّحٌ قويٌّ لا برهان.** والذي يجيز
-- الحذفَ هو الثلاثةُ أدناه مجتمعةً، لا المُعرِّفاتُ وحدَها.
--
-- ---------------------------------------------------------------------------
-- ✅ وثلاثةٌ تجعل الحذفَ آمنًا، كلُّها مقيسةٌ على قاعدة المالك
--
--   ① `pg_get_triggerdef` للاثنين **متطابقٌ حرفًا عدا الاسم** ⇒ فلا سلوكَ يضيع.
--   ② والدالّةُ **لا تكتب شيئًا** — فرعان كلاهما يرفع استثناءً ⇒ **التكرارُ
--      متكافئٌ**، وإسقاطُ أحدهما لا يغيّر ما يُقبل ولا ما يُرفض.
--   ③ والباقي `freeze_consignment_after_use` هو **الذي يديره ٠٧٩أ باسمه** ⇒
--      **فالحذفُ يعيد الحالةَ إلى ما يصفه المستودع**، ولا يُنشئ حالةً جديدة.
--
-- 🔴 **ولهذا يُحذف المسبوقُ لا الآخر، وذلك ليس ذوقًا:** حذفُ غيرِ المسبوق
-- **يُبقي كائنًا لا ملفَّ له، ويعيد أوّلُ تشغيلٍ لـ٠٧٩أ الاثنين من جديد.**
--
-- ---------------------------------------------------------------------------
-- ✅ والاسترجاعُ سطرٌ واحدٌ إن ظهر له سببٌ لم نره
--
--     create trigger trg_freeze_consignment_after_use
--       before update on public.products
--       for each row
--       execute function public.freeze_consignment_after_use();
--
-- ⚠️ **والسطرُ أعلاه مكتوبٌ من قراءةِ `pg_get_triggerdef` المنقولةِ في المراجعة،
-- لا من مخرَجٍ خامٍّ بين يديّ.** ⇒ **والصيغةُ القانونيّةُ الدقيقةُ تبقى متاحةً
-- بعد الحذف بلا خسارة:** `108b` يطبع `pg_get_triggerdef` **للباقي**، والاثنان
-- مقيسان متطابقين عدا الاسم — **فالباقي قالبُ المحذوفِ حرفًا، ولا يلزم إلّا
-- تبديلُ الاسم.**
-- ==========================================================================

-- ⚠️ `if exists` هنا في محلِّها **لأن الخطرَ تكرارُ التشغيل لا التزاحم** — وهو
-- عينُ التمييز الذي أنتج هذا الملفّ: تحمي من إعادة تشغيلِ هذا السكربت، **ولا
-- تحمي — ولا يُتوقَّع منها — من كائنٍ أنشأته يدٌ أخرى باسمٍ آخر.**
drop trigger if exists trg_freeze_consignment_after_use on public.products;
