-- ==========================================================================
-- 106 -- CHANGE ONLY. No standalone SELECT in this file. Verification is 106b.
--
-- PREPARED, NOT RUN BY ME. The owner executes it.
--
-- ---------------------------------------------------------------------------
-- ما الذي يبنيه هذا الملفّ، ولماذا الآن
--
-- 🔴 **قاعدةٌ مصمَّمةٌ منذ ٠٥٦أ ولم تُنفَّذ قطّ.** تعليقُ الجدول نفسِه يقول:
--
--   «غرامةٌ عُكس مستندُها لاغية، وذلك يُشتقّ من
--    stock_documents.reverses_document_id لا يُخزَّن — حالةٌ أقلّ، وحقيقةٌ لا
--    تتقادم عمّا تصفه.»
--
-- ⚠️ **والاشتقاقُ لا وجودَ له في أيّ موضع، مقيسًا:** `reverses_document_id`
-- مستعمَلٌ في عشراتِ المواضع (قائمةُ المستندات · documentCancellation · وview
-- `stock_document_liveness` المبنيُّ لهذا السؤال بعينه في ٠٧٩أ)، **ولا واحدٌ
-- منها يصل `stock_fines`.** والواجهةُ فيها موضعٌ واحدٌ يذكر `stock_fines` وهو
-- **تعليق**.
--
-- ⇒ **فالقاعدةُ تعيش في جملةٍ داخل تعليق عمود، وأوّلُ قارئٍ يُكتب سيقرّر
-- بنفسه هل يحترمها.** و`select … where employee_id = …` الطبيعيُّ **يرجع
-- اللاغيةَ كالقائمة.** هذا الملفُّ يجعل الاحترامَ هو الطريقَ الأقصر.
--
-- ---------------------------------------------------------------------------
-- 🔴 يَسِمُ ولا يُصفّي — وهذا قرارٌ مأخوذٌ من ٠٧٩أ حرفًا لا اختراعٌ جديد
--
-- تعليقُ `stock_document_liveness` يقول: «بيحمل الجواب عمودًا لا بيصفّي صفوفًا،
-- لأن قائمة المستندات لازم تضلّ تعرض المعكوس وعاكسه». **والغراماتُ أشدّ:**
-- شاشةٌ تُخفي غرامةً أُلغيت **تمنع الإنسانَ من رؤية أنه بُرِّئ** — وهو ما يهمّه
-- أكثرَ من كونها لم تعد محسوبة.
--
-- ---------------------------------------------------------------------------
-- 🔴 استعلامٌ قياسيٌّ فرعيّ، لا `join` — والفرقُ يمنع عطلًا محذَّرًا منه أصلًا
--
-- ⚠️ **تعليقُ ٠٧٩أ يحمل التحذيرَ بنفسه:** «الوصلةُ اليسرى ما بتتفرّع إلا لأن في
-- فهرس تفرّد على `reverses_document_id` مصدرُه ملفٌّ تاني، ولو راح الفهرس بيرجع
-- المستند مرّتين بلا خطأ بأي مكان — والمنطق ما بيتأثّر والعدّ بيتضاعف بصمت».
--
-- **و`join` على ذلك الـview كان سينقل التفرّعَ إلى هنا:** غرامةٌ واحدةٌ تظهر
-- صفَّين، **وكلُّ عدٍّ عليها يتضاعف بلا خطأٍ في أيّ مكان.**
--
-- ⇒ **والاستعلامُ القياسيُّ الفرعيُّ يفشل مغلقًا:** أكثرُ من صفٍّ يرفع
-- `21000 more than one row returned by a subquery` — **صرخةٌ بدل عدٍّ مضاعف.**
-- ✅ **ويبقى مصدرُ القاعدة واحدًا** (`stock_document_liveness`)، فلا نسخةَ
-- ثانيةٌ من منطق الحيويّة تتباعد عنه — وهي العلّةُ التي بُني ذلك الـview لأجلها.
--
-- ---------------------------------------------------------------------------
-- ⚠️ ولا `coalesce` على `is_live` — والحجّةُ صُحِّحت بعد مراجعةٍ نفّذت الـview
--
-- 🔴 **كُتب هنا أوّلًا: «`document_id` إجباريٌّ ومفتاحُه RESTRICT فالعدمُ
-- مستحيل». والمراجعُ نفّذ الـview على postgres محلّيّ بصفٍّ لا مقابلَ له في
-- الحيويّة، فعاد `is_void` عدمًا بصمتٍ تامّ.** ⇒ **والحجّةُ كانت تُثبت شيئًا
-- آخر:** المفتاحُ يضمن وجودَ المستند، **لا وجودَ صفٍّ مطابقٍ له في**
-- `stock_document_liveness`. **وهما ليسا نفسَ العدم.**
--
-- **والتطابقُ مضمونٌ بشرطين، وكلاهما مقروءٌ من ملفٍّ لا من القاعدة:**
--
--   (أ) `stock_document_liveness` **إسقاطٌ كاملٌ بلا `where`** — ٠٧٩أ:١٦٣
--       `from public.stock_documents d left join … r`، فكلُّ مستندٍ له صفّ
--   (ب) و`stock_fines_document_id_fkey` **مركّبٌ على `(document_id, salon_id)`**
--       بعد ٠٦٠أ، **فلا يمكن أن يسمّي المستندَ بصالونٍ آخر**
--   (ج) و`document_id` **`not null`** — 🔴 **وهذا الشرطُ كان ناقصًا من الحجّة**،
--       وقاسه المراجع: عدمٌ في العمود يجعل `l.document_id = f.document_id`
--       **غيرَ محقَّقٍ أبدًا**، فيعود `is_void` عدمًا. **والمفتاحُ المركّب لا
--       يسدُّها:** `MATCH SIMPLE` يمرّر العدمَ مجّانًا — وهي نفسُ الملاحظة
--       المكتوبة في `DATABASE_DIAGRAM` عن `storage_responsibles`.
--       ⚠️ **ولا يظهر في `pg_constraint` إطلاقًا**، فيُقاس بـ`attnotnull`
--       في `106b`.
--
-- 🔴 **و(ب) لا سطرَ له في `docs/sql/README.md` إطلاقًا** — فحالةُ تشغيله
-- **مجهولةٌ عندي**، والقاعدةُ تقول إنها لا تُستنتج من محادثة. **ولو لم يُشغَّل
-- ٠٦٠أ لبقي المفتاحُ بسيطًا، ولأمكن العدمُ الذي قاسه المراجع.**
--
-- ⇒ **فلا تُفترض الثلاثة: تُقاس.** `106b` يقرأ تعريفَ الـview من القاعدة،
-- ويقرأ قيودَ `stock_fines` كلَّها، ويسرد أعمدتَها **بـ`NOT NULL` ملصقةً**.
-- **وعدُّ الغراماتِ اليتيمةِ في `106c` منفصلًا** — لأنه يقرأ الجدولين فعلًا،
-- **فغيابُ الـview كان يُسقط ملفَّ التحقّق كلَّه بـ`42P01`** ويمنع طبعَ الشاهد.
-- **والعدمُ يبقى بلا `coalesce`** لأن سترَه هو ما يخفي هذه الحالةَ بالضبط:
-- شاشةٌ ترشّح `where is_void = true` **أو** `= false` تُسقطها من الاثنين معًا.
--
-- ---------------------------------------------------------------------------
-- ⚠️ ولا عربيَّ يُقرأ راجعًا في هذا الملفّ، وذلك مقصودٌ لا سهو
--
-- قاعدةُ `CLAUDE.md`: «أي سكربت بيودع نصًّا عربيًّا بيحمل `select` بيقرأه
-- راجعًا». **وقاعدةٌ ثانيةٌ تمنعه هنا: «ما في ملف بيجمع DDL مع استعلام تحقّق»**
-- — والحارسُ `sqlVerificationShape` يفشل على أيّ ملفٍّ جديدٍ يخلطهما.
--
-- ⇒ **والسابقةُ محسومةٌ في المستودع:** ٠٧٩أ أودع تعليقين عربيّين بلا قراءةٍ
-- راجعة، **وقرأهما ٠٧٩ب_٢ في ملفٍّ منفصل.** فالقراءةُ الراجعةُ هنا في `106b`.
-- ==========================================================================

-- 🔴 **التعبيرُ والتعليقُ أدناه صُحِّحا بـ١٠٧ — وهذا الملفُّ شُغّل، فهما موجودان
-- في القاعدة الآن.** الخطأ: `is_void := not (select l.is_live …)`.
-- **و`is_live` تُقصي شيئين لا شيئًا واحدًا** — أن يكون المستندُ عكسًا لغيره،
-- وأن يكون قد عُكس. **واللغوُ لا يُقصي مستندَ العكس نفسَه.** ⇒ **فغرامةٌ على
-- مستندِ عكسٍ كانت تُقرأ «لاغية» و`voided_by_document_id` بجانبها عدم.**
--
-- ⚠️ **والنصُّ كان في المستودع، `079a:160-162`**، بتعليقٍ إنجليزيٍّ فوق التعبير:
-- *"Both halves are needed: reversing a document retires the PAIR, not one of
-- them."* **وترويسةُ هذا الملفّ تستشهد بـ٠٧٩أ:١٦٣ — السطرِ الملاصقِ له.**
--
-- **والتصحيحُ يُشغَّل بسكربتٍ مستقلّ (١٠٧)** لأن ما نُفِّذ لا يُعاد كتابتُه —
-- سابقةُ ٠٤٣ ⟵ ٠٤٦: يُضاف فوق الجملة، ولا تُمسّ الجملةُ نفسُها.
-- ==========================================================================

-- ⚠️ `with (security_invoker = true)` مكتوبةٌ صراحةً — البند ٦: أيّ
-- `create or replace view` يعيد كتابة الكائن كاملًا، **وكلُّ خاصّيّةٍ لا تُذكر
-- معرَّضة.** وضياعُها هنا يعني أن الـview يعمل بصلاحيّة مالكه، **فيرى كلُّ
-- صالونٍ غراماتِ كلّ صالون** — والفشلُ صامتٌ تمامًا ما دام في القاعدة صالونٌ
-- واحد.
create or replace view public.stock_fine_voidness
  with (security_invoker = true)
as
select
  f.id           as fine_id,
  f.salon_id,
  f.document_id,
  f.storage_id,
  f.employee_id,
  f.attribution,
  f.resolution,
  f.role_at_resolution,
  f.fine_percent,
  f.fine_basis,
  f.created_at,

  -- الجوابُ نفسُه، منفيًّا عن حيويّة المستند لا محسوبًا من جديد.
  not (select l.is_live
         from public.stock_document_liveness l
        where l.document_id = f.document_id
          and l.salon_id    = f.salon_id) as is_void,

  -- ومَن ألغاها — فالشاشةُ تستطيع أن تقول «أُلغيت بالمستند كذا» لا «لاغية» فقط.
  (select l.reversed_by_document_id
     from public.stock_document_liveness l
    where l.document_id = f.document_id
      and l.salon_id    = f.salon_id) as voided_by_document_id

from public.stock_fines f;

comment on view public.stock_fine_voidness is
  'كل غرامة، ومعها هل هي لاغية. اللغو معناه إن مستند الجرد اللي كتبها انعكس — والجواب بينشتقّ من stock_document_liveness ما بينخزّن، متل ما قرّر تعليق stock_fines من أول يوم: حالة أقل، وحقيقة ما بتتقادم عن الشي اللي بتوصفه. ⚠️ وبيوسم ولا بيصفّي: الغرامة اللاغية لازم تضلّ ظاهرة، لأن اللي انكتبت عليه بدّه يشوف إنه تبرّأ، مش إنها اختفت. ⚠️ والحيويّة بتنقرأ باستعلام قياسي فرعي لا بـjoin: صفّان بمنظور الحيويّة بيرفعوا خطأ هون بدل ما يخلّوا الغرامة تنعدّ مرّتين بصمت — وهاد التحذير مكتوب بتعليق stock_document_liveness نفسه. ⚠️ وما في coalesce على is_live: العدم معناه إنه ما في صفّ مقابل بالحيويّة — وهاد مستحيل بتلات شروط مع بعض: مفتاح stock_fines للمستند مركّب على (document_id, salon_id)، وview الحيويّة إسقاط كامل بلا where، وdocument_id إجباريّ — لأن العدم فيه بيخلّي المقارنة ما بتتحقّق أبدًا، والمفتاح المركّب ما بيسدّها (MATCH SIMPLE بيمرّق العدم). التلاتة مقيسين بـ106b لا مفترضين، والتالت بـattnotnull لأنه ما بيظهر بـpg_constraint. ولو ظهر عدم يومًا فهو خبر لازم ينشاف مش ينستر: شاشة بتفلتر is_void = true أو = false بتخفيه من الاتنين معًا.';
